# Handoff: mayor 2026-08-17T11:20:00-08:00
Model: claude-opus-5[1m] — **SELF-REPORTED, not launcher-derived.** `mayor.sh` pins no
model and the `session.start` at 07:29:10 carries no model in its payload; Ilva noted
this in two reviews today. `fortkit-ph4g` is open for exactly this and `fortkit-j8ww`
for the adjacent alias problem.

## State of work

**Band 2 of the parity gate closed its blocking finding, three Forge beads merged, the
seat lint shipped, standing order 12 was corrected in the capital and adopted by both
elder forts, and a number-collision sweep found a comment that is correct in exactly one
of the four places it lives. Eight beads closed, twenty-six filed.**

| bead | state | outcome |
|---|---|---|
| `x508` | **CLOSED** | Seat-file lint built, two Warden rounds, merged `0f6e37a`+`eabbcc9`. Round-1 blocker was mine. Discharges `8rh` and `fd2`'s enforcement halves. |
| `wg8w.1` | **CLOSED** | Band 2's first sitting. Its blocking finding repaired in `wg8w.2` and re-reviewed. |
| `wg8w.2` | OPEN | B2R2 Regent sitting, Overseer present, four items + two found in-sitting. Warden APPROVE-WITH-FINDINGS. `bd` refuses closure while `rw86` and `mm7m` owe template halves — correct. |
| `naju` `obva` `qfnf` | **CLOSED** | Factory repairs from the two sittings, Warden-reviewed. `naju` unblocks `vhk.7`. |
| `opae` `hgbe` | **CLOSED** | Forge-built, Warden-reviewed, merged `--no-ff`. |
| `9l7z` | OPEN, merged | Diff correct, but its own acceptance criterion is unmet. Blocked on `tcmg`. |
| `hpmn` | OPEN | Capital SO12 worked example corrected, `dbe4639`, `charter.amended` emitted. Wants a Warden review before closing (SO9). |
| `6fga` | OPEN | Overseer-approved. **Both elder forts adopted.** Proofdelve SO16 (`ForgeOs-k4y2`, `a1e24b2`), Farlantern SO17 (`longburn-lr3z`, `cf68da4`+`232a76a`). |
| `ihwu` | **CLOSED** | Duplicate of `xgul.8.1` item 6, filed by me before checking. Evidence moved there. |

**Filed today (26):** `hpmn` `tcmg` `x02m` `a355` `881h` `e69p` `62kp` `6xjy` `smmw`
`khlu` `qd76` `t50x` `dwlv` `cch1` `ff2j` `lrsu` `ax86` `f3i1` `z85k` `upvo` `m2tj`
`iacn` `mm7m` `fotv` `wg8w.2` `9l7z`.

**Seven Warden reviews, all by Ilva, every one found something.** `wg8w.1`
REQUEST-CHANGES · `x508` REQUEST-CHANGES then APPROVE · `wg8w.2` APPROVE · `opae`
APPROVE · `hgbe` APPROVE · `9l7z` APPROVE-but-do-not-close.

## Verified facts

- **`CI=1 fort/scripts/verify.sh --no-emit` is exit 0, 16 files / 188 tests** in this
  checkout, and **also exit 0 with `HOME` pointed at an empty directory**, which is how
  the runner and the Warden's scratch see it. Both measured. Round 1 of `x508` shipped
  an unconditional exit-0 claim from one environment only; that is corrected on the bead.
- **`-g` is inert for `%cd`.** `git log -g -1 --format=%cd` and the same without `-g`
  both return `2026-08-13 22:17:58 -0800`; `%gd` returns `22:18:05`. Measured by Ilva,
  by me, and by Calder across three forts. This is `rw86`'s repair reintroducing `rw86`'s
  own class, now fixed.
- **Seat-lint positive control goes red end to end.** Pointing `FORT_REGISTRY` at a
  scratch registry whose sibling fort claims Emrith turns the real verifier red at the
  seat-lint step, exit 1, with no repository file edited.
- **The memory structure is already in both elder forts**, measured by listing them:
  ForgeOs 53 facts, longburn 32, both with `current.md`, `facts/`, `index.db`. The
  Researcher is **not**: three seat files each, no `researcher.md`.
- **The template is behind the live forts in at least four places** (`mayor.sh` marker,
  `forge.sh` and `warden.sh` guards, `status.sh`). Both elder Mayors found this
  independently on the same morning without seeing each other's work.
- **`.claude/settings.json:112` is `Edit(//home/justin/.codex/**)`** — double slash,
  inert. `fortkit-62kp`.
- **Standing-order numbering collides across all three forts** at 7, 9, 11 and 12. Full
  table on `fortkit-smmw`.
- **Both elder amendments are committed, announced and verified.** Proofdelve `a1e24b2`,
  verifier exit 0 / 152 passed / 3 skipped with the skips disclosed; Farlantern
  `cf68da4`+`232a76a`, two `charter.amended` events, verifier green twice.

## Next actions

1. **`tcmg` needs the Overseer's eye and blocks `9l7z`.** The factory charter lists three
   seats and carries no Researcher outbound-channel residual, so every founded fort ships
   a WebFetch-capable seat its constitution never mentions. Composing that row is real
   SO12 judgement: the capital's paragraph is half architecture, half this fort's history.
2. **`6xjy`, P1, needs a ruling or a measurement.** The template says
   `--dangerously-skip-permissions` bypasses ALL permission checks including deny lists;
   Proofdelve says most. `fortkit-3jv7` leans on a deny set as its SOLE mitigation. Do not
   converge the two comments to agree before establishing which is true.
3. **`62kp`** — the capital's inert `~/.codex` deny. `.claude/` is kernel-read-only, so
   Overseer or Regent. Check both elder forts' spellings while there.
4. **The template halves are Band 3's precondition, not Band 2's cleanup**: `x02m`,
   `a355`, `881h`, `e69p`, `xgul.8.1`, `y0ff`. Forge lane, dispatchable, `y0ff` serialised
   behind whatever touches `settings-permissions.json`.
5. **Decide whether Band 3 is bidirectional before it opens.** Recorded on `wg8w`. A
   one-directional pass today would regress both elder forts, revert a Regent edict, and
   report success, because nothing fails and no test asserts these strings.
6. **`smmw`** — four `warden.sh` copies cite standing order 12 for falsified review
   records; correct only in Proofdelve. Three live copies are Regent lane, the template is
   Forge. Proofdelve also owes an appended correction on four event notes citing SO7.
7. **Nothing is pushed anywhere.** 18 capital commits by the tracking ref, plus commits in
   both elder forts and `bd dolt push` owed in both since 08-14.

## Open risks / questions

- **`git ls-remote` fails from a masked seat** (publickey denied), so every push number in
  this handoff is this clone's last sync and not the remote. That is `rw86`'s open second
  half (`f3i1`), and it means the fort still cannot answer "are we behind" from inside.
- **`ff2j`** — two independent Forge sessions reported `seats.test.ts` red under the codex
  posture while four runs outside it are green. If real, every future Forge bead draws a
  red verifier unrelated to its diff. Reproduce before theorising.
- **Four of my own claims drifted from their subjects today**, each caught by a control
  independent of me: Ilva twice, a test fixture once, and the Overseer asking a second
  time once. Knowing the rule prevented none of them. See Failed attempts.
- `wg8w.2` and `hpmn` both want a Warden review before closing.

## Failed attempts

- **`test -w` is not a writability test for a directory's contents.** I probed both
  `test -w` and an actual `touch` before editing; they agreed here, but cycle 13 records a
  case where they did not. Probe by creating, not by asking.
- **`bd` cannot read another fort from a masked seat.** Embedded Dolt writes a `LOCK` file
  even to serve a read, so `bd show ForgeOs-k4y2` fails EROFS. Read their event streams
  and files instead; both are open.
- **`main..bead/x` (two-dot) is the wrong ref-range for a Warden review** and it is what
  `warden.sh`'s own usage line recommends. It made three reviews open with a phantom
  revert of a charter amendment. Use three-dot. `fortkit-khlu`.
- **I filed `ihwu` without searching first** and duplicated `xgul.8.1` item 6.
- **I asserted the template charter carried a false clause without opening it.** It does
  not; its SO12 is worded generically. `hpmn`'s scope halved after I read the file I had
  already written a bead about.
- **I read both elder Mayors' charter amendments once and reported them as excellent**,
  and found four countable imprecisions only after the Overseer asked a second time. All
  four were findable on a first careful pass. The check that caught them was not care.
