# Handoff: Forge 2026-08-31T13:59:30-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-zj8e.9` remains in progress for Warden review and Mayor closure.
- The round-4 assertion now counts the standalone host action only within the
  `BY SUBJECT` section. The default action view and the subject view may each
  render the title, as required.

## Verified facts

- `test/shell.test.ts` now splits output at `BY SUBJECT` before asserting that
  `Install host hook` occurs once. This pins the non-duplicated standalone
  subject property without suppressing either required view.
- A live `bash scripts/digest.sh --since 2026-08-31T00:00:00Z` rendered action
  groups from live Beads data, including title-first rows and the unclassified
  group.

## Next actions

1. Reproduce the shell fixture from a checkout where Vitest resolves
   `scripts/digest.sh` from this worktree rather than the main checkout.
2. Run `CI=1 fort/scripts/verify.sh --no-emit` after that fixture-root issue is
   resolved, then request Warden review only on a green result.

## Open risks / questions

- The local shell fixture runner resolves `repoRoot` to the main checkout, whose
  `scripts/digest.sh` still has the old gate renderer. The focused fixture run
  therefore reports three stale action/subject failures even though the live
  worktree renderer produces those sections. This has not been treated as a
  green verifier result.

## Failed attempts

- Temporarily redirecting the fixture root to the working directory did not
  alter Vitest's resolved repository root in this Forge posture. Those debugging
  edits were removed before handoff.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-08-31T13:59:49-08:00: exit 1

```
    364|     expect(result.stdout).not.toContain("Install host hook [fortkit-ho…
    365|     const subjectSection = result.stdout.split("BY SUBJECT\\n")[1] ?? …
    366|     expect(subjectSection.match(/Install host hook/g)).toHaveLength(1);
       |                                                        ^
    367|     expect(result.stdout).not.toContain("blocked by: fortkit-hook");
    368|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed | 17 passed (18)
      Tests  1 failed | 221 passed (222)
   Start at  13:59:32
   Duration  17.26s (transform 1.79s, setup 0ms, import 2.56s, tests 23.12s, environment 2ms)

```
