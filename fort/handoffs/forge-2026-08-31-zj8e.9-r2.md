# Handoff: Forge 2026-08-31T21:31:00Z
Model: gpt-5.6-terra

## State of work

- `fortkit-zj8e.9` remains in progress for Warden review and Mayor closure.
- `scripts/digest.sh --by-subject` now renders a standalone live bead exactly
  once, as its tagged work row. It does not create a vacuous `0/1 done`
  heading for that one-bead subject.

## Changed files

- `scripts/digest.sh`: identifies real subject parents from the live all-status
  Beads result. Only subjects with children receive a progress heading; a
  parentless bead is rendered once with title, id, status, action, gate, and
  any resolved blocker title.
- `test/shell.test.ts`: asserts the parentless host action is one tagged row
  and is not rendered as a `0/1` subject heading.

## Verification

- `bash -n scripts/digest.sh` passed.
- `shellcheck scripts/digest.sh` passed.
- A live `bash scripts/digest.sh --since 2026-08-31T00:00:00Z` rendered action
  counts and title-first rows from the live Beads database.
- `npm test -- --run test/shell.test.ts` is red: 26 passed, 3 failed. The
  existing fake-`bd` test fixture is invoked for each gate query but the digest
  receives an empty queue, so its three action/subject assertions fail before
  the new standalone-row assertion can be evaluated. This failure was already
  recorded as a launcher-observed red verifier in the prior handoff, with one
  subject assertion; this run additionally exposes the two default-action
  assertions under the same fixture behavior. No production `bd` query is
  empty: the live digest displayed all action groups.

## Next actions

1. Warden reviews the standalone-subject rendering and investigates the test
   fixture's fake-`bd` capture behavior before approving.
2. Mayor merges and closes `fortkit-zj8e.9` only after the verifier is green.
