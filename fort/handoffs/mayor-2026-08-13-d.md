# Handoff: mayor 2026-08-13T22:05-08:00
Model: claude-opus-5[1m] — SELF-REPORTED (`fortkit-ph4g`: mayor.sh pins no model). `session.end` hand-emitted (`fortkit-t9iw`: mayor.sh execs, so its EXIT trap has never fired in any fort).
Predecessor: `mayor-2026-08-13-c.md`.

**READ THIS FIRST IF YOU ARE THE NEXT MAYOR.** Only `## State of work` and
`## Next actions` reach the distilled view (`scripts/consolidate-memory.mjs:253`).
Everything load-bearing is in those two sections deliberately. The rest is for a
human or for a successor who opens this file directly.

---

## State of work

**Band 2 of the parity gate is decomposed, its three Overseer decisions are ruled and
recorded, one of its beads is merged and closed, and the band's critical path is now a
single Regent sitting of six items. Both elder forts closed out their E10 reviews. Nine
capital beads filed, seven of them from things that did not exist as knowledge this
morning.**

| bead | outcome |
|---|---|
| `n3bk.2` | **Forge-built, Warden-reviewed, merged, CLOSED.** `d459998`+`9822f21`, merge `1cff668`. Post-merge main verified green. |
| `8ib` | **Mayor-built, THREE Warden rounds, CLOSED.** `c220b31` → `7d9493a` → `6557e29` → `a9317e3`. Unblocks `fd2`. |
| `0po6` `8ib` `8rh` | **Overseer decisions D1/D2/D3 ruled and recorded as bead comments** + one `overseer.decision` event. |
| `naju` | **FILED P1.** The factory installs no Researcher. `bin/fort-init:31,32,46` render three enumerated lists; all four Researcher templates exist and none is in any of them. Extracted from `vhk.5.4` f14 where the port would never have found it. |
| `domm` | **FILED P2.** Band 2 exit measurement, folded into the founding smoke at `test/shell.test.ts:25-46`. Blocked on `naju`+`0po6`+`n3bk.2`+`fd2`+`opae`. |
| `6fga` | **FILED P1 `gate-1`.** SO12 is in the capital charter and the template only; NEITHER elder charter has it. Band 3 prerequisite — Band 3 is the converging band and SO12 is the law against erasing a citizen. |
| `ugw4` | **FILED P2.** Only `mayor.sh` sets `FORT_MASKED`, live and template, so E10's in-mask refusal covers one seat of three. Tova's finding, verified here. |
| `opae` | **FILED P2.** `templates/fort/charter.md:13` and `templates/fort/seats/mayor.md:14` still promise the retired Shape A carve-out. A fort founded today tells its Mayor she can write a file her mask refuses. |
| `y0ff` | **FILED P3.** Template deny list lost its alphabetical sort. |
| `rw86` | **FILED P2.** Every fort's `status.sh` reports push state from the local tracking ref. Found independently by BOTH elder Mayors. |
| `52vf.12` `px7e` | **SO7 corrections appended** (Sereth's finding 1: the "4 of 5 FAIL" headline describes the probe's rendered copy, not the shipped script). |
| `uj3q` | **Two comments, five measured instances.** The diagnosis changed — see Next actions item 6. |
| `wg8w` | **Lane correction comment.** Band 2 is mostly REGENT work, not Forge work. |
| `52vf.4` | Ilva's attribution finding + the Mayor-lane provenance gap. |
| `agf` | Fresh evidence: `agentdb.rvf` present in ForgeOs, contradicting the "dormant 7 days" correction. |

**ELDER FORTS, both closed out by their own Mayors under a scope fence, both held it.**
Proofdelve `ForgeOs-pqii` CLOSED, 5 beads filed (`5a5q` `jirx` `tfwm` `kiae` `5fyc`).
Farlantern `longburn-holk` CLOSED, 4 beads filed (`nogj` `5yc6` `2x6w` `70v9`).
Gate sign-offs recorded verbatim in both. Neither claimed anything from `bd ready`.

**Six Warden reviews across three forts: Ilva ×4, Tova ×1, Sereth ×1. Every one found
something. No blocking finding anywhere.**

**Capital: level with origin. Bead data Dolt-synced (`refs/dolt/data` = `df35026`).**

---

## Next actions

1. **THE REGENT SITTING IS THE CRITICAL PATH AND NEEDS THE OVERSEER PRESENT.** Six items,
   every one in a file kernel-read-only to all masked seats (`bin/`, `fort/scripts/`):
   `naju` (factory installs no Researcher) · `0po6` (the `{{HOME}}` placeholder in
   `render()`, D1) · `8rh` (roster collision refusal, D3) · `fd2` (moot scaffolding half)
   · `ugw4` (`FORT_MASKED` in `warden.sh`+`forge.sh`) · `rw86` (`status.sh` in three live
   copies). Brief them together — they all edit the same two files, and the Regent is
   attended-only and therefore scheduled rather than queued.

2. **THREE FORGE BEADS ARE DISPATCHABLE NOW, all `templates/` only, all independent, none
   needing the Overseer:** `opae`, `fd2`'s template half (the inherited Farlantern
   personality blocks + the `emit.sh` header naming Proofdelve), `y0ff`. Briefs for
   `n3bk.2` and `domm` are in those beads' `design` fields — `forge.sh:74` feeds
   `bd show` to Kethra verbatim, so THE BEAD IS THE BRIEF. Write them that way.

3. **FOUR ITEMS THE OVERSEER MUST DO BY HAND — a masked Mayor cannot.** Proofdelve:
   `bd dolt push` + commit the uncommitted push event. Farlantern: `bd dolt push` + push
   `a72daf6`. Nine beads and two closures currently exist only in local Dolt on one disk.
   Both elder Mayors correctly refused to stretch a git-push authorization into a Dolt
   sync. See the core fact `a-masked-mayor-session-cannot-launch-the-herald`.

4. **`domm` AFTER Band 2, and it is what turns "beads closed" into "factory correct".**
   It founds a real fort and measures. Its brief carries the trap that would otherwise
   cost someone an afternoon: `bin/fort-init` writes the registry to
   `$HOME/.claude/civilization.json`, which is kernel-RO, so **founding from a masked seat
   fails EROFS** — pass `FORT_REGISTRY` at a writable path (`seat-sandbox.sh:264-271`).

5. **THEN BAND 3**, where the Researcher finally reaches both elder forts. `vhk.7` now
   correctly shows THREE blockers instead of one: `vhk.5`, `naju`, `6fga`. Two of those
   surfaced today. **Do not open Band 3 before `6fga`** — Band 3 converges forts toward
   the template, `qu46` proved that direction can erase a living citizen, and neither
   receiving fort is under SO12 yet.

6. **READ THE TWO NEW `uj3q` COMMENTS BEFORE TRUSTING YOUR OWN MEASUREMENTS.** The core
   fact `read-the-artifact-remember-the-why` was extended today (`84893f6`) and the
   framing CHANGED: the class is **a claim and its subject drifting apart**, reached both
   by measuring a proxy AND by editing the subject and leaving the claim. Five instances
   in one evening, four of which involved running a command whose output could not have
   discriminated the property claimed. The cheap test: **what result would have falsified
   this?** What caught them every time was a control independent of the person making the
   claim — never care, never knowing the rule.

7. **Standing:** `34eg` (`docs/specs/backport-loop.md` contradicts the instrument) before
   Band 2 seats read it; `fnjn`'s sweep before any parity number is taken as a number.

---

## Verified facts

- **`8ib`'s premise was half stale when picked up.** `scripts/verify-impl.sh:150` already
  linted the templates (added under `ddvo` and `n3bk` f4, neither citing this bead);
  `shellcheck -x` on them exits 0; `templates/fort/scripts/verify.sh` already exists.
  A `fnjn` instance, recorded on the bead.
- **The rendered lint is not vacuous, demonstrated not asserted.** Planting
  `[ -d {{REPO_PATH}} ]` in `templates/fort/scripts/status.sh`: plain CLEAN, spaced
  SC1072+SC1073, empty SC1019+SC1072+SC1073. The plain pass alone misses it.
- **`bin/` is kernel-RO to the Forge**, not just to the Mayor. `seat-sandbox.sh:187-203`,
  guarded on `civ/covenant.md`, sits ABOVE `case "$seat"` at `:205` so it binds both
  branches, and it reaches the Forge's worktree deliberately (`:178-184`).
- **Both elder repos are unwritable from a masked Mayor**, measured by `touch`, not
  inferred. `bd -C` there fails `openat LOCK: read-only file system`.
- **Both Warden transcripts DID reach their beads** — inferred from control flow
  (`bd comment` precedes the `review.verdict` emit under `set -euo pipefail`, both emits
  fired), NOT read directly. The elder forts have no passive bead export (`or2.1`).
- **`02b36e2` in ForgeOs is the Overseer's own hand-push** at 21:25:44/21:25:47, one file,
  four insertions, author `Justin Schneider <justincase18@msn.com>`. Not an anomaly.
- **There is no push-drift watcher to extend.** `civ/scripts/` holds five scripts and
  `ls-remote` appears in none of them, nor in `scripts/` or `bin/`.
- Verifier green 8/8, 180 tests, at every commit. Capital pushed and verified with
  `ls-remote` against the real remote, not the tracking ref.

---

## Open risks / questions

- **`8ib` closed with round 4 UNREVIEWED** (comment-only). The Overseer took that call
  knowing round 3's own defect was also a comment. Stated, not buried.
- **A typo is permanent in Proofdelve's stream**: `guard必 precedes cd` in a `bead.filed`
  detail. Marrek tried to correct it, the mask correctly refused, append-only held.
- **`agentdb.rvf` + `.lock` are present in ForgeOs**, contradicting `agf`'s dormancy
  correction. **Deliberately not stat'ed from this masked session** — host mtimes read
  from a sandbox is exactly the false measurement that cost session B. Next unmasked seat
  should stat them and record the answer on `agf`.
- **The Dolt push was not independently verified.** The tool said `Push complete.` and
  `refs/dolt/data` = `df35026` exists, but the pre-push ref was not captured, so the ref
  cannot be proven to have advanced. **Next Dolt push: capture the ref first, compare
  after.** Recorded here as the baseline.
- **The Mayor lane produces no launcher-written provenance** — no `work.begun`/`work.ended`,
  no handoff, no model stamp for its own code changes. Ilva reviewed `c220b31`
  unattributed and said so. Routed to `52vf.4`; not fixed.

---

## Failed attempts

**Three regressions of mine, all in `8ib`, all caught by the Warden and not by me.**

1. **`trap ... RETURN` inside a function.** It PERSISTS past the function and fires again
   on the next function return, where `$tmp` is unbound. `set -u` turned main RED.
2. **`trap '...' INT TERM` with no `exit` in the handler.** Bash defers, runs, and
   RESUMES — the verifier became non-terminating on TERM, a gate `forge.sh:149` and
   `.github/workflows/ci.yml:29` both run. Worse than the leak it fixed.
3. **A scripted removal deleted the variable and left its seven-line comment.**

The interrupt-leak observation that prompted all three is now DECLINED, with both failures
and the conditions a fourth attempt must meet recorded at `verify-impl.sh:56-79`. **If you
are about to add a signal trap to the verifier, read that block first.**

**Two measurement errors I reported to the Overseer before catching them:** a `bd` error
message grepped for content, its zero matches read as "the beads carry no findings"; and
`timeout`'s exit 124 read as "the process survived", when 124 is what `timeout` returns
whenever it fires.

**And the premise error that cost two elder Mayors a decision each:** I wrote "19 commits
ahead" and "24 commits ahead" into prompts from `git rev-list origin/main..main`, which
measures this clone's last sync and not the remote. Both numbers were true when taken and
false when read. Marrek and Vardis each caught it independently with `ls-remote`. That is
`rw86`, and it is the single most useful thing anyone found today.
