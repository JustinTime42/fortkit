# Handoff: Mayor 2026-08-03T23:26-08:00
Model: Claude Opus 5 (1M context), as Emrith Cairnwright, first occupant of the seat

## State of work

The Founding Moot is convened, tallied, proclaimed, and closed. The fort is **Manyhalls**. No product code exists yet; this session produced identity, law, and a triaged queue.

Beads filed this session (five, all open, none claimed):

- `fortkit-be4` (decision, P1) — explicit immutable actor ids, separate from mutable display names. Root cause under Ilva's collision. Blocks `fortkit-aam`; related to `fortkit-nd6`, `fortkit-8rh`, `fortkit-fd2`.
- `fortkit-8rh` (feature, P1) — fort-init refuses rosters whose actor ids sit within edit distance 1. Related to `fortkit-vlj`.
- `fortkit-aam` (feature, P2) — the viewer never renders two actors indistinguishably. Blocked by `fortkit-be4`; related to `fortkit-bf0`, `fortkit-bzx`.
- `fortkit-fd2` (bug, P1) — fort-init ships three Farlantern citizens' personal declarations as default seat personalities. Related to `fortkit-vlj`, `fortkit-or2`, `fortkit-be4`, `fortkit-b37`.
- `fortkit-b37` (bug, P1) — all three Manyhalls seat files carry two personality blocks each. Proposed, not applied: constitution-tier under charter gate 1.

Not touched: the founding nine (`fortkit-gzn`, `jmq`, `bf0`, `bzx`, `nd6`, `or2`, `dtn`, `vlj`, `zgp`).

## Verified facts

Each checked in this session rather than assumed.

- Fort named Manyhalls, 7 points of 18, margin 2, the only name on every ballot cast. Full tally, both self-vote declarations, and the disinterested-ballot check that cuts against the convener are in `fort/annals/founding-moot.md` (9171 bytes, eleven sections).
- Registry `/home/justin/.claude/civilization.json` carries `"fort_name": "Manyhalls"` for fortkit. Verified by grep, not by report.
- `fort/charter.md:1` reads "Manyhalls Charter".
- Roster carries all three founders with pronouns and both rename histories: `fort/seats/mayor.md:3` (Emrith Cairnwright, she/her), `fort/seats/forge.md:3` (Kethra Anvilmark, she/her, prior name Kethra), `fort/seats/warden.md:3` (Ilva Trueglass, she/her, prior name Kestra Trueglass, actor id `ilva`).
- Ten events emitted by this seat into `fort/events/events-2026-08-03.jsonl`: `moot.convened`, two `seat.named`, `moot.ballot`, `moot.named` (payload carries the full tally and the convener-conflict note), five `bead.filed`. Verified by tail. The emitter is declared in the annals so nobody writes that stretch twice; this fort already carries a double-filing scar from earlier the same day.
- `fortkit-fd2` is real and reproducible: `grep -c 'Personality (in their own words)' fort/seats/*.md` returns 2 for all three files, and `templates/fort/seats/{mayor,forge,warden}.md:3` carry Vardis Slowfathom's, Orin Slowfire's, and Sereth Twicewalked's words verbatim. Every fort fort-init founds inherits them.
- Evidence for `fortkit-be4` is in our own canonical schema: `schema/events.md:11` documents actor examples `marrek`, `veyra`, `tova`, three given names serving as primary keys for another settlement.

## Next actions

1. `fortkit-b37` to the Warden and the Overseer. It is a three-line deletion behind gate 1, and until it clears, the roster of the fort that builds rosters declares two characters per seat.
2. `fortkit-gzn` (CI from commit one) before `fortkit-jmq` (the maiden bead). `fort/remember.md` already records that no verifiers exist, and the charter forbids closing a bead without verifiers green. The maiden bead cannot legally close until CI exists.
3. `fortkit-jmq` — data readers plus `fortkit status`, the first product code and the Forge's maiden run.
4. `fortkit-be4` before `fortkit-aam`, since the viewer cannot safely render an identity the schema has not defined.
5. `fortkit-fd2` alongside `fortkit-vlj`, which already scopes fort-init hardening and moot scaffolding.

## Open risks / questions

- **For the Overseer:** `fortkit-b37` needs a human gate opened. It is the only item blocking a clean roster.
- Nothing in this repo is committed. This session wrote `fort/handoffs/mayor-2026-08-03.md`, five beads, and ten event lines; the annals, roster, charter, and registry were written by others under the Overseer's founding order. Committed, pushed, and deployed are separately verified states (standing order 6) and none of the three has been checked for this work.
- `fortkit-be4` changes the identity key of the canonical schema that other settlements vendor. Add-only means existing events keep their actor strings and a mapping is appended. Proofdelve and Farlantern both have live streams keyed on given names; whoever takes that bead should read their `fort/events/` before deciding, because our schema change is their migration.
- Left unsettled on purpose, recorded in the annals as ruling R6: three founders independently reached for a glass name and the elected name has none. That settles what the fort is called and nothing about what the viewer is called. `fortkit-zgp` still holds the product-naming question.

## Failed attempts

- I first linked `fortkit-fd2` as a blocker of `fortkit-b37`, reasoning from the charter's fix-the-class rule. Wrong edge: it would have held a live roster defect hostage to a template rewrite. Removed and replaced with a relates-to link. Successors should read the charter's amendment rule as being about where the *fix* goes, never about what an instance is allowed to wait for.
- No other attempt failed, because no product code was attempted. That is itself worth recording: day one of this fort produced law, names, and a queue, and the fortress it exists to render does not exist yet.

## Appended after filing (Emrith Cairnwright, same session)

Everything above stands as written, per standing order 7. These facts changed or were found afterward.

- **`fortkit-b37` is closed**, fixed exactly as proposed: the second personality block deleted from each of the three seat files, nothing else. Verified: `grep -c 'Personality (in their own words)' fort/seats/*.md` now returns 1 for all three. The templates were decontaminated in the same commit and `templates/fort/seats/mayor.md:3` now reads `**Held by:** {{UNFILLED — set at the Founding Moot}}`. `fortkit-fd2` stays open for the real fix, the replace-not-prepend moot scaffolding and the registration check. The next-action list above is therefore short by one: item 1 is done.
- **`fortkit-1np`** (art pipeline research, pre-Limner, P2) was filed by another hand after this handoff was written.
- **`fortkit-dpu` filed (bug, P1): the two sources the viewer joins disagree about what day it is.** Found while verifying that b37 had closed. Same act, two calendars: `bd show b37 --json` reports `created_at 2026-08-04T07:23:59Z`, while the event emitted for that same work reads `2026-08-03T23:24:56-08:00`. `fort/scripts/emit.sh` compounds it by naming its file from the local date, so all founding-day work after 16:00 local sits in an event file dated a day earlier than the beads recording it. A replay of founding day would show beads created on a day the stream says has not happened yet, and every fort founded in an evening inherits the split, since fort-init ships emit.sh. **This now blocks `fortkit-jmq`**, and that blocker is deliberate in a way the `fd2`/`b37` edge was not: the maiden bead builds the readers that join these two sources, so the basis has to be decided before the readers exist rather than retrofitted into them. Recommendation recorded on the bead for one-read approval: no record is actually wrong, since events carry explicit offsets and beads are UTC; the only ambiguous artifact is the filename, so readers group by parsed UTC and the schema declares the day-file a shard key rather than a fact. Not applied. Schema custody is vendored by two other settlements and wants the Warden or the Overseer.
- Three of the five beads I filed this session, plus this one, come from the same habit: identity and time handled implicitly by the factory. `fortkit-be4` (identity keyed on a given name), `fortkit-fd2` (character inherited from a template), `fortkit-dpu` (day inferred from a filename). Whoever works this queue should treat that as one theme rather than three chores. **Anything the factory leaves implicit, every fort it founds inherits.**

## Second appendix, and the last (Emrith Cairnwright, at session close)

- **`fortkit-dpu` is closed**, the recommendation adopted verbatim under the Overseer's schema gate. Verified at `schema/events.md:7`, which now declares the daily filename a shard key rather than a fact, requires consumers to group and order by parsed UTC, and names the `23:24-08:00` → `07:24Z` pinning case as a required test of any reader. **`fortkit-jmq` is unblocked**; its only blocker shows closed.
- The next-actions list above is therefore spent except for its last two items: `fortkit-gzn` (CI) and then `fortkit-jmq` (the maiden run), in that order, because no bead may close without verifiers green.
- Session totals, verified: 6 beads filed by this seat (`be4`, `8rh`, `aam`, `fd2`, `b37`, `dpu`), of which 2 are closed; 19 events emitted; 1 handoff written and twice appended. No product code, no commits. Committed, pushed, and deployed remain three separately unverified states.
- The last thing I want a successor in this seat to have is not in a bead. This fort's first three defects were all found by reading its own records back rather than by any test, and all three were the factory leaving something implicit. The seat that watches the forts should expect to find its worst bugs in its own roster, its own clock, and its own founding paperwork, because those are the artifacts nobody thinks to verify. Verify them.
