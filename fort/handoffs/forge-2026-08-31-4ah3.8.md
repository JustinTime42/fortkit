# Handoff: Forge 2026-08-31T08:01:06-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-4ah3.8` remains in progress for Mayor review. Added explicit,
  single-control fingerprint recording, regenerated five stale fingerprints,
  corrected the control-register README, and kept `verify-impl.sh` untouched.

## Verified facts

- `scripts/control-lint.mjs --record <control-key>` resolves the named control's
  repository-relative citation, re-reads its cited line, and only then writes the
  SHA-256 manifest entry. It refuses a missing file or line without writing.
- The five regenerated keys are `prose-gate-mayor-push-deploy`,
  `prose-gate-charter-amendment`, `prose-gate-seat-file-amendment`,
  `fence-researcher-tool-whitelist`, and `falsifier-researcher-probe`.
- `test/control-lint.test.ts` proves recording updates a stale line fingerprint
  and that an unresolved citation leaves the manifest byte-for-byte unchanged.
- `fort/scripts/verify.sh --no-emit` passed: 17 test files, 198 tests; TypeScript,
  browser TypeScript, Biome, ShellCheck, and template-render all passed.

## Next actions

1. The Mayor may wire `node scripts/control-lint.mjs` into
   `scripts/verify-impl.sh` beside the existing lint steps.
2. After that insertion shifts the seven `verify-impl.sh` citations, run this
   recorder once per affected key to derive their replacement fingerprints.
3. Review this commit; the Mayor closes the bead only after review and merge.

## Open risks / questions

- The recorder intentionally cannot bulk-accept the register. A future wiring
  change requires seven explicit recordings, which preserves per-control
  provenance but remains deliberate maintainer work.

## Failed attempts

- The initial focused test helper passed recorder arguments through a fixed
  positional shell wrapper, producing an empty untracked `fence-example` output
  file. It was inspected as empty and deleted immediately. The helper now uses
  an argument-preserving wrapper.
- On Node 24, `child_process.execFile` returned empty captured streams even when
  the child printed. This made seven pre-existing `seat-lint` assertions fail.
  The seat test helper now captures output through explicit redirection; its
  original assertions pass unchanged.
