# Handoff: mayor 2026-08-11T20:33:57-08:00 (session E)
Model: claude-opus-5[1m] — SELF-REPORTED, see caveat.

**Two header notes, both of which are this session's own findings applied to
itself.**

1. **The stamp above is a full ISO instant on purpose.** Sessions C and D wrote
   `2026-08-11T` with no time. That does not parse, so `consolidate-memory.mjs`
   silently sorts them to epoch 0 and the distilled view names the 2026-08-10
   handoff as the fort's latest. This is `fortkit-ybg7`. Writing a full instant
   is a WORKAROUND, not the fix; the generator must fail loudly instead.
   **Successors: keep writing full instants until ybg7 lands.**
2. **The Model line is self-reported.** `fort/scripts/mayor.sh` pins no model
   (`grep -c model` = 0), so no launcher record exists to stamp it from.
   `fortkit-ph4g`, decided this session and not yet built.

---

## State of work

**Main is GREEN (153 tests, 14 files, exit 0) and PUSHED.** `origin/main` ==
`HEAD` == `753ce12`, verified after the push, not assumed.

### THE HEADLINE, AND IT IS TOMORROW'S FIRST WORK

**Every seat in the civilization is being told to read a retired memory store.**
`fortkit-08ny` (P1, filed tonight) carries the full brief. Do not let this get
buried under the rest of the session; the Overseer asked specifically that it
survive the handoff.

Ten live `--append-system-prompt` strings name `fort/remember.md`, which has been
a pointer stub in Manyhalls since the 88u migration and is now a stub in all
three forts after today's Project A work:

| Fort | Seats |
|---|---|
| Manyhalls / fortkit | Emrith `mayor.sh:25`, Kethra `forge.sh:81`, Ilva `warden.sh:111`, Saelin `researcher.sh:29` |
| Proofdelve / ForgeOs | Marrek `mayor.sh:25`, Veyra `forge.sh:214`, Tova `warden.sh:42` |
| Farlantern / longburn | Vardis `mayor.sh:32`, Orin `forge.sh:100`, Sereth `warden.sh:123` |

Plus `bin/regent:157-164` and `bin/civ-index:141` (held by `fortkit-ztzs`), and
`fort/scripts/probe-cycle7.sh:28`, whose positive control for "writable" now
asserts a stub is writable — true, meaningless, and still reporting PASS. That
last one was held by no bead before tonight.

**Verified from the live process table**, not only from source: the running
Mayor sessions' `bwrap` command lines contain the string.

**THE DEFECT IS NOT THE WRONG PATH. IT IS A LIVE CONTRADICTION.**
`fort/seats/mayor.md:14` says read `fort/memory/current.md`.
`fort/scripts/mayor.sh:25` says read `fort/remember.md`. Both are standing
instructions to the same seat and they disagree. **This session followed the
seat file and reached the real ledger. That was luck, not design.** Nothing
tells a seat which to believe, and the launcher prompt is the one it cannot
skip. A successor following the prompt reads eight lines and concludes the fort
has almost no memory. A fix that repoints launchers without confirming both
surfaces agree leaves the contradiction class intact.

**IT COMPOUNDS WITH `fortkit-ybg7`.** The read side is broken at BOTH ends. The
launcher points at a stub; the distilled view a diligent seat reaches instead
names a two-sessions-stale handoff. Three forts migrated today with zero records
discarded and **none of it reaches a session until both land.**

One edict, not four beads: every target is kernel-RO to every masked seat in
every fort, so no fort can repair its own launchers. `fortkit-xgul.6`,
`fortkit-ztzs`, `ForgeOs-fkmm.6` and `longburn-wtx7.6` stay open and close FROM
that work. `xgul.6` has a scope correction on it recording that its "four
launchers" is the right local count and the wrong unit of work.

### Project A: both elder forts migrated, in their own forts, by their own seats

The Overseer launched Marrek and Vardis from his own terminal, because a masked
Mayor cannot launch a seat that writes outside this repo (probe run this
session: `touch` under `/home/justin/dev/ForgeOs` → **Read-only file system**).

- **Proofdelve (`ForgeOs-fkmm`)** — 43 durable records in, **49 facts out, zero
  discarded**. Cover check 32/32, run against `git show ef18a66:fort/remember.md`
  rather than the working copy. A second cover check ran over the 11
  `bd remember` keys BEFORE `bd forget`, since forget deletes. Gate 6 signed.
  `.1`–`.5` and `.7` closed; `.6` (launchers) is now part of `08ny`.
- **Farlantern (`longburn-wtx7`)** — 29 bullets to 31 facts, cover 29/29.
  Verifier exit 0 bare and unpiped, 255 tests. `current.md` regenerates
  byte-identically under `cmp`. **Vardis proved `memory-lint` is not vacuous** by
  planting a fact with six deliberate defects and confirming six named failures
  and exit 1. `.1`–`.5` closed.
- **Both Wardens re-derived the verification independently** rather than reading
  it off the bead. Both returned ESCALATE with **zero blockers**, escalating only
  because the diffs touched human gates.

### Charter amended

`fortkit-zpw8`. Standing order 7 now says WHERE a correction goes: **corrections
in bead `comments`, `notes` for working state.** Test: if a reader acting on the
original text would be wrong, it is a correction. Overseer approval recorded
verbatim on the bead BEFORE the edit; `charter.amended` emitted 20:35:19. Not
retro-migrated, because rewriting where a past correction sits would itself be
an edit in place. First application was the `2kub` correction comment the same
evening.

### Beads filed or decided this session

| Bead | |
|---|---|
| **08ny** P1 | the read-side edict brief above |
| **ybg7** P1 | `consolidate-memory` picks the WRONG latest handoff |
| **6ovg** P1 | the cycle-7 `verify.sh` carve-out does not work |
| **ij8j** P1 | `interactions.jsonl` read from the wrong path in every fort |
| **2kub** P1 | editor swap files defeat the `.env` inode mask |
| **j8ww** P1 | `warden.sh` pins the alias `opus`, not a precise id |
| **px7e** P1 | `mayor.sh` has no in-sandbox launch refusal |
| **zpw8** P1 | the charter amendment above |
| **nwl6** P2 | `memory-lint` has no near-duplicate fact-key guard |
| **ks3t** P2 | editor-residue gitignore + the factory gap |
| **zya9** P2 | `src/cli.ts` 755 is correct; record it, don't revert |
| `ph4g` / `w1ew` / `agf` / `xgul.6` | decisions and corrections commented |

## Verified facts

Everything here was run or read by this seat, not carried from a handoff.

- **`verify.sh` twice, green both times**: 153 tests / 14 files / exit 0, after
  the charter edit and again after the `.gitignore` edit.
- **`origin/main` == `HEAD` == `753ce12`** after `git push`, checked with
  `rev-parse` on both. SO6: committed ≠ pushed, separately verified.
- **This session is masked.** `touch /home/justin/dev/ForgeOs/.mask-probe-emrith`
  → `Read-only file system`. Same class as the herald ledger fact.
- **`fortkit-ybg7` is live in this fort.** `current.md` names
  `mayor-2026-08-10.md` as latest while sessions C and D exist. Cause measured:
  their headers are `2026-08-11T`, unparseable, so they sort to 0.
- **`consolidate-memory.mjs:319`** joins `root` + `interactions.jsonl`; beads
  writes `.beads/interactions.jsonl`. Confirmed modified in both elder forts.
  One of six §5 corpora is indexed in **no fort at all**.
- **The ruflo writer is identified**: `@claude-flow/cli`'s
  `dist/src/config/proven-config.js`, reached through the ruflo plugin hook
  shim's `npx --prefer-offline` fallback, which "always exits 0" by design and
  is therefore silent. **Writes have been dormant 7 days**; `enabledPlugins` is
  `{}`. The bead's "still live" was true on 08-05 and is false now (SO7
  correction appended). Residue captured verbatim to the bead before any
  deletion. npx cache dir is **1.9 GB**, declares `ruflo@latest` only.
- **No editor-residue file has ever been committed** in any of the three forts
  (`git log --all -- '*.kate-swp' '*.swp' '*~'`, empty). Clean by luck and SO4,
  not by design.
- **`.gitignore` verified both directions**: all eight residue spellings now
  match; `src/cli.ts`, `fort/charter.md` and `fort/events/*.jsonl` confirmed
  **not** ignored. The negative check matters more than the positive one.
- **`src/cli.ts` should stay 755**: it carries `#!/usr/bin/env node`,
  `package.json` names it as `bin.fortkit`, and it is the only executable file
  under `src/`.
- **`mayor.sh:6` already carries an EXIT trap** emitting `session.end`. The
  model stamp rides existing machinery; no wrapper needs building (`ph4g`
  Decision D).
- **`fort/scripts/mayor.sh` has no in-sandbox launch refusal**; `longburn`'s
  `mayor.sh:5-7` does (`longburn-5v4`). Zero matches across all 44 lines.

## Next actions (ordered)

1. **`fortkit-08ny` + `fortkit-ybg7` as ONE read-side repair.** They are the
   same failure from two ends and fixing either alone leaves the architecture
   inert. Regent lane. Verification must include launching one seat per fort and
   confirming the string **in the running process**, plus confirming each fort's
   seat files and launchers now agree.
2. **Seat-sandbox batch as a second edict: `2kub` + `1q9` + `6ovg`.** All three
   are one careful edit to the same kernel-RO file. Three separate passes over
   the fort's most load-bearing script is the avoidable risk. Overseer ruled
   `2kub` in scope (threat 1, not the human-adversary exemption); option (d) is
   closed and should not be re-litigated.
3. **Only after ybg7 lands: `88u.6` and `fci.6`.** Both inject the distilled view
   at session start. Landing them first mechanically injects a stale handoff into
   every session in the fort.
4. **The ruflo purge.** Script written and Overseer-approved, not yet run:
   `<scratchpad>/ruflo-purge.sh` — reproduce it from the `agf` comment if the
   scratchpad is gone; the ordered plan is on the bead. Must be run unmasked.
   `~/.claude/brain/` is explicitly NOT touched.
5. **`ks3t`** — the elder forts' gitignores and, more importantly, the factory:
   `find templates -name '*gitignore*'` returns **nothing**.
6. `ij8j`, `nwl6`, `j8ww`, `px7e`, `zya9` behind those.
7. `or2.2` (backport rubric + drift watcher spec) remains the highest-leverage
   non-memory Mayor work, still untouched across three sessions.

## Open risks / questions

- **`fortkit-2kub`'s file vanished and we cannot say who removed it.** A
  768-byte Kate swap shadowing staging secrets existed at 18:25, was orphaned
  (nothing held it open), and was gone within the hour. Not session c — its
  handoff states "not read, not committed, not deleted by this session" and it
  filed `ForgeOs-szai`. Not git — never in history on any branch. Most likely
  Kate itself on reopen. **Consequence: the rotation question is now unanswerable
  from evidence**, since the content was never inspected. Nothing watches for
  this; the charter lists a secrets scan among watchers "added as earned" and
  this earns one.
- **`ForgeOs` has 4 unpushed commits**, two of which (`ForgeOs-mskx`, touching
  `verify.sh`) are flagged in three consecutive handoffs as unconfirmed for
  review. Not this fort's to push.
- **A near-duplicate fact pair exists in Proofdelve** —
  `workos-has-two-applications-concepts` and `workos-two-applications-concepts`,
  both active, no supersession link, `memory-lint` passes. Theirs to resolve by
  supersession; the missing guard is ours (`nwl6`). Root trigger was two
  concurrent sessions writing one ledger, and the memory architecture has **no
  concurrency story at all**: no lock, no claim, no detection.
- **`src/cli.ts` is left uncommitted on purpose**, mode bit only. It is product
  code and `zya9` says the commit is the Overseer's or the Forge's call.
- **`assets/trial/*`** — 13 PNGs and a provenance manifest, still untracked,
  presumed Limner trial output for `fortkit-zcb`. Nobody has claimed them.
- A stray unmasked `claude --dangerously-skip-permissions` had been running
  since 2026-08-09; the Overseer identified and ended it. Worth a habit: an
  unmasked skip-permissions session with no fort identity should be asked about,
  not assumed.

## Failed attempts

- **This seat noticed the stale distilled view at session start and excused it.**
  The opening read showed `mayor-2026-08-10.md` as latest while two newer
  handoffs existed, and the seat wrote it off as "the consolidated memory is a
  bit behind" instead of measuring it. Marrek found the root cause in another
  fort hours later. **A noticed anomaly that gets an excuse instead of a
  measurement is a missed finding**, and the excuse was written into this
  session's own first message.
- **The seat asserted the elder-fort migration route before checking it could
  execute it.** It said "approved, proceed" on `xgul.4`/`.5`, then discovered on
  the next probe that a masked Mayor cannot launch another fort's Mayor at all.
  The ledger fact for the Herald says exactly this and the seat had read it that
  morning. Probe capability BEFORE promising action.
- **The seat over-cautioned on the `ph4g` stamping point**, calling it "a real
  design call, not a copy" before reading `mayor.sh:6`, which already has the
  EXIT trap that makes it trivial. The Overseer's question ("why do we need a
  session end log?") was the thing that prompted actually reading the file.
  Cheaper to read the 44-line script first.
- **The seat recommended reverting the `src/cli.ts` mode change** and reversed
  itself one probe later on finding the shebang and the `bin` entry. The
  recommendation was given before the file was opened.
