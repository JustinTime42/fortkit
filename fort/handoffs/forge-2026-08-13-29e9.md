# Handoff: Forge 2026-08-13T10:45:00-08:00
Model: GPT-5.6 Terra

## State of work

- `fortkit-29e9` remains IN_PROGRESS, as directed. The factory sandbox library now carries the E8 `~/.codex` mechanism.

## Verified facts

- `templates/fort/scripts/lib/seat-sandbox.sh` now has `CODEX_DIR_RW` for both seat types, binds the live `~/.codex` directory before later read-only and transcript masks, and no longer re-binds `auth.json` read-only.
- The template includes E8's `--mask-file` interface and ordering documentation, the finding-6 `-f` residual, the sourced-file `0644` rationale, and Claude-only `SSH_AUTH_SOCK` handling.
- `fort/scripts/verify.sh --no-emit` passed after `npm ci`: TypeScript checks, browser typecheck, Biome, 163 Vitest tests, and shipped-shell ShellCheck all passed.
- `bash -n` and scoped ShellCheck passed for the updated template library before the full verifier.

## Next actions

1. Submit the path-scoped commit for Warden review.
2. The Mayor may close `fortkit-29e9` only after review and merge.

## Open risks / questions

- The template intentionally retains its older two-line comment difference from the live library's pre-E8 text. This patch carries the E8 delta without overwriting unrelated factory history.

## Failed attempts

- The first authoritative verifier attempt stopped at `tsc: command not found`; the worktree lacked its lockfile-installed dependencies. `npm ci` fixed the local prerequisite, and the unchanged verifier then passed.
