# Handoff: Forge 2026-08-31T13:25:02-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-zj8e.9` remains in progress for Warden review and Mayor closure.
  The digest renderer now has action and subject views.

## Verified facts

- `scripts/digest.sh` reads the live Beads database with `bd`, not
  `.beads/issues.jsonl`. It fetches the three gate labels separately because
  the installed `bd` label-pattern query included ungated work in a live read.
- The default decision view groups only live gate-labelled work by recorded
  `act-decide`, `act-regent`, and `act-host` labels. Unlabelled action work is
  explicit, never inferred or omitted. Each row contains its title, id, status,
  and secondary gate tag.
- `scripts/digest.sh --by-subject` uses the live all-status Beads query to roll
  child work to its root, render closed/total progress, and resolve blocking
  dependency ids to Bead titles.
- A live render on 2026-08-31 showed action counts and titled blockers, with no
  decision row identified solely by an id.
- `test/shell.test.ts` exercises action ordering, cap/elision, unclassified
  work, subject progress, and blocker title rendering.

## Next actions

1. Warden review the `scripts/digest.sh` and `test/shell.test.ts` diff.
2. Mayor closes `fortkit-zj8e.9` only after review and merge.

## Open risks / questions

- The current live queue has 14 decide, 41 Regent, and 2 host actions, which
  differs from the Mayor's dispatch snapshot (14, 40, and 3). The renderer
  intentionally reports the current live labels rather than preserving a stale
  count.

## Failed attempts

- `bd list --label-pattern='gate-*'` was rejected after a live render showed
  261 ungated beads in the unclassified group. The renderer instead unions the
  three explicit gate-label queries, preserving the digest's queue boundary.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-08-31T13:25:20-08:00: exit 1

```
    354|
    355|     expect(result.stdout).toContain("BY SUBJECT");
    356|     expect(result.stdout).toContain(
       |                           ^
    357|       "Session digest [fortkit-session] — 1/10 done; blocked by: Insta…
    358|     );

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed | 17 passed (18)
      Tests  1 failed | 221 passed (222)
   Start at  13:25:04
   Duration  15.65s (transform 1.44s, setup 0ms, import 2.15s, tests 21.38s, environment 2ms)

```
