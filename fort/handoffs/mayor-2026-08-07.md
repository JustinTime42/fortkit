# Handoff: Mayor 2026-08-07T~11:45-08:00
Model: Claude Fable 5, as Emrith Cairnwright, seventh session in the seat

## State of work

The Herald arc moved from "blocked on an unmasked launch" to "proven live,
timer drafted, crash paths hardened." One genuinely load-bearing discovery
about the permission engine came out of it (see Verified facts).

- **fortkit-r6x.6 — CLOSED.** The maiden run succeeded unattended-shaped at
  00:13-00:20 (Overseer's unmasked terminal, opus, exit 0): report at vault
  reports/2026-08-07.md, three drafts filed. I verified EVERY figure in all
  three drafts against the source forts' events, commits, and handoffs — zero
  failures to trace; full trace recorded in the bead notes. Overseer sign-off
  recorded. Herald report findings beaded before H7 per acceptance:
  fortkit-1jh (digest telemetry ~zero records, P2), fortkit-1y2 (Manyhalls
  duplicate session.start pairs, P3), recurrence comments on fortkit-0n6
  (closedBeads empty for Proofdelve+Farlantern, second occurrence) and
  fortkit-wtm (Farlantern suffixed handoffs invisible to the digest reader).
- **fortkit-r6x.7 — IN PROGRESS (claimed).** Mayor drafts complete and
  committed: civ/systemd/herald.service + herald.timer + README (05:00 daily,
  Persistent=true, service owns the opus→sol ladder on exit 65 only). Both
  units pass systemd-analyze verify. Remaining: Overseer installs
  (cp to ~/.config/systemd/user, enable, loginctl enable-linger), then one
  unattended morning + one missed-morning catch-up. The Overseer's "wait for
  tomorrow's run" suggests he may have installed; UNVERIFIED from this seat.
- **fortkit-izz — decomposed (tree approved), .1/.2 CLOSED, .3 ready, .4 blocked.**
  - izz.1 (launcher hardening): proposal at docs/proposals/izz-herald-hardening/
    (launcher-patch.md + herald.sh.proposed). Pre-flight vault probe into
    reports/ AND drafts/, guarded crash-stub, exit semantics 0/65/66
    (66 = vault dark, never ladder-retried), per-run log naming, smoke
    probe 9 (fallback surface). APPLIED by the Overseer's hand; live
    civ/scripts/herald.sh matches the proposal byte-for-byte.
  - izz.2 (profile fix): took THREE rounds — see Failed attempts. Final fix
    engine-confirmed and applied: fallback allow moved to Edit-family,
    broad Edit(//home/justin/dev/**) deny replaced by enumerated protected
    surfaces mirrored in Edit+Write families. Full three-round record with
    corrections appended in docs/proposals/izz-herald-hardening/profile-fix.md.
  - izz.3 (apply + prove, now READY): proofs 1-3 not yet run (pre-flight
    refusal, stub guard, log naming); proof 4 is probe 9 PASS on the next
    smoke. Proof 1 is runnable from a masked Mayor session (its point is that
    nothing launches); proofs 2-3 want the Overseer's terminal.
  - izz.4 (drop herald.service log-mv workaround): blocked by .3.
- **fortkit-a16 — FILED (P2).** Civilization-wide audit of inert
  Write-family permission rules in every seat profile + fort-init templates.
  Backport candidate.

## Verified facts

- **The Claude Code permission engine does not match Write(...) rules in
  file permission checks — only Edit(path) rules, which govern ALL
  file-editing tools.** Not inference: the CLI printed this advisory itself
  during a controlled reproduction (scratch settings, herald.sh's exact
  launch shape), and the Edit-family spelling granted end to end (WROTE-OK,
  file on disk, through the tempfile rename). Consequence: any Write(...)
  deny in any profile is an inert protection its author believes exists —
  that's fortkit-a16.
- The Write tool creates <target>.tmp.<pid>.<hex> then renames onto the
  target (inotify-measured). Real behavior, but NOT the cause of the
  fallback denial (see Failed attempts).
- Smoke probes 1-8 PASS in both of today's Overseer-run smokes; probe 5
  blocked by the mask under an allowing profile — layering as designed.
- Halric's conduct: three-for-three sessions with exemplary boundary
  behavior (did not touch the profile, diagnosed the deny shadow from
  inside the mask, filed findings in-transcript).
- civ/events/events-2026-08-07.jsonl carries the maiden run's paired
  session.start/session.end (exit 0, report_filed:true).

## Next actions

1. **Overseer:** confirm timer installed (systemctl --user list-timers
   herald.timer); tomorrow's 05:00 run closes r6x.7's "one unattended
   morning" leg. The fallback fix is live, so a dark-vault morning now
   surfaces properly.
2. **Mayor next session:** verify tomorrow's report exists and spot-check it;
   record the unattended morning on r6x.7. Then run izz.3 proof 1 (masked-OK)
   and coordinate proofs 2-4 with the Overseer.
3. **Mayor:** fortkit-a16 audit can start any time (read-only against all
   three forts' profiles); its fixes are per-fort edicts + a backport cycle.
4. Standing decisions still with the Overseer (carried since 08-04-d):
   viewer beads data source (0n6/oc8 — now more urgent, the Herald is
   scoring on a narrowed record), verifier npm-ci coherence, viewer product
   name (zgp).

## Open risks / questions

- r6x.7 timer installation is unverified from this seat; if tomorrow 05:00
  passes with no report and no incident, check systemctl --user --failed
  first (that's the izz secondary-signal path).
- The izz.3 stub-guard proof (vault goes dark mid-run) is timing-dependent
  as scripted; if it proves flaky, consider a HERALD_STUB_TEST hook rather
  than chmod-in-background — decide at izz.3.
- fortkit-a16: warden-settings.json's read-only-by-construction claim rests
  partly on profile denies that may be inert; the mask is the real boundary
  (as designed), but the audit should say so explicitly per profile.

## Failed attempts

The izz.2 fallback denial took three rounds, each with a plausible wrong or
incomplete diagnosis; the full corrected record is appended in
profile-fix.md (corrections appended, never edited in):

1. Round 1 (tempfile theory): measured real Write-tool behavior, attributed
   the denial to the .md-anchored glob missing the tempfile. Wrong as a
   cause. Lesson: measuring a real mechanism is not the same as proving it
   is the operative one.
2. Round 2 (deny shadow): Halric's diagnosis, correct as far as it went —
   the broad Edit(dev/**) deny WAS shadowing — but removing it still left
   the allow inert, because the allow was in a rule family the engine never
   consults.
3. Round 3: stopped theorizing, built a minimal reproduction, and the
   engine stated its own semantics. One controlled experiment settled what
   two review-and-reason rounds could not. Lesson for the seat: when a
   permission rule misbehaves twice, reproduce with a scratch profile
   before proposing a third fix.
