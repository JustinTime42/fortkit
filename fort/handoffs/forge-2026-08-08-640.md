# Handoff: Forge 2026-08-08T22:41:01-08:00

Model: GPT-5 (Codex), as Kethra Anvilmark

## State of work

- `fortkit-640` remains IN_PROGRESS as instructed. Character asset generation
  now has explicit isolation prompts, a pre-write/reuse alpha-transparency
  gate, and a walk-only reroll seed offset.

## Verified facts

- `scripts/run-pixellab-trial.mjs` applies the scenery-forbidding prompt and
  negative constraints only to `citizen-master` and `walk-frame` assets; it
  leaves building and glyph constraints unchanged.
- Character assets are decoded before writing or reuse. Their outer 2px band
  must be alpha-zero and nontransparent coverage must be at most 60%; a named
  refusal identifies the asset and both contamination percentages.
- Passing character manifest entries record `transparencyCheck: "passed"`.
  Contaminated previous stills are not reused.
- `PIXELLAB_ANIMATION_SEED_OFFSET` changes only the animation-card seeds. With
  `PIXELLAB_REUSE_STILLS=1`, focused coverage proves it retains the still and
  makes exactly the one animation call.
- `fort/scripts/verify.sh --no-emit` passed: Node and browser typechecks,
  Biome, 119 Vitest tests across 10 files, and the shipped shell surface.

## Next actions

1. Warden: review the path-scoped `fortkit-640` commit.
2. Mayor: merge after Warden approval; retain the bead's IN_PROGRESS status
   until review and merge are recorded.
3. Overseer: for a walk-only reroll, set both `PIXELLAB_REUSE_STILLS=1` and
   `PIXELLAB_ANIMATION_SEED_OFFSET=<integer>`.

## Open risks / questions

- The gate detects border contamination and excessive occupied area; it cannot
  prove that every internal opaque pixel belongs to the actor. The explicitly
  hardened generation prompts remain the complementary prevention control.

## Failed attempts

- The first authoritative verifier attempt found no installed dependencies
  (`tsc: command not found`). `npm ci` restored the lockfile dependencies and
  the following full verifier run passed.
