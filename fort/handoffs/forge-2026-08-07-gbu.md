# Handoff: Forge 2026-08-07T17:33:39-08:00

Model: GPT-5 Codex, as Kethra Anvilmark (she/her)

## State of work

- `fortkit-gbu` remains IN_PROGRESS as instructed. This research-only proposal update is committed for Warden review; Forge did not close or otherwise change the bead.

## Verified facts

- `docs/proposals/art-pipeline/README.md` names and links the public license records for `stabilityai/stable-diffusion-xl-base-1.0`, `nerijs/pixel-art-xl`, and `black-forest-labs/FLUX.1-dev`, with retrieval date, short license quotes, distribution consequences, and the gated/unreachable-record finding.
- The asset contract now fixes the native cell at 32×32 pixels, treats a master as the zoom-1 native grid, forbids resampling, and makes sheets exactly 512×512 (16×16 cells). It corrects the 1100×620 canvas attribution to `src/colony-page.html`.
- `CI=1 fort/scripts/verify.sh --no-emit` passed Node typecheck, browser typecheck, Biome (46 files), Vitest (74 tests across 9 files), and ShellCheck.

## Next actions

1. Warden reviews the path-scoped commit for research accuracy, license characterization, and contract coherence.
2. Mayor/Overseer decides whether to authorize a pinned SDXL base revision, a provenance-complete pixel adapter, or a separate FLUX commercial license; do not download or accept terms before that authorization.
3. Mayor merges and closes `fortkit-gbu` only after an approving recorded verdict.

## Open risks / questions

- The public Pixel Art XL page exposes a CreativeML OpenRAIL-M label but no full license text or training-data/provenance statement. It is intentionally not approved for use or redistribution.
- FLUX.1-dev's public model files remain gated behind contact-information sharing and acceptance; no account, sign-up, download, or click-through occurred. Its dev-weight non-commercial restriction makes it unsuitable for the production path absent separate commercial authorization.

## Failed attempts

- The first authoritative verifier invocation stopped at `tsc: command not found` because this worktree had no installed lockfile dependencies. `npm ci --no-audit --no-fund` restored them; it changed no tracked manifest or lockfile, and the rerun passed.
