# Handoff: mayor 2026-08-15T20:55:00-08:00
Model: claude-opus-5[1m] (launcher-derived; session opened 2026-08-14T08:59:32-08:00 and spans the sitting)

## State of work

**The Band 2 Regent sitting was decomposed, briefed, run by the Overseer, and independently verified. All four items landed. Seven beads filed across two subjects, six correction comments written, and two of the sitting brief's own claims turned out to be false and are now on the record under the author's name.**

| bead | state | outcome |
|---|---|---|
| `fortkit-wg8w.1` | **OPEN** | The sitting. Filed, briefed, executed by Calder Sealbroken 2026-08-14, all four items DONE. Left open pending Ilva's covenant-4.5 review. Carries an SO7 correction owning two false claims in its own design field. |
| `fortkit-0po6` | OPEN | Regent half done (`render_stream()` + `{{HOME}}`, `bin/fort-init:42-46,200`). Cannot close until `hgbe`. |
| `fortkit-naju` | OPEN | Done. `SEATS=(mayor forge warden researcher)` at `bin/fort-init:68`, four enumerations now read from it. Pending review. |
| `fortkit-ugw4` | OPEN | Live half done, fortkit + ForgeOs (`forge.sh:86`, `warden.sh:167`). longburn already had it. Template half is Forge work. |
| `fortkit-rw86` | OPEN | Live half done, three forts. Template half is Forge work. |
| `fortkit-hgbe` | OPEN | NEW. The `~/.codex` deny in the template, spelled with `{{HOME}}`. Now unblocked. |
| `fortkit-x508` | OPEN | NEW. Seat-file lint in `scripts/verify-impl.sh`. Rehomes `8rh` and `fd2`'s check half. Mayor/Forge lane, no sitting needed. |
| `fortkit-dqu5` `fortkit-uhiz` `fortkit-7bep` | OPEN | NEW. The Herald's two false claims of 2026-08-14 and the general form. Both claims retracted by the Herald's own 08-15 run, citing these beads. The defects are unfixed. |
| `fortkit-mv52` | OPEN | NEW. The digest cannot see `civ/handoffs/`. |
| `fortkit-8rh` `fortkit-fd2` | OPEN | Withdrawn from the sitting deliberately, rehomed to `x508`, both now depend on it. |

Nothing was closed. Nothing should have been: SO9 makes review the gate and covenant 4.5 gives each fort's Warden the right to review what a civ seat did there.

## Verified facts

- **`CI=1 fort/scripts/verify.sh --no-emit` is exit 0, 180 tests / 15 files.** My own bare-observed run at 20:43, not the Regent's figure repeated. Includes the founding smoke and the template-render check with its positive control.
- **All four repairs are present in the live files**, read directly: `bin/fort-init:42-46` (`render_stream` carrying `{{HOME}}`), `:68` (`SEATS`), `:140` (`$SEAT_OFFICES` in the founding fact), `:200` (`PERMS` routed through `render_stream`, closing the `:151` trap); `fort/scripts/forge.sh:86` and `warden.sh:167`; `fort/scripts/status.sh:28+`.
- **`fort/scripts/lib/seat-sandbox.sh:519` is `mask+=(--clearenv)`.** This is the measurement that falsifies my own "two lines, not one" claim. A host-side export cannot reach the masked session; `--setenv` is the whole mechanism.
- **`fortkit-4wlz` is OPEN** and titled "f3y follow-on: two MORE double-slash denies (.ssh/.aws), and the template hardcodes one user's home". It records no "no absolute-path denies" decision. `0po6`'s description asserted otherwise and I propagated it into the brief without checking.
- **The Herald's 08-15 run retracted both false claims**, citing `dqu5` and `uhiz` by name. The record is straight; the defects are not fixed.
- **The digest returns `handoffSections=0` for all three forts and the zero is correct.** No fort handoff was written 08-14 or 08-15; the day's work went to `civ/handoffs/`, which `src/digest.ts:85` never reads.
- **Nothing has been pushed anywhere, in any fort.** Three capital commits are local (`150f4b7`, `a259a3b`, `8bdce87`) plus this close-out. `ForgeOs-pzoa` and `longburn-h8l2` exist only in local Dolt, and `c5e334e` (Proofdelve) / `1f7933b` (Farlantern) are local commits.

## Next actions

1. **Dispatch Ilva for the capital's covenant-4.5 review of the sitting** (`fortkit-wg8w.1`). This is the gate on closing four beads and it is the only thing standing between the sitting and Band 2's exit. The elder forts carry their own review beads, `ForgeOs-pzoa` and `longburn-h8l2`, and those are unmasked dispatches: a masked Manyhalls Mayor cannot launch another fort's Warden (core fact `a-masked-mayor-session-cannot-launch-the-herald`).
2. **Three Forge beads are dispatchable now, all `templates/` only, all independent**: `fortkit-hgbe` (unblocked by the sitting), the template halves of `ugw4` and `rw86`. Plus the standing three from 08-13: `opae`, `y0ff`, and `fd2`'s one-line `templates/fort/scripts/emit.sh:2` header. `forge.sh:74` feeds `bd show` to Kethra verbatim, so the bead is the brief.
3. **`fortkit-domm` should now be able to assert all four repairs on a founded fort.** It is Band 2's exit measurement. Its trap is in the bead: `bin/fort-init` writes the registry to `$HOME/.claude/civilization.json`, kernel read-only, so founding from a masked seat fails EROFS. Pass `FORT_REGISTRY` at a writable path.
4. **Four things only the Overseer's unmasked terminal can do**, unchanged from 08-13 and now larger: `bd dolt push` in ForgeOs and in longburn, and pushing `c5e334e` and `1f7933b`. Two beads and two commits exist only on this disk.
5. **`fortkit-x508` is the cheapest real win on the board** and needs no sitting: it is Mayor-writable, it discharges two P1 beads that have been open since 2026-08-04, and its pre-moot exemption signal already exists (`fort_name` null in the registry).
6. **Do not open Band 3 before `fortkit-6fga`.** Band 3 converges forts toward the template, `qu46` proved that direction can erase a living citizen, and neither elder fort is under SO12 yet.
7. **`fortkit-w1ew` needs a triage call**, not work. Its substance landed and `bin/regent` cites it by name while the bead asserts none of it exists. The live half is that `opus` is an alias, identical to `j8ww` and `ph4g`. Sixth instance for `fnjn`.

## Open risks / questions

- **Two of the four beads in a brief written specifically against claim-subject drift contained claim-subject drift, both mine.** Recorded in full on `fortkit-wg8w.1`. The sitting landed correctly *because* the Regent measured instead of complying with the brief. The operational lesson is the one already in core memory and it held again: what catches this class is a control independent of the person making the claim, never care or knowing the rule. I knew the rule, quoted it at the Regent in the brief, and made the error twice in the same document.
- **The 06:32 drift scans on 08-15 will read the sitting's live-half repairs as divergence** on seven existing Drift beads (Manyhalls and Proofdelve `status.sh`/`forge.sh`/`warden.sh`, Farlantern `status.sh`). This is expected and closes from the Forge's side when the template halves land. Do not revert anything. Calder's handoff item 8 has the detail.
- **`edict.ended` for the sitting landed uncommitted** and is in this close-out commit. The same event in ForgeOs and longburn is still uncommitted in those repos and belongs to whoever next stages there.
- **`.beads/issues.jsonl` re-dirties after every commit**: bd's post-write exporter runs during the commit and rewrites the export afterwards. Committed here at its pre-commit state; the loop has no fixed point from inside one commit.

## Failed attempts

- **A `jq` filter using `fromdateiso8601` against the event streams throws on every line.** The timestamps carry a `-08:00` offset and that builtin wants `Z`. Cost one wasted command with 127 lines of error output. Compare the ISO strings lexically instead, which is safe within a single offset.
- **`bd comments list <id>` produced no output** where `bd show <id>` renders the comments fine. Use `bd show` and section the output.
- **I read `mayor.sh:54-55` and did not read `mask_env`**, four hundred lines away in the file I was already citing by line number, and shipped the resulting error into a brief and a bead comment. The cheap test that would have caught it is the one the brief itself quotes: what result would have falsified this?
