# Handoff: Forge 2026-08-31T14:08:03-08:00
Model: gpt-5.6-terra

## Scope

Corrected the standalone-subject assertion in `test/shell.test.ts`.

## Change

- Split the digest output on the actual `BY SUBJECT` newline, rather than the
  two-character `\\n` literal.
- Fail immediately with `BY SUBJECT section was not rendered` if the section is
  absent, rather than converting that state into an empty string.

## Verification

`npm test -- --run test/shell.test.ts` is red locally: 26 passed, 3 failed.
The three failures are the digest action/subject fixtures, which render no gate
groups after `DECISIONS WAITING`. Instrumentation established that the temporary
fake `bd` receives the expected `--status=open,in_progress --label=gate-N`
arguments, and executes successfully, but its redirected output is empty during
the digest run. Direct execution of the same generated fake returns the expected
gate JSON. This is distinct from the repaired subject-section assertion.

No full verifier was run after this focused red test run.

## Next action

Resolve why the fake-bd output is empty only when called from the digest test,
then run `CI=1 fort/scripts/verify.sh --no-emit` and quote its launcher result.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-08-31T14:08:05-08:00: exit 1

```
     365  365 │       const subjectSection = result.stdout.split("BY SUBJECT\n")[1];
     366      │ - ····if·(subjectSection·===·undefined)·throw·new·Error("BY·SUBJECT·section·was·not·rendered");
          366 │ + ····if·(subjectSection·===·undefined)
          367 │ + ······throw·new·Error("BY·SUBJECT·section·was·not·rendered");
     367  368 │       expect(subjectSection.match(/Install host hook/g)).toHaveLength(1);
     368  369 │       expect(result.stdout).not.toContain("blocked by: fortkit-hook");
  

Checked 72 files in 69ms. No fixes applied.
Found 1 error.
check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Some errors were emitted while running checks.
  

```
