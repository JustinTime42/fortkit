# Handoff: Forge 2026-09-01T22:27:38-08:00
Model: gpt-5.6-terra
## State of work

- `fortkit-3lme` remains in progress for Warden review and Mayor closure. The factory charter now states that civilization seats are not ruled by "this settlement" rather than Manyhalls.
- `fortkit-3lme.1` remains untouched. It owns the identity-leak scan and its provenance allowlist.

## Verified facts

- `templates/fort/charter.md:80-81` now uses the generic wording required for a fort whose name is not available until its Founding Moot.
- The two legitimate provenance citations at `templates/fort/charter.md:29` and `:31` still name Manyhalls as their origin.
- `CI=1 fort/scripts/verify.sh --no-emit` passed: TypeScript and browser type checks, Biome, 19 test files (199 passed, 40 skipped), rendered-template linting, and ShellCheck all completed successfully.

## Next actions

1. Warden review the path-scoped commit.
2. Mayor merges and closes `fortkit-3lme` after review. Do not close `fortkit-3lme.1`; the identity-leak scan remains separate work.

## Open risks / questions

- The charter repair is intentionally limited to one sentence. There is still no mechanical template identity-leak scanner until `fortkit-3lme.1` lands.

## Failed attempts

- The initial verifier run stopped at `tsc: command not found` because this worktree lacked installed Node dependencies. `npm ci` restored the lockfile-defined dependencies; the subsequent full verifier was green.
