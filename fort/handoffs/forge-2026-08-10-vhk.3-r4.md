# Handoff: Forge 2026-08-10T20:09:23-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-vhk.3` remains IN_PROGRESS as directed. The Researcher launcher
  template is ready for its final Warden pass; the Mayor retains closure and
  the Overseer retains the gate-1 installation decision.

## Delivered

- `build_mask` now receives `"$root-worktrees"` as an extra read-only path,
  giving the entire worktrees tree the same kernel-level read-only treatment as
  the Warden candidate tree.
- The launcher header now documents exit 65 (a clean session without the
  `RESEARCH-COMPLETE` sentinel, recorded nowhere and eligible for failover) and
  exit 78 (missing `bwrap`), including the rule that a nonzero exit never
  represents recorded research.

## Verification

- `git diff --check` — exit 0.
- `bash -n templates/fort/scripts/researcher.sh` — exit 0.
- `shellcheck -x templates/fort/scripts/researcher.sh` — exit 0.
- `npx vitest run test/researcher-launcher.test.ts` — exit 0; 2 tests passed.
- `CI=1 fort/scripts/verify.sh --no-emit` — exit 0: both TypeScript checks,
  Biome (59 files), Vitest (14 files / 145 tests), and the shipped ShellCheck
  surface passed.

## Next actions

1. Request the final Warden review of the path-scoped commit.
2. Submit the gate-1 template changes to the Overseer for live-fort
   installation.
3. Keep same-profile fan-out for `fortkit-vhk.6`; it must re-enter this
   launcher and retain the depth-one cap.

## Surprises

- None. The two requested round-four changes fit the existing `warden.sh` and
  `seat-sandbox.sh` conventions without test or profile changes.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-08-10T20:09:28-08:00: exit 0

```
> vitest run


 RUN  v4.1.10 /home/justin/dev/fortkit-worktrees/vhk.3

(node:551859) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)
(node:551842) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)

 Test Files  14 passed (14)
      Tests  145 passed (145)
   Start at  20:09:24
   Duration  2.70s (transform 1.36s, setup 0ms, import 1.98s, tests 5.65s, environment 1ms)

```
