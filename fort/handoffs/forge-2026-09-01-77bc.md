# Handoff: Forge 2026-09-01T20:19:42-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-77bc` remains in progress for Mayor review and closure. The digest now
  defaults to a 1,000-event per-fort cap, exposes an exhaustive body-free
  handoff-section index, and fetches selected full section bodies by index ID.

## Verified facts

- `src/digest.ts`: events use the new 1,000 default and accept a positive
  `maxEventsPerFort` option. Any remaining event truncation discloses counts by
  category and UTC day. Constitution-diff correlation still receives the full
  event window before any display cap.
- `src/readers/handoffs.ts`: every windowed section produces a stable index
  record with fort, date, seat, inferred bead, section name, file, and ID;
  section bodies remain on the existing 50-item display cap.
- `src/cli.ts`: `digest --max-events-per-fort <positive integer>` overrides the
  event cap and `digest --fetch <id>[,<id>...]` returns selected full bodies.
  Unknown IDs fail with `Handoff section id not in digest window: <id>`.
- `test/digest.test.ts`: proves a 751-event three-fort day is whole by default,
  the 51-section index survives the 50-body cap, successful and missing-ID fetch
  paths, truncation composition, constitution verdict independence from the
  display cap, and byte-identical legacy fields when new fields and raised-event
  fields are excluded.
- `CI=1 fort/scripts/verify.sh --no-emit` passed: 19 files, 198 tests passed,
  40 skipped. The shell-spawn skips are explicitly classified by the t8a4
  harness behavior; no bare spawn-permission failures occurred.

## Next actions

1. Warden review the scoped digest, CLI, handoff-reader, and regression-test
   changes.
2. Mayor merges and closes `fortkit-77bc` if review accepts the additive output
   shape.
3. The Regent can teach the Herald to use the index and fetch verb under
   `fortkit-77bc.1`; that work remains deliberately separate.

## Open risks / questions

- The bead reference is inferred from conventional handoff filename suffixes.
  A handoff with no bead-shaped suffix remains indexed with `bead: null`, rather
  than inventing an association.

## Failed attempts

- The worktree initially had no installed dependencies, so the first targeted
  test invocation stopped at `vitest: command not found`. `npm ci` restored the
  locked dependency set; all subsequent targeted and authoritative checks passed.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-09-01T20:20:12-08:00: exit 0

```

 RUN  v4.1.10 /home/justin/dev/fortkit-worktrees/77bc

(node:792139) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)
(node:792145) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)

 Test Files  19 passed (19)
      Tests  238 passed (238)
   Start at  20:19:44
   Duration  21.65s (transform 1.59s, setup 0ms, import 2.32s, tests 34.84s, environment 2ms)

template-render: 14 template scripts rendered in 3 passes and linted clean; positive control went red as required.
beads-export: SKIPPED — /home/justin/dev/fortkit-worktrees/77bc is a worktree of /home/justin/dev/fortkit; its tracked export is a branch snapshot, not a claim about database freshness.
```
