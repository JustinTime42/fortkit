# Handoff: mayor 2026-08-31T22:20-08:00 (evening session)

Model: claude-opus-5[1m] — **SELF-REPORTED, not launcher-derived.** `mayor.sh`
pins no model (`fortkit-ph4g`, open), so neither the commit nor the
`session.start` payload records one. Second Mayor session of 2026-08-31; the
first is `mayor-2026-08-31.md`.

## State of work

Justin arrived with "good afternoon" and no agenda, approved the two P1s I
proposed from the morning handoff, and the evening became four programmes: the
digest's day-zero defect, the export-freshness bead, a Regent sitting he ran
himself, and a stale-bead sweep I did not plan and should have.

**Measured, not recalled:** 25 commits, 3 merges, 4 Forge sessions, 5 Warden
reviews, 8 beads closed. `HEAD` and `origin/main` both `2265ddb`, **0 unpushed**,
verified by `git fetch` rather than by `@{u}` alone. 315 open beads.

| bead | state | outcome |
|---|---|---|
| `fortkit-zj8e.11` | **CLOSED** | five stream states the digest could not tell apart |
| `fortkit-zj8e.12` | **CLOSED** | unreadable shards classified; dead branch removed |
| `fortkit-4ah3.12` | **CLOSED** | the register's census asserted; **caught instance 8 on its first run** |
| `fortkit-v7us` + `.1 .2 .3` | **CLOSED** | export rule measured, moved into the read path, made mechanical |
| `fortkit-bucl` | **CLOSED** | stale — every condition it named as absent was present |
| `fortkit-2twy` group | **CLOSED** | the Overseer's Regent sitting; Warden-reviewed, gate-1 signed off |
| `fortkit-mc0m` | **FILED, P1** | the founding-as-test-case epic — tomorrow's priority |

Main green at close: **19 files, 235 tests, 48 controls, 12 verifier stages.**

## Verified facts

- **The export throttle DEFERS TO THE NEXT WRITE, NOT TO A TIMER.** 51 samples
  over 244s with zero catch-up; one further write brought the file fully current.
  A burst of `bd` writes leaves the export at the state of the burst's FIRST
  write, indefinitely. `bd export` is byte-stable, which is what makes a
  diff-based verifier stage buildable.
- **The rule was already written down, correctly, on 2026-08-07**, in
  `docs/proposals/` — referenced from one handoff, describing a mechanism live in
  every fort. Three probes re-derived it. **The defect was placement, not
  content.**
- **The Warden CANNOT read `bd`, and the reason is the MASK, not her profile.**
  Measured verbatim: `openat LOCK: read-only file system`. It ERRORS; it is not
  denied. Four earlier reviews were right about the outcome and wrong about why.
  So the export is load-bearing for this fort's only review seat.
- **A day-zero fort is NOT eventless** — `bin/fort-init` emits five founding
  events. The `zj8e.11` bead said otherwise and a fix built to its text would
  have shipped a false message.
- **`bin/fort-init` needs `FORT_REGISTRY` to run from a masked seat** — until the
  Regent's `2twy` repair, it aborted between the registry write and the emissions,
  leaving a fort that looked founded with an empty `fort/events/`.

## Next actions

1. **`fortkit-mc0m` (P1)** — found ONE fort as the test case. Blockers measured
   against the tree: `6xjy` (a security claim that ships and contradicts
   Proofdelve in writing), `3lme` (one sentence at `charter.md:81`), `520l` (only
   if the first fort is non-node — **ask which goes first**).
2. **`fortkit-v7us.5` (P2)** — my defect. Fix the root: id-keyed verdict, and
   capture the reporter's exit status. Dispatch to the **Forge**, which is the
   only seat positioned to reproduce it.
3. **`fortkit-fnjn`** — seven beads have outlived their fixes. Do not answer a
   readiness question from `bd` alone until this is addressed.
4. **`fortkit-b4lg` (P2)** — the launcher attestation heading, plus the posture
   vocabulary gap. Detector half can be built here; launcher half is Regent lane.
5. **`fortkit-iyj6.2`** — deferred to 2026-09-01; needs a night with the machine off.

## Open risks / questions

- **`fortkit-v7us.5` is a live defect in a control I shipped today.** Seven runs:
  every red from inside a Forge mask, every green from outside one. Leading
  mechanism is `beads-export-check.sh:95`, where the `node -` reporter has no
  failure handling. **Recorded as an inference from a perfect correlation, not a
  measurement** — I have not watched node fail inside a Forge namespace.
- **The board is not trustworthy for planning.** Four of six blockers I gave the
  Overseer did not survive contact with the tree.
- **`civ/handoffs/.edict-2026-08-31T212231` is untracked, deliberately.** It is
  the Regent launcher's marker and belongs to the next sitting's staging. Do not
  add it.
- **The second new fort's language is unknown.** Justin said "at least one" is a
  node project. `520l` is live for the other.

## Failed attempts

- **I gave the Overseer a six-item founding-blocker list built from bead TITLES.**
  On reading the artifacts, `fd2` and `bucl` were already fixed, `3lme` was one
  sentence rather than a whole constitution, and `0po6` was narrower than its
  title. Two of six survived. The cost was not a wasted fix — it was a wrong
  planning answer, and planning has no verifier.
- **I ranked the correct hypothesis last on a bad argument.** Ilva proposed the
  `node -` heredoc; I weighed it below the reorder theory by testing "node is
  broken globally" when her claim was "this invocation fails". A proxy measured
  and reported under the target's label, in the session where I had already
  written that rule into the fact ledger.
- **I wrote "not reproducible in four attempts" while a second Forge session was
  still running** and about to report the same symptom independently.
- **I ran three probes before opening the file that had the answer.**
  `docs/proposals/issues-jsonl-export-rollout.md` had stated the export rule
  correctly for three weeks.
- **`git add` aborts the whole staging on one bad pathspec.** Twice: once on a
  `git mv`-ed path, once on an event shard for a date that had not arrived.
- **`git merge -F -` does not read stdin.** Commit and merge messages go through
  a file, never `-m` and never a pipe.

## The thing worth carrying forward

**Every defect that mattered today was caught by something other than care, and
three of them were mine.**

The control register went red on my line-number shifts and refused my
`falsified-by` when I gave it a file path where it wanted a control key. Two
Forge sessions independently reported a red I could not reproduce and initially
explained away — and it was real. The Warden derived its mechanism from source
without seeing either handoff. The census assertion caught a stale figure on its
first run, in a file I had predicted was stale and then did not check.

Yesterday's handoff closed on "eleven blocking defects, not one caught by care."
Today the fort had more instruments than it had yesterday, and every one of them
earned its place by going red at its author. **That is the return on the
enforcement spine: not that the fort makes fewer mistakes, but that it now finds
them without needing someone to be careful.**
