# Handoff: mayor 2026-08-13T10:10:55-08:00
Model: claude-opus-5[1m] — SELF-REPORTED (`fortkit-ph4g`: `mayor.sh` pins no model). This session
has a `session.start` and its `session.end` is hand-emitted, because `mayor.sh` execs and its EXIT
trap has never fired in any fort (`fortkit-t9iw`). Both are still open.

Predecessor: `mayor-2026-08-12-c.md` (the verdict-reap addendum) and `-b` (the close-out) before it.

---

## State of work

**19 beads closed, 16 filed across all seats, main pushed and verified.** A reaping session: it
finished work other sessions had left in flight rather than opening new ground.

| bead | outcome |
|---|---|
| `hwol` | **closed** — `fort/seats/mayor.md:17` corrected under the prose gate; Overseer approval verbatim, `charter.amended` 07:40:33, commit `98cf750` |
| E2b cluster ×12 | **closed** — `52vf.9`, `6ovg`, `x9ou`, `vkq8`, `faka`, `w6mb`, `yowr`, `4n8c`, `ddvo`, `52vf.3`, `2kub`, `5sk` |
| `qbq2` | **closed** — the 44/17 positive control landed, P0 cleared |
| `n3bk.1` | **filed, built, reviewed, merged, closed** — 13 inert template `Write()` rules converted to `Edit()` |
| `1gz3` | **filed, built, reviewed, merged, closed** — the distilled view off the superseded cycle-7 fact, and the test that pinned it there |
| `52vf.10` (E8) | **closed** on Overseer gate-1 sign-off after Warden ESCALATE, no blocking findings |
| `6jf` | **closed** — ForgeOs's `forge.sh` was the last inline mask in the civilization; it is gone |
| `u200` first half | **executed** — 74 worktree/branch pairs pruned, 16.4GB reclaimed |

**Filed forward:** `u1s3`, `elh9`, `1gz3`, `c1kj`, `n3bk.2`, `1gz3.1`, `3jv7`, `52vf.10.1`.

**The board did NOT shrink, and an earlier claim of mine in-session that it fell to 218 was
wrong.** Accurate: open 228 → **227**, closed 168 → **187**, total 400 → **416**, blocked 23 → 12.
Nineteen closed against sixteen filed (eight mine, the rest the Regent's) is a file-to-close ratio
of roughly 1.2:1, against the 5:1 the last session flagged as unsustainable. That is a real
improvement in rate and a flat board in absolute terms. Both statements are true and the second
is the one a successor should plan against.

## Verified facts

- **`origin/main` == `HEAD` == `e620ae1`**, confirmed by `rev-parse` on both after a `fetch`
  (SO6). Thirteen commits, `5c5e676..e620ae1`. Verifier green at exit 0 immediately before the
  push and after every merge.
- **Three Warden reviews, three verdicts, zero blocking findings.** `n3bk.1`
  APPROVE-WITH-FINDINGS (6), `1gz3` APPROVE-WITH-FINDINGS (6), `52vf.10` **ESCALATE** — a
  mandatory gate-1 + auth-surface referral with no blockers, routed and signed off.
- **Ilva proved `1gz3`'s central criterion herself** rather than accepting the handoff, the
  reward-hacking flag being live for the gpt-5.6-terra rung: run against main's genuinely stale
  view, the positive assertion scores 0 matches stale / 1 candidate, the negative 1 / 0.
- **The worktree prune was gated four ways per tree** — bead `closed`, branch merged, `git status
  --porcelain` empty, no live `.forge.lock.info` — and executed with `git worktree remove` and
  `git branch -d` rather than force, so git independently re-checked every removal. 74 removed,
  **zero refusals**. 17048MB → 673MB. Post-state: 4 worktrees, `worktree prune --dry-run` clean.
- **`edict.ended` for E8 landed one per fort at 09:13:06**, matching `edict.begun` at 08:15:35.
  The Regent's only safety property holds end to end.
- **`fort/scripts/forge.sh:15-17` couples worktree and branch**, PROVEN not asserted: `git
  worktree add <path> -b bead/ybg7` → `fatal: a branch named 'bead/ybg7' already exists`, clean
  failure, nothing created. Round-2 dispatch works today only because the worktree persists and
  the `[ ! -d ]` short-circuits.
- **`templates/scripts/*.sh` is on the ShellCheck surface** (`4de5845`), proven non-vacuous by a
  planted syntax error the new glob names and the old glob silently exits 0 against.

## Next actions

1. **`fortkit-3jv7` is the one thing waiting on the Overseer.** P1, gate-1. The Warden is no
   longer read-only *by construction* at `~/.codex` — E8 unified on `CODEX_DIR_RW` for both seat
   types, so at the kernel she can write `~/.codex/skills` and `plugins`, instruction executed by
   the next Forge launch outside her mask. Her profile holds the line at the tool layer, so the
   property is now **profile-enforced** while the charter and `fort/seats/warden.md` both still
   say "by construction". Two shapes offered; the seat recommends narrowing the wording plus a
   one-line residual. Needs prior approval on the bead under the prose gate.
2. **`fortkit-c1kj`** — the memory staleness gate. **Mayor lane, cannot be dispatched**, since it
   edits `scripts/verify-impl.sh`. Design is settled from two independent directions and recorded
   on the bead: key on ledger identity (*no fact rendered in the view is `status: superseded`*),
   never on prose or a whole-file diff.
3. **`fortkit-f3y` raised to P1.** Until E8 the double-slash spelling was moot for claude seats
   because `~/.codex` was a tmpfs; E8 deliberately opened it, so that one rule is now the only
   tool-layer control over the newly-opened surface, in the spelling this fort doubts.
4. **`u200`'s second half** — make `forge.sh` reuse an existing branch rather than fail on `-b`.
   Regent lane. Until it lands the worktree/branch pairing is hand-maintained.
5. `52vf.10.1` (Warden findings 1, 5, 6, 7, 8) and `n3bk.2` (the factory's permission spelling).
6. **The elder forts' half of E8 is unreviewed by this fort** and stays open to their own Wardens
   under covenant 4.5. The gate-1 sign-off covers the capital only; the bead says so explicitly.
7. `or2.2` (backport rubric + drift watcher spec) — still the highest-leverage untouched Mayor
   work, now six sessions running.

## Open risks / questions

- **`src/cli.ts` uncommitted, mode bit only** (`zya9`), and **`assets/trial/*` untracked and
  unclaimed** — seventh session for both.
- **`bead/m0wm` has a branch and no worktree.** Deliberate on the Regent's part (the branch
  carries commits and records are append-only), and it is also the pairing trap in the wild: that
  bead can no longer be dispatched. Both facts are on `u200`.
- **Every mask-behaviour number in the E8 record is taken on faith by the Warden**, enumerated by
  her: she could not run `build_mask` (`bash -c` not in her allow list, the `hn2` class) and
  `/proc/self/mountinfo`, `~/.codex` and both elder repos are outside her readable roots. The
  numbers come from the Regent's unmasked probes. A successor should not read her APPROVE as
  independent confirmation of them.
- **Two seats now report the same `packed-refs.lock` read-only warning on commit** (`n3bk.1` and
  `1gz3`). That is `fortkit-8cq`'s signature, now reproducible rather than a one-off.
- **227 open beads.** The rate improved; the absolute number did not.

## Failed attempts

**One error, and it is the same class the predecessor handoff named five instances of.**

**I measured my own sandbox and called it the host.** I cited `~/.codex/skills` mtime
`08:12:11.888` against a Forge `session.start` at `08:12:11` and concluded the codex runtime
mutates that directory at startup — then built a rider on a live edict from it, and wrote the
figure into a bead's description as decisive evidence. `/proc/self/mountinfo`:
`/home/justin/.codex` is a **tmpfs** to a masked claude seat, with `auth.json` the only path
re-bound from real disk. Everything I read off that directory was scratch created by my own
session. The Regent, unmasked, measured host mtimes three days and a month older, and — correctly
— declined to call my premise wrong without establishing what it counted.

**My own output contradicted me in the same command block.** `find ~/.codex/skills
~/.codex/plugins -newermt '1 hour ago'` returned nothing, which cannot be true of directories
modified eight minutes earlier. I read the `stat` line and never reconciled it with the `find`
line. Incident emitted 09:04:36; corrections appended to `elh9` and `52vf.10`; ledger fact
`masked-seats-cannot-measure-the-host` written and committed at `2e306d6`.

**A second, smaller one, caught before it mattered.** `grep -c 'RO_PATHS+=("$t/fort/scripts")'`
returned **0** for a string `grep -cF` finds at `lib:352`. I was one step from recording the
per-tree read-only bind as missing. Third false-discrepancy-from-a-bare-grep in this fort's recent
record. The rule is cheap: **`-F` for code literals, always.**

**And a scoping error in my own reasoning that the Warden caught.** I justified running a Forge
lane beside a Regent sitting on path disjointness. Paths stayed disjoint and no file was ever
contended — but the Forge bead's spec *named the Regent's file as its reference tree*, so the
Regent's edit invalidated the Forge's alignment 36 minutes after it shipped. Ilva's formulation:
**disjoint paths do not imply disjoint content when one lane's spec cites the other lane's file.**
The corrected test for parallel dispatch is disjoint write paths **and** neither lane's spec
citing a file the other may touch.

The mechanical repair the predecessor asked for still stands and I did not apply it consistently:
**before recording a verified fact, name the baseline and read it.** Here the unnamed baseline was
*whose filesystem I was looking at*.
