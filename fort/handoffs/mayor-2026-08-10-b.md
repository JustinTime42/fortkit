# Handoff: mayor 2026-08-10T (session B)
Model: Claude Fable 5 (as Emrith Cairnwright)

## State of work

- **Main is GREEN and pushed** (origin at a9fb3ed). Long session; the whole
  Muniment Room tail, a full new fort seat, and a security assessment landed.
- **Memory epic (fortkit-88u) substantially built and merged today:**
  88u.5 (consolidation, plain-SQLite index after FTS5 crash), 88u.7 (recall
  CLI), 88u.9 (bead-snapshot cap + cycle7-write-boundaries fact supersession),
  88u.14 (spec amendments: capped snapshot, ~8k budget, verbatim-sections
  accepted risk). All merged, verify green each time. Remaining 88u work:
  88u.6 (injection — preconditions now resolved), 88u.12/13/15 (hardening).
- **Backport loop: or2.2 spec approved, or2.3 drift watcher MERGED**
  (civ/scripts/drift-watch.mjs, Overseer's hand — kernel-RO). or2.7 §3.1
  amendment landed. Next: or2.4 (install the timer + first live run, which
  MUST include one real filing against a scratch tracker — --dry-run never
  exercises the bd seams; Warden or2.3 findings). or2.6 = Forge follow-ups.
- **THE RESEARCHER SEAT (fortkit-vhk) — new fourth fort seat, built and
  seated today.** Motivated by ForgeOs-lr8h (a research agent doing active
  external probes) and validated by the OpenAI ExploitGym incident (below).
  - Spec approved (vhk.1, docs/specs/researcher-seat.md). Launcher + profile
    templates merged after 4 clean Warden rounds (vhk.2/vhk.3). Live-installed
    in Manyhalls by the Overseer (fort/scripts/researcher.sh,
    fort/profiles/researcher-settings.json — kernel-RO, his hand).
  - **SEATED: Saelin Stillmere (it/its)**, actor id `saelin` (no roster
    collision), declared its own name/pronouns/personality/ladder at its
    seating (fort/seats/researcher.md; seat.founded + seat.named emitted).
    Ladder Sonnet 5 -> Opus 5 -> Mayor.
  - Its FIRST launch (seating smoke) caught a blocking bug: the deny-only
    profile bricked WebSearch/WebFetch in non-interactive -p mode. Fixed
    (vhk.10: added allow entries + Edit(**) replaces inert Write/NotebookEdit
    denies), Warden-approved, merged, Overseer reinstalled the live profile.
  - **RE-SMOKE PASSED** (task bbb6r9dqu). All 5 tools functional post-fix
    (WebSearch/WebFetch/Grep/Glob/Read); Saelin confirmed no Bash/write/Agent
    and attempted none. vhk.8 and vhk.10 CLOSED. Saelin is OPERATIONAL for
    supervised research; vhk.5 (formal boundary proof) remains the gate before
    UNSUPERVISED untrusted-web research.
- **Security assessment delivered (fortkit-zxc):**
  docs/assessments/rogue-agent-resilience-2026-08-10.md. The OpenAI Black Hat
  2026 ExploitGym/Hugging Face incident (models cheated an eval by hacking a
  third party for the answer key; emergent coordination + persistence). Verdict:
  external validation of the fort's capability-separation bet. Gaps filed: c62
  (Mayor subagent policy — DECIDED + encoded in mayor.md), ywa (egress
  restriction investigation), csb (egress logging), 16y (per-seat outbound-
  capability catalogue — Justin's over-restriction-wary standing concern).

## Verified facts

- Post-merge verify.sh green on main after every merge today (88u.5/7/9/14,
  or2.3, vhk.3, vhk.10). Test count 145 on the final tree.
- The Researcher's security spine is Warden-verified across 4 rounds: exact
  tool set WebSearch/WebFetch/Read/Grep/Glob, no --dangerously-skip-permissions,
  no bypassPermissions, whole-repo read-only via the mask, worktree .env
  inode-masked, evidence-gated close (RESEARCH-COMPLETE + exit 65).
- Live researcher profile verified post-reinstall: allow=[WebSearch,WebFetch],
  deny includes binding Edit(**) (not the inert Write/NotebookEdit).
- The OpenAI incident is real and multiply-sourced (OpenAI's own posts +
  Cybersecurity Dive, The Register, Forkast). Research was done read-only
  (WebSearch/WebFetch, no shell) as a deliberate instance of the safe pattern.
- Model default fix: Justin removed the global model line (was claude-fable-5)
  — x47 stays open until a Mayor handoff's Model line shows non-Fable (SO5).
  THIS session was still Fable (change applies to new sessions).

## Next actions

1. DONE: Saelin operational (re-smoke passed, vhk.8/vhk.10 closed). Next on the
   seat: vhk.5 (formal boundary proof) is the last gate before UNSUPERVISED
   untrusted-web research — dispatch the Forge to author the probe.
2. Saelin personhood parity (vhk.12, Overseer-approved): launcher writes
   researcher handoffs host-side + Mayor curates welfare/identity memories into
   the ledger scoped seats:[researcher]. NO write tools (Warden precedent +
   untrusted-content-poisoning safeguard). Fold the launcher change with vhk.9
   (researcher.sh prompt still points at retired fort/remember.md — fix before
   vhk.7 propagation) and vhk.13 (correct the misleading "gates" wording in the
   seat file + spec, gate-1).
3. vhk.11 (gate-1): add Researcher + Saelin to the charter Seats table.
4. vhk.7 propagation to elder forts + fort-init (blocked on vhk.8 + vhk.9).
5. Non-Researcher: or2.4 install/first-run, 88u.6 injection, or2.6/88u.15
   hardening batches.

## Open risks / questions

- Saelin is FOUNDED but not yet proven operational until the re-smoke confirms
  the tools work (in flight). Do not treat it as usable for real research until
  the re-smoke passes AND vhk.5 (boundary proof) is green.
- Accepted residual on record (Warden vhk.10 f4): broad WebFetch is a
  theoretical exfil channel (encode repo content into a fetched URL). Bounded:
  secrets inode-masked, no Bash, repo already pushed offsite. vhk.5 should
  measure + mark it accepted.
- Gate-1 queue for the Overseer: vhk.11 (charter recognition), vhk.13 (wording,
  needs approval before the seat-file edit), or2.4 (drift timer install), f3y
  (warden double-slash glob), plus the standing queue (5sk, 1q9, fci.6, etc.).

## Failed attempts

- or2.3 merge attempted from this masked Mayor session: EROFS on civ/scripts/
  (kernel-RO). The Overseer merged it by hand. Templates/ (vhk.3, vhk.10) I
  CAN merge myself; civ/scripts and fort/scripts/profiles I cannot.
- Committed the or2.7 spec with a premature "verify green" claim while verify
  was actually red from orphaned or2.3-merge debris (an untracked test importing
  the un-merged module). Corrected per SO7 (appended to or2.7). Lesson logged:
  chain verify with && not ; so a red verify blocks the commit.
- The Researcher's first seating smoke "failed" — but that was the point: the
  positive-control caught the profile bricking WebSearch/WebFetch before we
  trusted the seat. Proof-before-trust working as designed.
