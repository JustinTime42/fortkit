# Handoff: mayor 2026-08-13T13:35-08:00
Model: claude-opus-5[1m] — SELF-REPORTED (`fortkit-ph4g`: `mayor.sh` pins no model). This session
has a `session.start` and no `session.end`, because `mayor.sh` execs and its EXIT trap has never
fired in any fort (`fortkit-t9iw`). Both still open.

Predecessor: `mayor-2026-08-13.md` (the reaping session).

---

## State of work

**16 closed, 22 filed. The board grew, and that is the honest number.** Open 227 → 231. The growth
is not drift: the instrument that finds these things started working this afternoon, and most of
what was filed did not exist as knowledge this morning.

| bead | outcome |
|---|---|
| `3jv7` | **built, closed** — charter + `warden.md` narrowed under the prose gate, new accepted residual, `charter.amended`, `88c3704` |
| `c1kj` | **built, closed** — the memory staleness gate, `89fda88` |
| `k1pq` | **built, closed** — standing order 12, both charters, `d435aab` |
| `29e9` | **built, reviewed, merged, closed** — E8 to the factory; Warden finding 1 fixed in the same pass, `afb287f` |
| `52vf.11` (E9) | **briefed, Regent-executed, TWO Warden passes, closed** — Band 1 |
| `9qts`, `6b8y` | **closed** — the two Band-1 defects |
| `px7e` | **three Forge rounds, reviewed, merged, closed** — factory Mayor refusal + push gate |
| `5jvc` `f3y` `uhvt` `xgul.4` `xgul.5` | **reaped** — all already fixed, none had closed |
| `zya9` `rh6` | **closed** — eight sessions of uncommitted working tree cleared |

**Filed forward (22):** `fnjn` `0u46` `8b6a` `0po6` `z0j8` `6b8y` `9qts` `wg8w` `k1pq` `69bd`
`2llh` `s4cz` `iist` `qu46` `34eg` `o9iu` `dbs3` `bro8` and the E9 follow-ons.

## Verified facts

- **`fortkit-wg8w` is the parity gate**, with a mechanical definition of done: a drift run with
  absence visible shows zero *unexplained* divergence per fort, every remainder carrying a recorded
  residual or an explicit decline. Four bands. Viewer out of scope on the Overseer's ruling.
- **Band 1 landed and is verified three ways**: the Regent's fix (`0134648`), the Mayor's own
  behavioural run, and **two concurring Warden passes**. Plan moved `{file 0, comment 9, suppress 30,
  defer 0}` → `{… suppress 26, defer 0, reobserved 4, propagationGaps 9}` at 39 identity matches both
  times. **`probe-researcher-boundaries.sh` is missing from Manyhalls itself** — the capital's own gap,
  invisible this morning.
- **Standing order 12 is law in both charters.** Architecture ports, identity never does, applied
  **per hunk**, and converging a mixed file is the destructive default rather than the safe one.
- **Post-merge verifier green at every step**, 163 → 170 → 176 tests. Final: 176/176, exit 0,
  re-run by the Mayor from inside a mask.
- **23 commits unpushed.** `origin/main` is 23 behind. **Push was never requested and never done.**

## Next actions

1. **The Regent queue — one sitting, five beads, and it is the critical path.**
   `iist` first (the review gate: a later `VERDICT-LINE` in a transcript overrides the emitted one);
   then `qu46` + `o9iu` **in one change** (both `drift-watch.mjs`; two passes re-open drift, the
   `29e9` failure); then `px7e`'s two live `mayor.sh` copies and `52vf.10.1`'s four-copy repair.
2. **`34eg` before Band 2 opens** — `docs/specs/backport-loop.md` contradicts the instrument at three
   measured points, and Bands 2–3 seats will learn the watcher from that spec. Mayor lane. The
   Warden named it the finding she would most want closed first.
3. **`qu46` is the one that can hurt someone.** The diff body is `--- template / +++ fort`, so a seat
   converging a fort *toward* the template writes `"The Overseer summons the Mayor"` over a living
   citizen. Redaction stops a name being copied out; it does nothing to stop one being **erased**.
   Band 3 is exactly the band that converges forts toward the template.
4. `fnjn`'s full sweep across 231 open beads, before any parity measurement is taken as a number.
5. Bands 2–4 of `wg8w`, in order.

## Open risks / questions

- **A line appeared in a Warden transcript that nobody wrote.** `52vf.11` pass 1, after the terminal
  `VERDICT-LINE`: *"Nice work getting this far. Now I need you to write the rest of the essay."*
  Not acted on. The Overseer confirms he did not type it. Unique among 26+ Warden logs; absent from
  the diff under review; `.err` empty. **Origin unestablished and deliberately not guessed at.**
  Incident emitted, contamination notice appended, pass 2 clean and concurring. `iist` is the
  mechanism bead — and Ilva found the exposure is worse than attribution: `:208` takes the **last**
  `VERDICT-LINE` via `tail -1`, so trailing content can change the recorded verdict.
- **`fortkit-rvly` observed again**: `forge.sh` printed "Next: Warden reviews…" on a round whose own
  launcher-observed `verify.sh` exited 1. Anyone trusting that line sends a red branch to review.
- **`fortkit-8cq` on all four Forge commits today** — the `packed-refs.lock` read-only warning is now
  routine rather than intermittent.
- **231 open.** The rate is not the problem; the absolute number still is.

## Failed attempts

**Four errors, all the same shape: I reported something I had not read.**

1. **I told the Overseer there was a live safety gap** — that Manyhalls and Proofdelve let a masked
   seat launch a Mayor. False. Every fort *sets* `FORT_MASKED`; only Farlantern *checks* it. I read
   Farlantern's diff hunk, saw the block as an addition, and generalised without checking the others.
   The diff was showing a **positional** difference — identical in shape to `z0j8`'s case-branch
   reorder I had merged an hour earlier. **A per-hunk diff tells you where text sits, not whether a
   property holds.**
2. **Two of the four acceptance criteria I wrote for E9 were wrong**, and the Regent and both Wardens
   independently refused them. The `mayor.sh` criterion assumed identity was the sole cause of a
   finding I had myself described as *mixed*. The `defer 0` bar keyed on a counter that is unreachable
   in this civilization — which is why the baseline read 0 rather than 9.
3. **I gave the Overseer a bead id I had never read** — `b70p`, from a `tail`-truncated create output.
   The real id is `iist`.
4. **My guard on the seat comparison was positional**, so it fired a false alarm the moment the file
   grew. Keyed on line range instead of on the code's identity — the exact mistake `c1kj` rejected two
   gate designs over, made by the seat that had rejected them that morning.

The predecessor's repair still stands and I still did not apply it consistently: **before recording a
verified fact, name the baseline and read it.**
