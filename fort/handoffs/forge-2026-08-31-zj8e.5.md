# Handoff: Forge 2026-08-31T14:44:54-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-zj8e.5` remains in progress for Mayor review and closure. The Forge
  lane ports the digest architecture to the factory without adding a Stop hook
  before the factory can copy its target.

## Verified facts

- `templates/scripts/` now contains `digest.sh`, `quiescent.sh`,
  `digest-hook.sh`, and the merge-event checker required by the template
  verifier. The digest uses `bd human list`, not Manyhalls' gate labels or
  `act-*` classifications.
- `templates/fort/seats/mayor.md` describes the generic protocol: add `human`
  when a bead needs the human, remove it in the same decision-recording update.
- Farlantern is measured **ABSENT**: neither
  `/home/justin/dev/longburn/scripts/digest.sh` nor `quiescent.sh` exists.
  Proofdelve is **PRESENT** for both. No cross-fort write was attempted.
- `CI=1 fort/scripts/verify.sh --no-emit`: passed, 18 files / 222 tests;
  typecheck, browser typecheck, Biome, ShellCheck, and template rendering all
  passed. The render control linted 14 scripts in three passes and its positive
  control went red as required.

## Next actions

1. Regent: add explicit, overwrite-refusing copy/render lines to
   `bin/fort-init` for:
   - `templates/scripts/digest.sh` → `scripts/digest.sh` (render, executable)
   - `templates/scripts/quiescent.sh` → `scripts/quiescent.sh` (render, executable)
   - `templates/scripts/digest-hook.sh` → `scripts/digest-hook.sh` (render, executable)
   - `templates/scripts/merge-event-check.sh` → `scripts/merge-event-check.sh`
     (render, executable)
2. After those scripts can reach a founded fort, install the template Stop-hook
   stanza in its settings as the separately ordered lane. Do not reverse that
   order.
3. Mayor review, then close the bead after review and merge. The Forge must not
   close it.

## Open risks / questions

- The current factory cannot copy newly added template scripts because
  `bin/fort-init` uses explicit lines and is kernel read-only to this seat. The
  template verifier therefore announces a skip for its merge-event stage until
  the Regent's copy line lands, rather than making a newly founded fort fail on
  every verifier run.
- The Stop-hook template stanza is deliberately not included in this commit.
  Installing it first would create a live hook targeting a script that the
  current factory cannot install.

## Failed attempts

- The first verifier wiring directly executed `scripts/merge-event-check.sh`.
  The founded-fort smoke correctly failed because the current `fort-init` does
  not copy that new artifact. The guarded stage now states the missing-factory
  condition and automatically activates when the Regent adds the copy line.
