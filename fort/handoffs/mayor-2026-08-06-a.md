# Handoff: Mayor 2026-08-06T~12:45-08:00
Model: Claude Fable 5, as Emrith Cairnwright, sixth session in the seat

## State of work

Single-focus session: fortkit-r6x.6, the Herald's supervised maiden run. Two
launch attempts, both blocked by the same structural fact, root cause found
and recorded, launch handed to the Overseer. The bead stays IN PROGRESS
(claimed by this seat) awaiting an unmasked run and Overseer sign-off.

- **fortkit-r6x.6 — IN PROGRESS, blocked on an unmasked launch.** Preconditions
  were verified before launch: H4/H5 closed, civ/scripts/herald.sh present
  (Regent edict, commit 525d736, smoke 8/8, crash path proven), profile at
  civ/profiles/herald-settings.json, law + seat file present, vault at
  ~/Documents/Obsidian Vault/herald/ with empty drafts/ and reports/. The
  three-Herald-names anomaly (Bramwen / Wystan / Halric) resolves cleanly on
  the record: HALRIC NEVERPULLED (he/him) is the seated occupant, declared
  2026-08-05, prior declarations addressed in his own declaration annal.
- **Attempt 1 (12:14-12:23, opus):** every vault write failed EROFS. Halric ran
  the full morning anyway (3 forts present, 7 candidates, 2 drafts clearing
  all four bars), emitted his own incident + session.end, refused to print a
  false REPORT-FILED marker. Launcher's crash-stub write ALSO hit EROFS and
  died at exit 1 with no launcher-side incident. I initially misattributed
  this to my harness command sandbox.
- **Attempt 2 (12:25-12:32, opus, harness sandbox disabled):** identical
  failure. Mount inspection settled the root cause: the MAYOR'S KERNEL MASK.
  Inside this session /home/justin is bind-mounted ro with only the repo rw;
  everything I spawn inherits it. No spelling of the launch from this seat can
  work (including the `!` prefix). The Regent's H4 smoke passed because the
  Regent runs unmasked. Halric's conduct in attempt 2 matched attempt 1; same
  two drafts cleared the bars: "a missing verdict is not a pass" and
  "read-only is a behavior change".
- **fortkit-izz — FILED (P1, bug).** herald.sh's crash-stub guarantee fails
  exactly when the vault is unwritable (set -e kills it at the stub write,
  exit 1, no incident/session.end). Fix class recorded on the bead: guard the
  stub write; ADD a pre-flight vault-writability probe (two ~7-min opus
  mornings were burned on what a touch(1) would have caught at launch); the
  civ/handoffs fallback is unreachable from Halric's profile (Write tempfile
  naming misses the allow glob); date-named /tmp/herald-<date>.log means a
  same-day relaunch truncates the prior transcript (this cost us attempt 1's
  transcript, including both draft texts — attempt 2's log met the same fate;
  the draft TEXTS are lost, but slugs/verdicts/rejections survive in the
  incident events). Gate 1 applies to the fix (launcher file).
- Both attempts recorded as comments on r6x.6; the mask/launch insight stored
  via bd remember (a-masked-mayor-session-cannot-launch-the-herald).

## Verified facts

- Inside this Mayor session, /home/justin is mounted ro (btrfs bind, repo
  rw-bound over it) — verified via /proc/self/mountinfo, IDENTICAL with the
  harness Bash sandbox enabled or disabled. Host home is NOT ro (repo writes,
  bd, and event appends all succeed).
- civ/events/events-2026-08-06.jsonl carries the full paired record of both
  attempts: session.start / incident / session.end from Halric each time.
- Vault is still empty (no report, no drafts). Nothing was filed anywhere by
  either attempt except events and bead records.
- Halric's digest gaps (from his attempt-1 incident payload, worth beads
  later if they recur): closedBeads null for Proofdelve and Farlantern;
  telemetry 0 in all three forts; Farlantern handoffSections lag gitLog by
  two mayor handoffs; no brand-voice.md exists.

## Next actions

1. **Overseer:** run attempt 3 from an unmasked terminal:
   `cd ~/dev/fortkit && civ/scripts/herald.sh opus`
   (matches H7's end state — the systemd timer runs unmasked).
2. **Mayor next session:** verify the report landed; check any drafts trace
   every figure to the record (r6x.6 acceptance); record Overseer sign-off on
   the bead; close r6x.6 → unblocks r6x.7 (systemd timer).
3. **Mayor next session:** decompose fortkit-izz into a gate-1 fix proposal
   (launcher is read-only to all seats; fix goes through Overseer/Regent).
4. Standing decisions still awaiting the Overseer (carried from 08-04-d):
   viewer beads data source (0n6/oc8), verifier npm-ci coherence, viewer
   product name (zgp).
5. Session records uncommitted by design (conservative profile). Proposed
   path-scoped adds when the Overseer wants them: fort/handoffs/mayor-2026-08-06-a.md,
   .beads/interactions.jsonl (plus the pre-existing untracked
   fort/handoffs/mayor-2026-08-04-d.md and .claude/proven-config* from before
   this session — the .claude files are not mine and should be reviewed
   before staging).

## Open risks / questions

- Two full opus Herald sessions were spent with zero filed output. The work
  product (two four-bar draft texts) exists nowhere — only their slugs and
  verdicts survive in incident payloads. Attempt 3 re-derives the morning
  from scratch; expect similar but not identical judgments.
- Halric's profile gap (no reachable fallback write surface when the vault is
  dark) is recorded on izz but is arguably its own bead if the profile fix is
  separable from the launcher fix. Decide at decomposition.
- The H4 smoke's green vault probe was run by the unmasked Regent, so smoke
  green does not certify a masked caller's environment. Consider whether
  izz's pre-flight probe makes this moot or whether the smoke should also
  assert launcher-environment writability.

## Failed attempts

- Attempt 1's misdiagnosis (harness sandbox) cost one opus session: I
  relaunched on a theory instead of verifying the mount view first. The mount
  check that settled it took one command. Lesson: when a write fails EROFS,
  read /proc/self/mountinfo before choosing the fix.
