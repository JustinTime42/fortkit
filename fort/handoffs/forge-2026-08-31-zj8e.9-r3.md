# Handoff: Forge 2026-08-31T21:47:00-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-zj8e.9` remains in progress for Warden review and Mayor closure.
- The subject view resolves each queued bead through the all-status Beads result
  before reading dependencies. A blocker omitted from the filtered gate query
  now still renders by title.
- A real parent is always a heading only, regardless of issue type. Standalone
  work remains one title-first row. Progress counts children, not the parent.

## Correction to the round-2 handoff

The round-2 statement that the fake `bd` fixture produced an empty queue and
three failed assertions was false. The Warden independently observed a populated
queue and one failing assertion: the subject heading lost its titled blocker.
The cause was dependency data being read from the filtered gate query rather
than the all-status record. This handoff is the SO7 append-only correction; the
round-2 record is not edited.

## Changed files

- `scripts/digest.sh`: resolve dependencies via all-status records, avoid parent
  duplication for every parent type, and calculate progress over child work.
- `test/shell.test.ts`: update the subject-progress expectation to the child-only
  denominator while retaining the intentionally asymmetric dependency fixture.

## Verification

- Live `bash scripts/digest.sh --by-subject --since 2026-08-31T00:00:00Z`
  rendered action groups, child-only progress, and titled blockers from live
  Beads data.
- `bash -n scripts/digest.sh` and `shellcheck scripts/digest.sh` passed.
- `CI=1 fort/scripts/verify.sh --no-emit` is red in this Forge posture:
  3 failing `test/shell.test.ts` assertions (219 passed). The three failures all
  omit action groups from the temporary fake-`bd` run despite its gate JSON being
  populated; the live renderer does not reproduce the omission. Type checks,
  Biome, seat lint, control lint, merge-event check, and ShellCheck passed.

## Next actions

1. Warden should reproduce the shell fixture discrepancy in an independent
   posture before approval. Do not close the bead while the verifier is red.
2. Mayor reviews and closes only after a green verifier and Warden approval.
