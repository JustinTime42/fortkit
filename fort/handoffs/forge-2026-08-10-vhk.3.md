# Handoff: Forge 2026-08-10T18:00:37-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-vhk.3` remains IN_PROGRESS as directed. The template Researcher
  launcher and its coupled `fortkit-vhk.2` profile are implemented and ready
  for Warden review; the Mayor retains closure.

## Verified facts

- `templates/fort/scripts/researcher.sh` separately launches Claude from an
  empty scratch cwd with the repository read-only, an exact
  `WebSearch,WebFetch,Read,Grep,Glob` tool set, `--setting-sources ''`, and
  `researcher-settings.json`; it does not enable skip-permissions or grant a
  shell or Agent tool.
- The host close path writes captured findings to the dispatching bead, emits
  `handoff.written`, then emits `session.end`. The vhk.6 extension point is
  explicitly constrained to re-enter this same launcher/profile at depth one.
- `test/researcher-launcher.test.ts` prevents drift in the exact tool set,
  settings isolation, kernel mask, and non-bypass profile.
- `CI=1 fort/scripts/verify.sh --no-emit` exited 0: Node/browser typechecks,
  Biome, 14 Vitest files / 145 tests, and ShellCheck passed.

## Next actions

1. Request Warden review of the path-scoped commit.
2. The Overseer may install the reviewed template artifacts into the live
   protected `fort/scripts/` and `fort/profiles/` surfaces (gate-1).
3. Implement bounded same-profile fan-out in `fortkit-vhk.6`, re-entering this
   launcher rather than using the general Agent tool.

## Open risks / questions

- The Researcher model ladder remains deliberately unfilled in its seat
  template; its final ladder is `fortkit-vhk.4` work.

## Failed attempts

- The focused Vitest run initially failed because this worktree lacked
  dependencies (`vitest: command not found`); `npm ci` restored the declared
  toolchain before the green focused and full runs.
