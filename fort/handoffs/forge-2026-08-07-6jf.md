# Handoff: Forge — fortkit-6jf

Model: GPT-5 Codex, as Kethra Anvilmark (she/her)

## State of work

- `fortkit-6jf` remains IN_PROGRESS, as directed; it was neither closed nor otherwise changed.
- Forge now sources the shared `seat-sandbox.sh` builder. The inline mask block is gone from both the live launcher and the fort-init template.
- The shared builder gained two narrow Forge options: `--env-root` retains worktree `.env*` coverage, and `--mask-ssh-auth-sock` retains the former Forge behavior of denying the SSH agent socket. Forge also supplies its worktree `.claude` directory as an additional read-only path.

## Verification

- `bash -n` and ShellCheck passed for the live/template launchers and shared libraries.
- A Forge-equivalent `bwrap` probe confirmed write attempts to `fort/charter.md`, `fort/seats/forge.md`, and `fort/profiles/warden-settings.json` fail with the read-only filesystem boundary.
- `CI=1 fort/scripts/verify.sh --no-emit` passed: both TypeScript checks, Biome, 80 Vitest tests across 9 files, and the verifier's ShellCheck stage.

## Surprise

- The worktree initially had no installed Node dependencies (`tsc: command not found`); `npm ci --no-audit --no-fund` restored the lockfile-pinned toolchain without changing tracked dependency files.

## Next action

1. Warden reviews the scoped commit, including the observed constitution-path probe.
2. Mayor merges after an approving recorded verdict; the dated `fort/remember.md` caveat is intentionally untouched, per dispatch.
