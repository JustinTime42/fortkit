# Handoff: Forge 2026-08-08T21:35-08:00

Model: GPT-5 (Codex), as Kethra Anvilmark

## State of work

- `fortkit-fav` remains IN_PROGRESS as instructed. The PixelLab runner now
  submits raw PNG base64 to the animation endpoint, retries that endpoint once
  after a 5xx response, and can reuse hash-verified stills when
  `PIXELLAB_REUSE_STILLS=1`.

## Verified facts

- `scripts/run-pixellab-trial.mjs` sends `reference_image.base64` without a
  data-URL prefix, while `pngFromBase64` still accepts both response forms.
- Animation requests log their first and (when needed) second attempt, pause
  250ms after a first 5xx, and never retry a 4xx or a still-image request.
- Reuse reads the prior manifest and retains the matching entry's provenance,
  adding only `reusedAt`; a missing, invalid, or hash-mismatched PNG falls
  through to normal generation.
- `test/pixellab-trial.test.ts` pins raw outbound base64, 500→200 success,
  double-500 failure, no 422 retry, and matching/mismatching still hashes.
- `fort/scripts/verify.sh --no-emit` passed: Node and browser typechecks,
  Biome, Vitest (113 tests across 10 files), and ShellCheck.

## Next actions

1. Warden: review the path-scoped `fortkit-fav` commit.
2. Overseer: on the next authorized live run, set `PIXELLAB_REUSE_STILLS=1`
   to avoid regenerating verified stills; observe the raw-base64 animation
   request against the vendor.
3. Mayor: close the bead only after review and merge.

## Open risks / questions

- The vendor's previous 500 remains a live-service outcome that no local test
  can prove resolved; this change records and bounds its client-side retry.
- Reuse intentionally trusts manifest SHA-256 equality, not a regenerated
  request identity, so the original asset provenance remains the truthful
  record of the on-disk still.

## Failed attempts

- The first focused test run could not start because repository dependencies
  were absent (`vitest: command not found`). `npm ci` restored the locked
  dependencies; all subsequent focused and full verification passed.
