# Handoff: Forge 2026-08-11T10:50:56-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-88u.16` remains IN_PROGRESS for Mayor review. memory-lint now
  measures core budgets per seat and exposes the `seats: [all]` shared floor.

## Verified facts

- `scripts/memory-lint.mjs` derives the seat roster from `fort/seats/`, adds
  each shared core fact to every seat's budget, and prints a fact/line report
  for the shared floor and each seat.
- `test/memory.test.ts` plants a Forge-only over-budget fact set: 23 core facts
  at 299 lines pass; adding the 24th makes the lint fail at 312 lines.
- `CI=1 fort/scripts/verify.sh --no-emit` passed: memory-lint reported a
  shared floor of 2 facts / 33 lines; TypeScript checks, Biome, all 153 tests,
  and ShellCheck passed.

## Next actions

1. Submit the path-scoped commit for Warden review.
2. After review and merge, run the combined-tree verifier before the Mayor
   closes `fortkit-88u.16`.

## Open risks / questions

- The linter treats every `seats: [all]` fact as one shared contribution per
  roster seat; no single session receives another seat's scoped facts.

## Failed attempts

- The first focused-test fixture wrote seat files concurrently with creation of
  their directory, causing an `ENOENT`; setup now creates directories before
  the parallel fixture writes.
- This worktree's memory spec still had the global-core wording despite the
  bead note saying the Mayor half had landed, so this commit includes the
  required §4.2/§4.4 wording with the enforcement change.
