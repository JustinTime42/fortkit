# Handoff: Forge 2026-08-13T20:06:33-08:00
Model: gpt-5.6-terra
## State of work

- `fortkit-n3bk.2` remains in progress for Warden review and Mayor closure. The factory permission template now carries the post-E8 spelling and the three relative protections shared with the live capital.

## Verified facts

- `templates/config/settings-permissions.json` replaces the six granular `fort/scripts` denies with `Edit(fort/scripts/**)`, adds `Edit(.claude/**)` and `Edit(fort/profiles/**)` to deny, and adds `Edit(scripts/**)` to allow.
- The template rules were compared directly with `.claude/settings.json`; the target additions match the live capital's relative spellings.
- `jq . templates/config/settings-permissions.json` parses the template successfully.
- `CI=1 fort/scripts/verify.sh --no-emit` passed after `npm ci` installed the previously absent locked dependencies: TypeScript checks, Biome (64 files), Vitest (15 files / 180 tests), and ShellCheck all completed successfully.

## Next actions

1. Send the path-scoped commit for Warden review.
2. Let the Mayor close `fortkit-n3bk.2` only after review and merge.
3. `fortkit-domm` must measure a real founded fort: zero inert-permission warnings with a positive control. This template alone is not a launchable settings file.

## Open risks / questions

- The acceptance launch measurement is deliberately deferred to `fortkit-domm`. `bin/fort-init` substitutes `{{REPO_PATH}}`, so it cannot be taken against this template without founding a fort.
- Absolute-path denies and capital-only `bin/**`, `civ/profiles/**`, and `civ/scripts/**` rules remain intentionally outside this change.

## Failed attempts

- The first verifier run stopped before typechecking because this fresh worktree had no `node_modules` (`tsc: command not found`). `npm ci` installed the lockfile dependencies, and the rerun passed.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-08-13T20:06:39-08:00: exit 0

```
> vitest run


 RUN  v4.1.10 /home/justin/dev/fortkit-worktrees/n3bk.2

(node:4006466) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)
(node:4006467) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)

 Test Files  15 passed (15)
      Tests  180 passed (180)
   Start at  20:06:34
   Duration  2.89s (transform 1.09s, setup 0ms, import 1.90s, tests 6.62s, environment 1ms)

```
