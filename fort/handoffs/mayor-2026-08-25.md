# Handoff: mayor 2026-08-25T07:37:09-08:00
Model: claude-opus-5[1m] — **SELF-REPORTED, not launcher-derived.** `mayor.sh` pins no
model (`fortkit-ph4g`, open), so neither the commit nor the `session.start` payload
records one. Session opened 2026-08-24T14:11:36 and closed on the 25th.

## State of work

**A read-only advisory session. No bead was claimed, closed, or filed; no code changed.
Justin asked whether he could run Codex as the primary runtime for agents in some forts,
and whether he could mix and match per seat. The answer is yes to mixing, but the
finding underneath it is that `build_mask codex` is not a runtime branch at all: it is a
Forge branch wearing a runtime's name, and three of its four deltas are wrong for any
attended seat. A three-bead tree was proposed and DELIBERATELY NOT FILED. I told him I
would show him the tree before anything reached `bd`, and he closed the session before
answering.**

| item | state | outcome |
|---|---|---|
| Codex feasibility question | ANSWERED, nothing filed | Per-seat runtime is already the design; the mask lib's seat axis is not. |
| proposed bead tree (3) | NOT FILED, awaiting approval | axis split (Regent lane) · Codex-Warden spike in Proofdelve · Codex-Mayor, which depends on the split. |
| `70806e9` Regent edict | landed MID-SESSION, verified legitimate | Announced correctly; see Verified facts. |

Beads touched: **none.** The fort stands where `mayor-2026-08-17-b.md` left it, minus the
event-stream drift the Regent cleared.

## Verified facts

- **`build_mask` is already seat-type parameterized and the codex branch is live**, not
  theoretical: `fort/scripts/lib/seat-sandbox.sh:205-243` (codex) and `:244-345`
  (claude), with `forge.sh:75` calling `build_mask codex`. Anyone porting a seat starts
  from working machinery, not a blank file.
- **The codex branch conflates runtime with posture.** Every one of its deltas is
  justified IN ITS OWN COMMENTS by unattendedness rather than by which CLI runs:
  - `:216` `MASK_DIRS+=("$HOME/.claude")` — "the Forge has no business with the other
    runtime's credentials or memory". This also removes `~/.claude/civilization.json`,
    named at `:257` as "the settlement registry every civ tool reads", and
    `~/.claude/skills`.
  - `:241` `RO_PATHS+=("$t/fort/charter.md" "$t/fort/seats" "$t/scripts/verify-impl.sh"
    "$t/skills")` — "an unattended seat cannot ask first, so a prose gate on it guards
    nothing (cycle 6 ruling)". Those four paths are exactly what cycles 7 and 13 moved
    to a PROSE gate for attended seats, and three of the four are Mayor work.
  - `:518` `SSH_AUTH_SOCK` sits in `claude_only`, with the rationale spelled out at
    `:501-504`: "the codex seat gets neither the socket nor the name, because the
    unattended Forge never pushes." The Mayor is the seat that pushes.

  A Codex MAYOR under this branch therefore loses the registry, loses both skills, cannot
  amend the charter or the seat files or the verifier, and cannot push. A Codex WARDEN
  loses nothing: she is read-only, never pushes, never amends, and binds her whole
  checkout as extra read-only, so the unattended lock is redundant for her.
- **`~/.claude/skills` is exactly two symlinks and both point into this repo**:
  `civ -> /home/justin/dev/fortkit/skills/civ` and
  `fort-backport -> /home/justin/dev/fortkit/skills/fort-backport`. Masking `~/.claude`
  costs any Codex seat both, and `civ` is the skill that answers "who holds which seat,
  what is this fort's law."
- **Proofdelve carries the same mask lib structure, in its own copy.**
  `ForgeOs/fort/scripts/` holds its own `mayor.sh`, `forge.sh`, `warden.sh` and
  `lib/seat-sandbox.sh`, with the same `codex_only`/`claude_only` split (`:523`/`:525`)
  and the same RO carve-out line (`:242`). Line numbers differ from ours by about seven,
  so the copies are NOT byte-identical and I did not diff them. Per-fort launcher copies
  are why mixing runtimes per fort works at all.
- **Codex CLI is 0.147.0**, and it carries policy machinery the fort's own record does
  not mention: `[permissions.<name>.filesystem]` path denies in `~/.codex/config.toml`,
  `-p/--profile` layering `$CODEX_HOME/<name>.config.toml`, `--ignore-user-config`,
  `--ignore-rules`, `--json`, `-o/--output-last-message <FILE>`, and
  `--output-schema <FILE>`.
- **The Warden's recurring verdict-capture failure class is a text-scraping problem
  Codex has a first-class answer to.** `warden.sh:193-198` runs `claude -p` and the close
  path greps the transcript for a VERDICT-LINE; "recorded NO verdict: no VERDICT-LINE in
  transcript" appears eight times in the incident log in `fort/memory/current.md`,
  alongside `fortkit-5mw` and `fortkit-7kzi`. `--output-schema` plus `-o` writes a
  schema-constrained final message to a named file.
- **`~/.codex/rules/default.rules` is a 6,957-byte GLOBAL accumulated allow list**
  (`prefix_rule(pattern=[...], decision="allow")`, one per past interactive approval),
  shared across every project, and `curl` is allowed outright in it. Any Codex seat
  inherits it unless launched with `--ignore-rules`.
- **Codex path denies accept only exact paths or `dir/**` subtrees**; filename globs like
  `**/.env*` are rejected at startup, and the profile is policy-layer only on Linux
  (openai/codex#11316). Both are recorded in the header of `~/.codex/config.toml`,
  written by Proofdelve's Forge on 2026-08-04. bwrap remains the real boundary either way.
- **`-p/--profile` LAYERS on top of the base user config rather than replacing it**, per
  `codex exec --help`. That is weaker than the Warden's `--setting-sources ""`, whose
  whole point per `warden.sh:5` is that the fort's profile is the ONLY source.
  `--ignore-user-config` is the candidate replacement. UNTESTED: whether `-p` still
  resolves underneath it. That is the first probe of any Codex seat work.
- **`fortkit-78o` is the standing evidence that the charter's cross-vendor ladders have
  never been real.** Every Claude seat's ladder names a GPT rung; on 2026-08-08 the
  Warden's failed live with "There's an issue with the selected model (gpt-5.6-sol)."
  The launchers are runtime-specific, not model-specific.
- **The Regent's edict `70806e9` landed mid-session and is LEGITIMATE, checked rather
  than assumed.** The charter's civilization-layer section makes an unannounced
  privileged change a security signal, so I checked instead of assuming: `edict.begun` at
  2026-08-24T20:09:16 and `edict.ended` at 2026-08-25T07:31:49, both from `calder`, both
  on this fort's own stream. It committed seven days of drifted event streams
  (`civ/events` 08-18..08-24, `fort/events` 08-17..08-24) and changed nothing in the
  capital's code.
- **`ssh-add -l` returns one ED25519 identity**, so the push blocker named as next action
  1 of the previous handoff is GONE. Push was still not performed: not asked for, not
  approved.

## Next actions

1. **Show Justin the three-bead tree and file it on his approval.** As proposed: (a)
   split the mask lib's seat axis into runtime and posture, Regent lane, all three forts
   plus the template or it manufactures a divergence; (b) a Codex-Warden spike in
   Proofdelve, which depends on nothing and can start immediately; (c) Codex-Mayor, which
   depends on (a). He did not answer, so nothing is filed.
2. **If he wants only one thing, it is the Codex Warden.** It needs no mask-lib change,
   and `--output-schema` plus `--sandbox read-only` would close the verdict-capture class
   and improve `fortkit-3jv7` / `fortkit-ypv1` by construction rather than by deny list.
3. **`fortkit-p5mr.10` still needs its Warden re-review before it closes.** Unchanged
   from the last handoff; SO9 makes the review the gate.
4. **`fortkit-p5mr.9` / `fortkit-34eg`** remain the highest-leverage cleanup:
   `docs/specs/backport-loop.md` still encodes the repealed convergence policy, and
   `fortkit-wg8w` is named in `fort/memory/current.md` as the gate the Overseer resumes
   behind, so a fresh seat is pointed at repealed policy at session start.
5. **`fortkit-ypv1`** is still the most consequential open bug: four forts, and the sole
   mitigation named in an accepted charter residual.
6. **Nine commits are unpushed** (`main...origin/main [ahead 9]`). The agent now holds an
   identity, so a push would work. It needs Justin's word, every time.

## Open risks / questions

- **The asymmetry that does not resolve: the Researcher is probably not portable.** Its
  whole boundary is `researcher.sh:70`, `--tools "WebSearch,WebFetch,Read,Grep,Glob"`,
  withholding Bash and Agent; its own profile says "The LAUNCHER'S exact tool set is the
  security boundary... These denies are defense in depth only." Codex has no
  tool-whitelist flag and its agent always holds shell. Porting it means rebuilding a
  capability boundary as a deny list, which is the design the seat was created to avoid
  after ForgeOs-lr8h. My recommendation is do not port it.
- **`~/.codex` is bound live read-write for BOTH seat types** (`seat-sandbox.sh:416`), so
  a Codex seat can write `~/.codex/AGENTS.md`, `skills/` and `plugins/` at the kernel,
  which the next launch executes. For Claude seats the profile denies cover it. For a
  Codex seat the mitigation named at `:304-321` is an argument about `workspace-write`
  writable roots, and `fortkit-wtps` records that the mask harness carries NO `~/.codex`
  assertion at all. Any seat moved to Codex inherits an argument rather than a probe.
- **A Codex Mayor would be the most informative experiment and the most expensive one.**
  It is the only swap that reveals what Codex is like to WORK WITH rather than to run,
  and it is the swap that needs the axis split first. That tension is Justin's to resolve.
- **The tree moved under this session.** The Regent's edict committed between my
  `session.start` and this handoff, so the git state I reported at the top of the session
  (ahead 8, fourteen untracked event files) was true when read and false an hour later.
  That is `fortkit-0iwa`'s class reaching the Mayor's seat and not only the Warden's.
- **`.beads/issues.jsonl` carried 17 removed-and-re-added lines I did not author**, all
  of them drift-watcher beads, pure export churn from the daily scans. Committed here
  rather than left to drift, and named as not mine.
- **Halric reports every fort's closed-bead view unusable for eight-plus consecutive
  runs.** `fortkit-ebf` covers teaching him to notice the error status; I found no bead
  for the view itself being broken. Not filed, because it was not asked for.
- **`session.end` will not be emitted by the launcher for this session.** `mayor.sh` sets
  an EXIT trap and then `exec`s bwrap, so the trap never fires (`fortkit-t9iw`, open, all
  three forts).

## Failed attempts

- **I twice read a persisted tool-result path back with `sed` and got the persistence
  notice instead of the file**, because the output was large enough to be persisted
  again. Cost two round trips on `fort/memory/current.md`. Reading the source file with a
  line range and `cut -c1-300` worked first time; do that instead of re-reading a
  persisted blob.
- **I nearly answered the Codex permission question out of the fort's own record.** That
  record describes Codex as having coarse sandbox modes only. 0.147 has a filesystem
  permissions profile, execpolicy `.rules`, profile layering, JSONL output and an output
  schema. `codex --help` cost one command, and the stale answer would have been wrong in
  the direction that makes the experiment look harder than it is.
- **I initially framed the Mayor as the easy swap**, on the strength of "its boundary is
  the mask plus a prose gate, and neither cares which runtime is inside." That was
  reasoning about the architecture instead of reading the branch. Reading
  `seat-sandbox.sh:205-243` reversed it: the Mayor is the expensive swap and the Warden
  is the free one. This is `read-the-artifact-remember-the-why` in its second form, a
  claim about a subject I had not opened, and it survived one full answer to Justin
  before his follow-up question made me open the file.
- **A heredoc write of this handoff was denied by the permission layer** (`cat >` with a
  redirect). The Write tool succeeded on the same path. Not a mask refusal;
  `fort/handoffs/` is writable to this seat.
