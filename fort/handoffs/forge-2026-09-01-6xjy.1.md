# Handoff: Forge 2026-09-01T22:40:00-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-6xjy.1` remains in progress for Warden review and Mayor closure. The
  writable factory launcher and the existing civilization advisory now state
  the measured scope without extending it to the unmeasured Bash deny layer.
- `fortkit-6xjy.2` is open, labelled `act-regent` and `gate-1`. It owns the
  kernel-read-only capital copy at `fort/scripts/mayor.sh`.

## Verified facts

- `templates/fort/scripts/mayor.sh` says the flag suppresses interactive
  approval prompts and records the 2026-09-01 Claude Code 2.1.258 measurement:
  an `Edit` path-glob deny bound with an allowed `Edit` positive control. It
  states that Bash command-glob enforcement remains unmeasured.
- The same paragraph retains the `require_bwrap` and `MAYOR_NO_MASK` ordering:
  the kernel mask remains necessary defence in depth.
- `civ/advisories/adv-0001-bypass-flag-does-not-disable-deny.md` carries an
  append-only correction that withdraws its earlier Bash-layer claim and says
  the capital and factory wording was false; Proofdelve's existing wording did
  not make that claim.
- Launcher sweep recorded on `fortkit-6xjy.1`: the exact false sentence occurs
  only in the template and capital mayor launchers. `forge.sh`, `warden.sh`,
  `researcher.sh`, and their template copies are clean.
- `CI=1 fort/scripts/verify.sh --no-emit` passed: type checks, Biome, 19 test
  files (199 passed, 40 skipped), template rendering, and ShellCheck.

## Next actions

1. Warden reviews the path-scoped commit.
2. Mayor merges and closes `fortkit-6xjy.1` after review.
3. The Overseer invokes the Regent for `fortkit-6xjy.2`; the live
   `fort/scripts/mayor.sh` remains intentionally unchanged until then.

## Open risks / questions

- Bash command-glob deny enforcement under the flag remains unmeasured. The
  new comment and advisory correction deliberately make no claim about it.

## Failed attempts

- The initial verifier run stopped at `tsc: command not found` because this
  worktree lacked Node dependencies. `npm ci` restored the lockfile-defined
  dependencies and the rerun passed.
