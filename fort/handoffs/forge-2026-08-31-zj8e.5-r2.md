# Handoff: Forge 2026-08-31T14:56:55-08:00
Model: gpt-5.6

## State of work

- `fortkit-zj8e.5` remains in progress for Warden review and Mayor closure.
  This round fixes the factory artifacts identified in the round-one review.

## Verified facts

- `templates/scripts/merge-event-check.sh` defaults its audit lower bound to
  the rendered founding date (`{{DATE}}`), not the epoch. A founded fort
  therefore ignores pre-founding merges unless `MERGE_EVENT_SINCE` or
  `--since` deliberately overrides the checkpoint.
- `templates/scripts/quiescent.sh` derives its default worktree root as
  `${main_root}-worktrees`, matching `templates/fort/scripts/forge.sh` for an
  arbitrary founded repository path. Its generic event override is now
  `FORT_EVENTS_DIR`; capital-specific `FORTKIT_` overrides do not travel.
- The template digest queries `bd human list --status=open,in_progress --json`.
  I ran that exact command against this Beads workspace: it exited 0 and
  returned valid JSON (`null` when no matching queue entries exist). The
  rendered output drops the constant `human` tag, and its protocol reference
  is the founded-fort path `fort/seats/mayor.md`.
- The template verifier now ShellChecks `scripts/*.sh` as well as the fort
  scripts, covering the factory artifacts installed at founding.
- `CI=1 fort/scripts/verify.sh --no-emit` passed: TypeScript and browser
  typechecks, Biome (72 files), 18 test files / 222 tests, ShellCheck, and
  template rendering. Template rendering linted 14 scripts in three passes;
  its positive control went red as required.
- The Stop-hook stanza remains absent from
  `templates/config/settings-permissions.json`. `bin/fort-init` remains
  untouched, so the required installation ordering is preserved.

## Next actions

1. Regent: add explicit, overwrite-refusing render/copy lines in
   `bin/fort-init` for these template artifacts, marking each executable:
   - `templates/scripts/digest.sh` → `scripts/digest.sh`
   - `templates/scripts/quiescent.sh` → `scripts/quiescent.sh`
   - `templates/scripts/digest-hook.sh` → `scripts/digest-hook.sh`
   - `templates/scripts/merge-event-check.sh` →
     `scripts/merge-event-check.sh`
2. Only after those lines land, add the template Stop-hook stanza in its
   separately ordered lane.
3. Warden review this round, then Mayor review and closure. The Forge must not
   close the bead.

## Open risks / questions

- The factory artifacts cannot reach a founded fort until the Regent changes
  kernel-read-only `bin/fort-init`; the guarded merge-event stage remains
  intentionally inactive before that installation step.
- The template retains the deliberate SO13 divergence: Manyhalls keeps its
  charter-specific gate labels, while new forts use Beads' generic `human`
  queue. No cross-fort change was attempted.

## Failed attempts

- None this round. The prior port's epoch checkpoint and `$HOME/dev` worktree
  derivation were corrected rather than retained as compatibility fallbacks.
