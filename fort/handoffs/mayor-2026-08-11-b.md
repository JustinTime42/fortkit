# Handoff: mayor 2026-08-11T (session D)
Model: Claude Opus 5 (1M context) — `claude-opus-5[1m]`

**Attribution caveat, and it is the day's own finding turned on itself:** that
Model line is SELF-REPORTED. `fort/scripts/mayor.sh` pins no model — measured,
`grep -c model` returns 0 — so no launcher record exists to stamp it from.
Filed as `fortkit-ph4g`. Read this line as the session's own word, grounded in
its harness prompt, not as a launcher-derived fact.

## State of work

**Main is GREEN (153 tests) and PUSHED.** 23 commits carrying the whole day.

**Project A (memory parity, `fortkit-xgul`) is half landed.** The Overseer
approved memory-parity-before-Researcher-parity, and the reason is a security
argument rather than a tidiness one: `docs/specs/researcher-seat.md` §8 names
origin-tagged memory as one of three threat-2 mitigations, and a flat
`remember.md` has no `origin` field.

- **A1 done** — `docs/specs/memory.md` §8.5, the cross-fort migration spec.
  Seven decisions, on the bead.
- **A2 + A3 done** in one Regent edict (`0734b27`), gate-1 signed both halves.
  `fort-init` now founds forts ON the ledger. Ilva: *"origin-tagging now reaches
  every founded fort at birth… it is the best thing in this diff."*
- **A4 / A5 are the front of the queue and now unblocked.** Migrate Proofdelve
  (`/home/justin/dev/ForgeOs`) and Farlantern (`/home/justin/dev/longburn`).
- **A6, A7** open behind them.

**Four beads merged and closed today beyond Project A:** `vhk.5.5` (probe skip
disclosure + `--strict`), `vhk.9` (template office-id guard), `88u.16` (per-seat
core budget), `8cv6` (warden.sh reviewing the wrong tree). 32 beads closed in
total; 9 of those were drift beads superseded under `vhk.7`.

**Per-seat core replaced the proposed relevance-router.** The cap was always a
property of one session's context, not of the ledger. `seats: [all]` at core
tier is now the expensive declaration and the lint reports it as a shared floor.
The router is a v2 candidate with a named SO11 trigger, recorded so it is not
lost.

## Verified facts

Everything below was re-run or re-read by the Mayor, not taken from a handoff.

- `verify.sh` on merged main: **153 tests**, 14 files, exit 0. Count moved
  145→150→152→153 across the day, monotonic.
- Probe on merged main: `19 pass, 0 fail, 0 skip`, exit 0.
- `--strict` on the same probe in a worktree: `17 pass, 0 fail, 2 skip`, **exit 1**.
- `origin/main` == `HEAD` after the push.
- **The 9 duplicate drift beads were all Researcher-seat artifacts** — pulled by
  actual classification, not keyword. Superseded under `vhk.7`, per SO7.
- **`report_skip` is unreachable when the probe is sourced.** Measured:
  `report_skip: command not found`, `rc=0` — a sourced skip is silently lost and
  reads as a pass. Same defect Ilva reached by a different route.
- **The elder forts' `remember.md` are 52 and 46 LINES**, not thousands. The
  21KB/17.7KB figures are long bullets. This corrects a framing this seat used
  earlier in the day that made the migration sound far larger than it is.
- **Both elder forts' `emit.sh` parse `getopts "a:s:t:p:T:"`** — identical to
  fortkit's. Closes Warden `w1ew` finding 10, which her mask prevented her
  checking.
- **`consolidate-memory.mjs` filters on `status` and `tier` only, no scope.** So
  per-seat delivery does NOT exist yet; recorded as an explicit unmet
  precondition in spec §4.2.
- **`grep -c model fort/scripts/mayor.sh` = 0.** The Mayor's launcher pins nothing.
- Model of record for the A2/A3 edict is **Claude Opus 4.8** per the commit
  trailer; prose records saying Opus 5 are wrong. Correction appended to
  `xgul.1`. The trailer is a transcription of a harness-supplied string; prose is
  self-belief. The trailer also *varied* between edicts (4.8 → Opus 5), which is
  evidence it tracks something real.

## Next actions

1. **A4 / A5 — the elder-fort migrations.** Spec of record is
   `docs/specs/memory.md` §8.5 including its four-part verification. **Scope
   addition both beads now carry: the migration must ALSO install
   `consolidate-memory.mjs` and `memory-lint.mjs` into the receiving fort** —
   A2 ships them to *newly founded* forts only.
   **Recommended route: a bead in each fort, run by its own Mayor and reviewed
   by its own Warden**, not a fifth Regent edict. Covenant §1 reserves the
   Regent for what a fort *cannot* do; this is ordinary in-settlement work.
   Needs the Overseer to launch Marrek (Proofdelve) and Vardis (Farlantern).
2. **Gate-1 queue for the Overseer**, new today: `w1ew`, `ph4g`, `ztzs`,
   `xtlq`, `8cv6`-successors. `ztzs` is the one to read first.
3. **`fortkit-ztzs` (P1)** — `bin/regent` and `bin/civ-index` still read
   `fort/remember.md`. The Regent's cross-fort briefing has been reading
   Manyhalls' *stub* since the 88u migration, and it gets emptier as Project A
   succeeds.
4. `xgul.8`, `jd3g`, `f8ol`, `vukq` — Warden follow-ups, all filed with evidence.
5. `or2.9` stays OPEN for the elder forts' own Wardens.

## Open risks / questions

- **Records law question put to the Overseer and unanswered:** SO7 corrections
  land in bead `notes`; Warden review habit reads `comments`. Both ship in the
  export. A correction placed where the reviewing seat does not look does not do
  its job. Mayor's instinct is comments-for-corrections, notes-for-working-state.
- **`fortkit-vukq`** — the per-seat lint has holes in both directions: a core
  fact with no parseable `seats:` is counted against nobody, and superseded core
  facts are still charged. The count is wrong two ways and the test would not
  notice.
- **Three consecutive Regent cycles had announcement defects** (late `begun`
  ×2, missing `ended` ×3-forts). Cycle 13 made it structural — the launcher now
  emits both halves — so the next cycle is the test of that fix.
- **179 lines landed in `bin/regent` with zero automated coverage** (`jd3g`).
- The `.claude/proven-config.json` ruflo residue is still untracked and still
  evidence for `fortkit-agf`.

## Failed attempts

- **The Mayor left a spec edit uncommitted and dispatched the Forge anyway.**
  The Forge branched from committed main, correctly saw stale wording, and wrote
  its own version of the same section. This is Ilva's `f8ol` finding 5 in the
  opposite direction, **three hours after she filed it** — she read an
  uncommitted charter amendment as law; the Forge read a committed spec the
  Mayor had already superseded. Resolution: preserved the diff, reverted, merged
  clean, re-applied only the doctrine paragraphs. **The lesson is not "remember
  to commit"** — it is that `f8ol` now covers DISPATCH as well as REVIEW, so
  `forge.sh` warns and names dirty paths.
- **The Mayor under-scoped the `vhk.5.5` dispatch**, enumerating findings 1–5
  when the bead had 7. Ilva caught it; under SO9 the bead would have closed and
  findings 6 and 7 would have vanished. Filed forward as `vhk.5.6`. **Read every
  finding before dispatching, and count them.**
- **The Mayor launched two Warden reviews with the candidate-dir argument
  omitted**, so both reviewed merged main instead of the candidate. Ilva caught
  it about her own session. Root cause was a silent default plus a launcher
  briefing that asserted something the seat could not check — `8cv6`, now fixed
  and proven: she later ran the full suite in-mask, the first fortkit review on
  record to reproduce the suite rather than take it on faith.
- **The Mayor overstated a cross-fort risk**, telling the Overseer Farlantern's
  Warden was "reviewing without executable verifiers right now." Measured: no
  `vitest.config.*` there, so the gap is latent. Ilva had said *latent* and the
  Mayor sharpened past her evidence. Corrected on `or2.9`, and the edict brief
  was written to forbid porting to Proofdelve on the same kind of assumption.
- **The Mayor was overruled by the Warden on `vhk.9` and she was right.** The
  Mayor kept a template change pointing at `fort/memory/current.md` and gated
  propagation on `uhvt`; Ilva blocked, because before the change the path existed
  everywhere the template is *used* and after it, nowhere. Root cause: the bead
  conflated the live tree with the template tree, and the Mayor caught that
  conflation in the actor-id half and kept it in the memory half. Same error
  twice from one bead. Reverted.
- **Two dependencies wired backwards** (`8cv6`→`hn2`/`5mw`, `w1ew`→`vn8`),
  making P1 beads falsely blocked. Both removed and recorded as prose relations.
