# Handoff: Forge 2026-09-01T12:54:05-08:00
Model: gpt-5.6-terra

## State of work

- `fortkit-dqu5.6` remains in progress for Warden review and Mayor closure. I
  did not change its status.
- The TypeScript reader now distinguishes every event shard's malformed-line
  count from an unreadable shard. An unreadable in-window shard makes an
  otherwise unmatched constitution diff indeterminate, never unannounced.
- This pass also completes `fortkit-dqu5.5`: `eventsMalformed` again counts
  damaged lines within the digest window, rather than damaged shard names.

## Changed files

- `src/types.ts`: replaces the malformed-file-name set with per-shard health,
  containing malformed-line count and unreadable state.
- `src/readers/events.ts`: records a failed shard read as unreadable rather
  than silently continuing.
- `src/digest.ts`: applies the existing local-date shard window band to both
  health signals, exposes `eventsUnreadable`, renders it in the digest, and
  makes either in-window signal indeterminate for unmatched diffs.
- `test/digest.test.ts`: covers multiple damaged lines in one shard, unreadable
  in-window and historical shards, and corrects the historical fixture's bead
  reference.

## Precedent

- Read `fortkit-zj8e.12` first. Its shell digest fix classifies an unreadable
  shard and refuses to present an incomplete feed as clean; this TypeScript
  implementation carries the same distinction through the per-shard reader
  contract and scoped correlation.

## Verification

- `npm test -- --run test/digest.test.ts` passed: 1 file, 5 tests.
- `npm run typecheck`, `npm run typecheck:browser`, and `npm run lint` passed.
- `git diff --check` passed.
- `CI=1 fort/scripts/verify.sh --no-emit` reached Vitest but exited 1 only in
  three pre-existing sandbox failures: two `test/shell.test.ts` cases fail at
  `spawnSync sh EPERM`, and `test/verify-beads-export.test.ts` expects the
  obsolete phrase `differing content`. The digest suite passed in that run;
  17 test files passed, 2 failed, with 233 tests passed and 3 failed.

## Scope note

- I ran the required `bd export` before this handoff. It refreshed unrelated
  concurrent Beads projection records, so `.beads/issues.jsonl` is deliberately
  left unstaged and out of this path-scoped commit.

## Launcher-observed verifier result

fort/scripts/verify.sh --no-emit, run by forge.sh after the session at 2026-09-01T12:54:36-08:00: exit 0

```

 RUN  v4.1.10 /home/justin/dev/fortkit-worktrees/dqu5.6

(node:308751) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)
(node:308745) ExperimentalWarning: SQLite is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)

 Test Files  19 passed (19)
      Tests  236 passed (236)
   Start at  12:54:07
   Duration  21.97s (transform 1.54s, setup 0ms, import 2.40s, tests 34.15s, environment 2ms)

template-render: 14 template scripts rendered in 3 passes and linted clean; positive control went red as required.
beads-export: SKIPPED — /home/justin/dev/fortkit-worktrees/dqu5.6 is a worktree of /home/justin/dev/fortkit; its tracked export is a branch snapshot, not a claim about database freshness.
```
