# Handoff: mayor 2026-08-11T22:23:41-08:00 (session F)
Model: claude-opus-5[1m] — SELF-REPORTED. `fort/scripts/mayor.sh` still pins no
model (`fortkit-ph4g`, now carried by E3), so no launcher record exists to stamp
this from. Session E flagged the same thing; nothing has changed.

**Full ISO instant in the header on purpose.** `fortkit-ybg7` is still live:
`consolidate-memory.mjs` sorts unparseable stamps to epoch 0, so a bare
`2026-08-11T` silently becomes the oldest record in the fort. Keep writing full
instants until ybg7 lands.

---

## State of work

This session was a **planning session**, not an implementation one. The Overseer
asked for the civilization-wide retired-memory-store defect and the rest of the
kernel-RO backlog planned as an efficiently executable set of Regent edicts. That
plan is filed. One piece of it was executed.

### The programme: `fortkit-52vf`, seven children, all filed

| Bead | | Gate |
|---|---|---|
| `52vf` | epic: the kernel-RO repair sweep | gate-1 |
| `52vf.1` | **E1 the read side** — SHIP FIRST | gate-1 |
| `52vf.2` | **E1a** — Mayor's half, **DONE THIS SESSION**, claimed pending review | — |
| `52vf.3` | E2 the mask (`seat-sandbox.sh` ×3 + template) — highest blast radius | gate-1 |
| `52vf.4` | E3 identity and provenance | gate-1 |
| `52vf.5` | E4 warden reliability (`warden.sh` ×3) | gate-1 |
| `52vf.6` | E5 in-mask launch refusal — depends on E3 | gate-1 |
| `52vf.7` | E6 profiles | gate-1 |

Grouped **by file × fort, not by bead**, because the two costs that dominate are
reopening the same load-bearing file in separate sittings and verification sweeps
that need real seats launched while the Overseer is present. Sittings are the
scarce resource. Read the epic for what was deliberately *not* bundled and why.

Every bead now carried by an edict got a cross-reference comment naming which one
(31 beads). They stay OPEN and close FROM that work rather than being superseded,
because each is still the accurate record of its own surface.

### Three beads filed outside the programme

- **`fortkit-nqoy`** — `civ/remember.md` (568 lines) has not migrated to the facts
  ledger. The civilization layer is the last flat store and the Regent is the seat
  reading it. Held by no bead before this one.
- **`fortkit-gohb`** — carries the Overseer's SO11 ruling on `fortkit-e4q`: measure
  the `/proc` sibling-read exposure (existence and byte count **only**, never
  content), then record it as a charter accepted residual and supersede e4q.
- **`fortkit-in8w`** — HOLD + rescan for the 29 `Drift:` beads.

Plus **`fortkit-0s4a`** (P3), from this session's own friction: `verify.sh` green
output does not enumerate which steps ran.

### Four Overseer decisions, recorded verbatim on the beads they decide

Each records that the text is the *option selected*, not words typed, and gives
his typed instruction alongside it. All four are on both the deciding bead and the
bead that carries out the decision.

1. **Sequencing** — ship E1 first; accept one cycle of a stale `Latest handoffs`
   line. On `52vf.1` and `ybg7`.
2. **`6ovg`** — make the `verify.sh` carve-out actually work. This makes E2 a mask
   *design* change, not a line edit. On `6ovg` and `52vf.3`.
3. **`civ/remember.md`** — file it. On `nqoy`.
4. **`e4q`** — does NOT clear SO11; record as accepted residual. On `e4q` and `gohb`.

### E1a executed

`fort/seats/warden.md` and `fort/seats/researcher.md` gained the session-start
memory instruction they both lacked. Cycle-7 prose gate honoured: Overseer approval
recorded verbatim on `52vf.2`, `charter.amended` emitted 22:22:16.

## Verified facts

Everything here was run or read by this seat this session, not carried forward.

- **The ten seat prompts are real, and I counted them myself** rather than trusting
  `08ny`: fortkit 4 (`mayor.sh:25`, `forge.sh:81`, `warden.sh:111`,
  `researcher.sh:29`), ForgeOs 3 (`:25`, `:214`, `:42`), longburn 3 (`:32`,
  `:100`, `:123`).
- **NEW: four of the ten seats had no fallback.** `fort/seats/warden.md` carries
  **zero** session-start memory instructions in **all three forts**, and Manyhalls'
  `researcher.md` carried none. `mayor.md` and `forge.md` are correct everywhere.
  So `08ny`'s "this session followed the seat file and reached the ledger, and that
  was luck" understates it: for the three Wardens and the Researcher there was no
  luck available. The launcher prompt was their only memory instruction. Every
  Warden review in this civilization has been conducted that way.
- **NEW: `civ/CIVILIZATION.md:108` carries the retired path too**, and no bead named
  it. It is `bin/civ-index`'s checked-in output, so the generator fix must regenerate
  the handbook in the same commit. Folded into E1.
- **NEW: `civ/remember.md` is 568 lines, flat, unmigrated**; `civ/` has no `memory/`
  directory; `bd search 'civ/remember'` returned nothing.
- **`fortkit-6ovg` reproduced a third time, by this seat**: `test -w
  fort/scripts/verify.sh` → TRUE, open-for-append → OK, `touch
  fort/scripts/.cycle7-probe-emrith` → **Read-only file system**. Three forts, three
  Mayors, three independent measurements.
- **THE EFFICIENCY FINDING: `ybg7`, `ij8j`, `xgul.7.1` and `xgul.8` are NOT Regent
  work.** They live in repo `scripts/` and `templates/`, neither of which is in the
  cycle-7 kernel-RO set. Manyhalls' own Forge can do them with no Overseer present.
  Lane-assignment comment added to all four. Only carrying `ybg7`'s fix into the
  elder forts needs an unmasked launch.
- **Templates are already clean.** `grep -rn 'fort/remember.md' templates/` returns
  one hit, `templates/fort/memory/memory-lint.mjs:109`, which is the retired-reference
  guard and **needs** the string. Do not "fix" it.
- **`warden.sh:141` binds the main checkout as `extra_ro` UNCONDITIONALLY**, verified
  before writing the root-absolute clause into `warden.md`. An instruction to read a
  path she cannot reach would have been worse than no instruction.
- **Verifier green, exit 0, twice**: 14 files / 153 tests. ShellCheck genuinely runs
  and genuinely passes (`verify.sh:67`; exit 0 on the identical invocation run by
  hand) — `run_step` is simply silent on success, which is `fortkit-0s4a`.
- **`origin/main` is one commit BEHIND `HEAD`** and was so at session start: `HEAD`
  `9206f75`, `origin/main` `753ce12`. Not pushed by this session.
- **The distilled view was stale at session start**, naming `mayor-2026-08-10.md` as
  latest while sessions C, D and E existed. Measured, not excused (see Failed
  attempts in session E's handoff — this seat did not repeat that error).

## Next actions (ordered)

1. **E1 (`52vf.1`) as the first Regent sitting.** Its verification must launch one
   seat per fort and confirm the string **in the running process table**, and must
   confirm each fort's seat files and launchers now agree.
2. **Dispatch the Forge lane now — it needs nobody present**: `ybg7`, `ij8j`,
   `xgul.7.1`, `xgul.8`. `ybg7` unblocks `88u.6` and `fci.6` (dependencies encoded
   this session).
3. **Review `52vf.2`** so it can close. Two-line prose change to two seat files;
   verifiers green. SO9 wants a review and this seat did not spend a frontier one
   unasked.
4. **E2 (`52vf.3`)** as its own sitting, with `6ovg` as a mechanism fix. Verification
   must prove Edit, `sed -i` and a `git checkout` that replaces `verify.sh` all work
   — **not** that `test -w` returns TRUE, which is the symptom that hid this for a cycle.
5. **E3, then E5** (E5 depends on E3 for the seat-named marker; E5 lands when no
   seat is working in any fort, because its guard has already refused a fort's own
   Mayor mid-mill).
6. **E4, E6.** In E6, `zlh` waits on `hn2`'s two-session verification: do not carry
   an unproven profile shape into the factory.
7. `gohb`, `nqoy`, then `or2.8`/`pp1k`/`bpuv` → `in8w` (the drift rescan).
8. **The ruflo purge** (`agf`) — script written and Overseer-approved, unrun, must be
   run unmasked. `~/.claude/brain/` explicitly untouched.
9. `or2.2` (backport rubric + drift watcher spec) remains the highest-leverage
   non-memory Mayor work, still untouched across four sessions.

## Open risks / questions

- **`origin/main` is one commit behind and this session added a second.** Push is a
  prose gate: the Mayor asks every time. Asked at the end of this session; record
  what he answered.
- **`src/cli.ts` is still uncommitted on purpose** (mode bit only, `zya9`). Not
  staged by this session.
- **`assets/trial/*`** — 13 PNGs plus a provenance manifest, still untracked, still
  unclaimed, presumed Limner trial output for `fortkit-zcb`. Fourth session running.
- **The 29 drift beads are now held behind `in8w`.** If someone works them anyway
  the work is wasted, not wrong. The hold is recorded on `or2` rather than on 29
  beads; that is cheaper but also less visible, and a seat reading only a drift bead
  will not see it.
- **E2 is the sitting that can break everything.** All three live `seat-sandbox.sh`
  copies have already diverged (13528 / 14004 / 13652 bytes). It is three edits to
  three files, not one edit applied three times. Diff before editing.
- **`bd stats` and `current.md` disagreed at session start** on which beads are
  in_progress (`current.md` listed `6jf`, `hn2`, `r6x.7`; `bd` reported 0 in progress
  before this session claimed one). Not chased. Possibly another face of `ybg7`'s
  staleness, possibly separate. Unmeasured, and recorded as unmeasured.

## Failed attempts

- **`bd` cannot be run from outside the repo.** The first filing attempt `cd`'d to
  the scratchpad to write description files and every `bd create` failed with "no
  beads database found". Fix: stay in the repo, reference scratchpad files by
  absolute path. Cost one round trip.
- **`bd dep add <task> --depends-on <epic>` is rejected**: "tasks can only block
  other tasks, not epics." The drift rescan had to depend on the six children
  individually rather than on `52vf`. Worth knowing before designing a dependency
  graph that leans on epics as barriers.
- **Assumed a green verifier proved ShellCheck ran.** It does, but the log does not
  say so, and confirming it took three extra probes. That friction is now
  `fortkit-0s4a` rather than a shrug — the fort has too many controls that report
  PASS while proving nothing to let another one pass unrecorded.
