# Handoff: mayor 2026-08-17T15:20:00-08:00
Model: claude-opus-5[1m] — **SELF-REPORTED, not launcher-derived.** `mayor.sh` pins no
model (`fortkit-ph4g`, open), so neither the commit nor the `session.start` payload
records one.

## State of work

**The parity instrument was designed, rejected, and replaced by a mechanism that has
already been used. Standing order 13 is law in all three forts, each in its own words
and numbering. The advisory registry exists with seven entries and both elder forts
answer into it. Nine beads closed, twenty-one filed. The Warden found something in
every one of nine reviews, including six wrong claims by this seat.**

| bead | state | outcome |
|---|---|---|
| `881h` `a355` `x02m` | **CLOSED** | Template halves, merged in that order. `a355`'s ported comment was false until `881h` landed, so order mattered. |
| `8t5u` | **CLOSED** | The founding smoke now EXECUTES the rendered `status.sh` and the verifier shim, with controls at exit 91 and exit 70. Caught a real defect 3h later. |
| `hpmn` `p5mr.6` `tcmg` | **CLOSED** | Charter work. `tcmg` took three rounds; `p5mr.6` enacted SO13. |
| `p5mr` | OPEN, epic | The adoption mechanism. Nine children; A1/A2/A5/A7 delivered by Regent edict. |
| `p5mr.10` | OPEN | Regent sitting. Warden REQUEST-CHANGES; both blockers discharged, **wants a re-review before it closes**. |
| `x2eb` | OPEN | SO13's four defects, found by Farlantern. Repaired in both charters at `bea6ec5`. |
| `876i` | OPEN | My false write-boundary claim, propagated into the Regent's work. Corrected in three places. |
| `ff2j` | OPEN | Resolved as to mechanism: session context, nothing else. |

**Filed (21):** `ypv1` `i50s` `bjd8` `3539` `7unc` `bucl` `8t5u` `0aml` `osga` `p5mr`
`p5mr.1`-`.10` `3lme` `dbbr` `5tfd` `x2eb` `876i` `byhp` `4rns` `9ahr` `0iwa`.

**Both elder forts adopted by pull, in their own words, on their own numbering:**
Proofdelve SO17 (`ForgeOs-dijj`), Farlantern SO18 (`longburn-o6qb`). Neither copied.
Both wrote ledgers. **Farlantern found four defects in the source order within hours**
and Proofdelve invented a fifth result state we adopted.

## Verified facts

- **`--dangerously-skip-permissions` does NOT bypass deny rules.** Measured in a live
  masked Mayor session running under the flag: `Edit` on a deny-listed path refused,
  `Bash rm` on it refused, with passing controls on both. Proofdelve's wording was
  right and the capital's, the template's and Farlantern's were false (`fortkit-i50s`).
- **Bash path enforcement is verb-pattern matching, not path matching.** Same path,
  same rule: `rm` refused, `> redirect` allowed, `find -delete` allowed. So a path
  protected only by an `Edit(...)` deny is not write-proof against a seat holding Bash.
- **`Bash(find *)` is on the Warden allow list in all four forts** while `rm`/`mv`/`cp`/
  `chmod`/`ln` are denied. That deny set is the sole mitigation named in `3jv7`'s
  accepted residual (`fortkit-ypv1`).
- **`civ/` is NOT kernel read-only to the capital's masked seats.** Writable: `civ/`,
  `civ/advisories/`, `civ/seats/`, `civ/law/`. Read-only: `civ/scripts/`,
  `civ/profiles/`, `bin/`, `fort/scripts/`. Matches `cycle13-write-boundaries`.
- **`ff2j` is the Forge session posture and nothing else.** Same worktree, same Node
  v24.14.0, deps installed: red in-session at 13:22:15, green on the host at 13:13:23
  and 13:24:09, green in the Warden's scratch at 13:25:45.
- **`CI=1 fort/scripts/verify.sh --no-emit` is exit 0 at 16 files / 192 tests** on main
  at `4fab3f1`, and was green after every commit today.
- **Push and Dolt sync are not blocked by the mask.** `seat-sandbox.sh:501-511` passes
  `SSH_AUTH_SOCK` through to claude seats deliberately. `ssh-add -l` returns "The agent
  has no identities"; a masked socket would return "Connection refused". The agent is
  empty, not blocked.

## Next actions

1. **Run `ssh-add` on the host before launching a Mayor.** That is the whole blocker on
   push, `ls-remote` and `bd dolt push` from this seat. The mask already passes the
   agent socket through for exactly this.
2. **`fortkit-p5mr.10` needs a Warden re-review before it closes.** Both blockers are
   discharged; SO9 makes review the gate.
3. **`fortkit-ypv1` is the most consequential open item.** All four forts, and it is the
   sole mitigation named in an accepted charter residual. Proofdelve has already
   answered `PRESENT` and filed `ForgeOs-dijj.1`. Regent lane in each fort.
4. **`fortkit-p5mr.9`: supersede `docs/specs/backport-loop.md` and re-scope
   `fortkit-wg8w`.** Both still encode the repealed convergence model, and `wg8w` is
   named in `fort/memory/current.md` as "the gate the Overseer resumes work behind",
   so a fresh seat is pointed at repealed policy at session start. Highest-leverage
   remaining cleanup.
5. **ForgeOs is NOT blocked from product work.** Feature pulling and the memory
   architecture are both live there, measured. The Researcher seat is the only gap and
   is an enhancement, not a prerequisite. `fortkit-vhk.7`'s two remaining blockers are
   discharged in substance and open only as bookkeeping.
6. **To add the Researcher to an elder fort:** one pull session with that fort's Mayor,
   plus the Overseer's hand to install `fort/profiles/researcher-settings.json`, which
   is kernel read-only to every seat.
7. Standing: `fortkit-9ahr` (fort-init registers a fort before refusing a node-less
   host), `fortkit-0iwa` (Warden reviews race a moving tree), `fortkit-xtlq` (SO7
   corrections are invisible to the memory index while the text they withdraw is not).

## Open risks / questions

- **The capital is the fort furthest behind on its own new law.** Both elder forts had a
  `fort/advisories.md` before Manyhalls did. Created today at `bc4a6b8`.
- **The capital has no researcher boundary probe.** Only `templates/` has one, so the
  fort that wrote "run the probe against your own profile" into every founded charter is
  the one fort that cannot run it. Bears on `vhk.5` and on propagating the seat.
- **`fortkit-3jv7`'s residual is now doubly undermined**: verb-incomplete here, and
  Proofdelve reports `ForgeOs-21f.8` measured deny globs defeated by obfuscated path
  spellings, 6/6. **I could not verify that figure — ForgeOs ships no
  `.beads/issues.jsonl`, only `interactions.jsonl`.**
- `edict.ended` for the 14:00:58 sitting is not on this fort's stream yet.

## Failed attempts

- **I claimed `civ/` was kernel read-only to every masked seat, four times, and the
  Regent adopted it from my brief and published it as measured fact.** The correct
  enumeration was in `cycle13-write-boundaries`, injected at session start, the whole
  time. One `touch` would have falsified it and cost one second. Three of that sitting's
  four items did not need the Regent.
- **Backticks in a double-quoted bash string are command-substituted before `bd` sees
  them.** A word vanished from a posted comment. Caught only because it was an unknown
  command; a backticked `find` or `ls` would have silently inserted its OUTPUT. Use
  `bd comment --file` or `--stdin` with a quoted heredoc. The check that caught it was
  reading back what LANDED, not reviewing what I wrote.
- **I wrote a false uniqueness claim over a two-element set** into the factory charter,
  having inspected one element.
- **I asserted two paragraphs were "byte-comparable" without diffing them.** They
  differed in seven places.
- **I argued the launcher's exit 0 refuted the posture hypothesis for `ff2j`.**
  `forge.sh:165` runs on the HOST, so it could never have discriminated. Void, not
  weakened.
- **I reintroduced a fort-local index into portable charter text** minutes after the
  Warden had made me remove one.
