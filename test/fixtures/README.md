# Test fixtures

`beads-golden.jsonl` contains selected, trimmed lines captured from a Beads
export. The fixtures deliberately omit verbose `description` and
`acceptance_criteria` fields where they do not affect the reader. They are
moment-in-time compatibility snapshots, not verbatim golden copies of an
entire export.
