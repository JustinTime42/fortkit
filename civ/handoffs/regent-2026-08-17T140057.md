# Regent handoff — edict of 2026-08-17: the advisory mechanism, and one visit to the factory (fortkit-p5mr.10)

Model: opus, as **Calder Sealbroken** (they/them). Overseer present throughout.
Twenty-second edict, and the second of the day. Previous:
`regent-2026-08-17T082548.md` (B2R2, the Band 2 repair sitting).

**The launcher announced correctly again**: `edict.begun` at 14:00:58, actor
`calder`, seat `regent`, one per fort. The closing half is this launcher's to
write, from `civ/handoffs/.edict-2026-08-17T140057`.

**The standing item from the last two sittings held again**: the previous
sitting's `edict.ended` lands after its own handoff commit by construction, so
it always belongs to the next sitting's staging. Expect it again.

## The edict

`docs/proposals/2026-08-17-advisory-mechanism-edict.md`, drafted by Emrith
Cairnwright, bead of record `fortkit-p5mr.10`. Four items, every one in `civ/`
or `bin/`, both kernel read-only to every masked seat in the capital including
the Forge — so no seat of this fort could do any of it.

Commit **`5cea344`**, path-scoped, not pushed.

| item | beads | outcome |
|---|---|---|
| 1 the registry | `p5mr.1` | `civ/advisories/` + schema README. **Done.** |
| 2 the seeds | `p5mr.5` | ADV-0001..0006, every check run. **Done.** |
| 3 the covenant | `p5mr.7`, `ugr.7` | §12, §8.7, §8.8, §10 pointer. **Done.** |
| 4a seat-lint | `bucl`, `e69p` | Both halves, one commit, founding-measured. **Done.** |
| 4b ledger | `p5mr.2` | Factory half. **Capital's own ledger still Mayor lane.** |
| 4c founding failure | `byhp` | Regent half. **Smoke assertion still Forge lane.** |

**No bead closed.** SO9 makes review the gate and Ilva has seen none of it; the
brief said so in terms and `fortkit-wg8w.2` set the precedent.

## What was verified, with commands rather than adjectives

**Elder Mayors can read the capital's `civ/` — measured, not inferred.** The
whole epic rested on this and nobody had run it from the far side. Each elder
fort's own mask, built from that fort's own `seat-sandbox.sh` exactly as its
`mayor.sh` builds it, no session launched:

    HOST     /home/justin/dev/fortkit/civ/covenant.md   16535 bytes
    ForgeOs  mask: READ 16535   WRITE into civ/ -> Read-only file system (exit 1)
    longburn mask: READ 16535   WRITE into civ/ -> Read-only file system (exit 1)

Host byte count asserted first, so an empty read could not pass as a good one;
the write probe is the control proving the mask was real. No canary left.

**The factory, by founding throwaway forts** (`FORT_REGISTRY` at scratch paths;
live registry re-read afterwards and still exactly three forts):

| factory | condition | exit |
|---|---|---|
| pre-edit | consolidate-memory crashes | **0**, fort founded, `current.md` ABSENT |
| post-edit | healthy | 0, all three artifacts present |
| post-edit | consolidate crashes (3) | **1**, names the status and the 0-byte view |
| post-edit | consolidate exits 0, writes 7 bytes | **1** |
| post-edit | `node` absent from PATH | **1** |

**The founded fort's own verifier**, three ways:

| condition | seat-lint | verifier |
|---|---|---|
| no `FORT_REGISTRY` | rules 2+3 announced SKIP | exit 0 |
| `fort_name: null` | rule 3 EXEMPT, announced | exit 0 |
| `fort_name` set, placeholders surviving | 4 seat files FAIL | **exit 1** |

The third row is `e69p`'s acceptance in the failing direction; without it a
green verifier would only prove the step does not crash.

**Capital verifier**: `CI=1 fort/scripts/verify.sh --no-emit` → exit 0, 192
tests, 16 files, template-render 10 scripts clean with its positive control
red. `vitest run test/shell.test.ts --reporter=verbose` shows all five
`fort-init` tests EXECUTING (founded-fort case 3758ms) rather than skipped —
checked because a skipped smoke would have made the green meaningless.

**`shellcheck -x`** clean on every candidate before install.

**Install lane** for `bin/` (Edit-denied even unmasked): python3 patcher with
anchor-count asserts → scratch candidate → shellcheck → `os.replace` gated on a
pre-image sha256 and a `filecmp` byte comparison. Three installs, no Overseer
round trip.

## What failed — mine, in order

1. **I founded a throwaway fort without `FORT_REGISTRY` and registered it in
   the live `~/.claude/civilization.json`.** The trap is on three of the beads
   I was working from AND in my own briefing. Caught in under a minute by
   reading the registry back; entry removed; pre-image sha256
   `f776d1e5…`; `diff` confirms only that entry changed. Incident on the
   capital's stream at 14:24:52.
2. **A `path:line` in my own `ugr.7` ruling was wrong by four lines**, written
   from the briefing rather than the file. SO7 correction appended. **The first
   verification attempt also failed**: a multi-word `grep` returned zero against
   a hard-wrapped prose file, which reads exactly like "the clause is absent."
3. **My `byhp` repair turned a warning nobody reads into a SILENT DEATH**, then
   its byte check **died under `pipefail`** before it could print the refusal it
   existed to print. Both caught by the crash control, both on that bead.
4. **A brace literal in a comment I wrote failed the template-render lint** —
   fifth sighting of that class here, inside a comment about placeholders.
5. **My first node-free control still resolved `node`** (there is a
   `/usr/bin/node` distinct from the nvm one), so it could not have
   discriminated. Rebuilt as a shim directory.

None was caught by care or by knowing the rule. All by a control or a re-read.

## What the next Regent should know

**THE LAW CAN MOVE UNDER YOU MID-SITTING, AND `git status` IS THE DETECTOR.**
The Mayor amended standing order 13 in both charters while I was implementing
it (`fortkit-x2eb`, from Vardis Slowfathom's findings in Farlantern). Two
changes reached my artifacts: a fifth result state `unresolved`, and the
correction that **"any fort may raise" is a false promise — any fort may
ORIGINATE, only the capital may FILE**, via an `advisory-candidate` bead that
is not a raised advisory until it appears in the registry. My first drafts of
the README and of covenant 12.3 carried the exact false promise she had just
struck. I found it because I read `git status` before staging and saw two files
modified that I had not touched. **Path-scoped staging is a correctness control
here and not only hygiene.**

**Do not stage the Mayor's files.** `fort/charter.md` and
`templates/fort/charter.md` are hers and are deliberately uncommitted in this
tree. `.beads/issues.jsonl` likewise left unstaged, because a concurrent
session's in-flight bead work would have ridden into a Regent commit.

**`civ/advisories/` and `civ/covenant.md` are NOT Edit-denied to the Regent.**
Only `civ/profiles/**` and `civ/scripts/**` are. `templates/**` is writable
too; `bin/**` and `fort/scripts/**` still need the scripted lane.

**Covenant sections are addresses.** New material goes in as a new numbered
section appended after 11, or as a new subsection of an existing one (8.7, 8.8
were free). Never renumber: three charters cite `covenant 4.5`/`4.6`, and the
seat files and `civ/remember.md` cite `section 9`, `section 10`, `8.1`, `8.3`,
`gate 6.4`.

**Immediate follow-ups, none of them Regent lane:**

- Mayor: the capital's own `fort/advisories.md`, with rows for ADV-0001..0006.
  Note ADV-0002 is `present` here, wider than `ypv1` assumed.
- Mayor: two drifted `bin/fort-init` citations and an off-by-one in
  `scripts/seat-lint.mjs`, measured and recorded on `fortkit-bucl`.
- Forge: the founding smoke's `current.md` assertion (`byhp`), which collides
  with `fortkit-domm` in the same describe block.
- **Ilva's covenant-4.5 review of all of it**, which is the gate everything
  above is waiting behind.

## Appended at close

**Ilva's review is already running.** `session.start ilva warden — Ilva begins
review of fortkit-p5mr.10 (opus)` at 14:50:15, in this fort's stream. So the
gate named above is live rather than pending, and **the next reader should look
for her verdict before treating anything in this sitting as settled.** If no
`review.verdict` appears against `fortkit-p5mr.10`, that is a dead session and
not an approval — the recorded failure mode, seen many times in all three
forts.

Her session line is deliberately **left unstaged**. It is one half of a pair;
her `session.end` and verdict belong in the same commit as her start, and
splitting a live seat's record across a Regent commit would make it harder to
read, not easier.

**Left in the working tree at close, all deliberate:**

| path | why |
|---|---|
| `fort/charter.md`, `templates/fort/charter.md` | the Mayor's concurrent `fortkit-x2eb` amendment. Hers to commit. |
| `.beads/issues.jsonl` | passive export; a concurrent Mayor session is in this tree and her in-flight bead work would have ridden into a Regent commit. |
| `fort/events/events-2026-08-17.jsonl` | Ilva's live session line, above. |
| `civ/handoffs/.edict-2026-08-17T140057` | launcher scratch, consumed at close. No `.edict-*` file has been tracked in any of the twenty-two sittings; checked, not assumed. |

**The tree was pushed during close-out, not by me.** `main` and `origin/main`
are both at `5a48d69`, which contains both of my commits. Stated that way
rather than as a count, because `git rev-list origin/main..main` measures this
clone's last sync and not what the remote lacks — the recorded scar.

## SO7 CORRECTION, appended before sleeping: THIS SITTING'S LANE PREMISE WAS FALSE

**The line at the head of this handoff — "every item edits `civ/` or `bin/`,
both kernel read-only to every masked seat in the capital" — is FALSE for
`civ/`.** It is left standing above, per standing order 7, and corrected here.

The Overseer caught it during my close-out and filed `fortkit-876i`, with two
commits (`6cc9254`, `5a48d69`) correcting the edict document and the registry
README. **I then measured it myself rather than adopting the correction the way
I had adopted the error**, building the capital's own Mayor mask from its own
`seat-sandbox.sh` and write-probing each tree:

    civ                WRITABLE          civ/scripts        read-only (EROFS)
    civ/advisories     WRITABLE          civ/profiles       read-only (EROFS)
    civ/seats          WRITABLE          bin                read-only (EROFS)
    civ/law            WRITABLE          fort/scripts       read-only (EROFS)

No residue left; the probe removes its own canary and both paths were checked
after.

**So three of this sitting's four items did not need a Regent at all.** The
registry, the seeds and the covenant are all `civ/` and are Mayor-writable in
the capital. **Only item 4 — `bin/fort-init` — genuinely required this seat.**
Least force says that plainly: a Mayor could have done the other three through
her own lane, and the brief's own reasoning for convening a Regent sitting was
wrong about three quarters of its subject.

**How the error travelled, because that is the part worth keeping.** The claim
originated in the Mayor's edict brief. I adopted it, restated it in
`civ/advisories/README.md` and in covenant 12.5 — **and attached my own
elder-fort measurement to it**, so a false clause about the capital was
published wearing the label of a probe that had only ever entered the two elder
forts. That is ADV-0005 exactly, the advisory I wrote in this same sitting: a
measurement reported under a claim it could not have discriminated. The correct
enumeration was in `cycle13-write-boundaries`, a core memory fact injected into
my context at session start and present the entire time.

**Repaired here:** covenant 12.5 now states the real reason (no fort can write
into another fort's tree; the registry is in the capital's tree) and carries the
correction inline, including the practical consequence — **transcribing a
candidate is ordinary Mayor work and needs no Regent sitting.** The README was
already repaired by the Overseer. Corrections appended to `fortkit-p5mr.1`,
`.5`, `.7` and `.10`.

**Uncorrectable and therefore flagged:** commit message `5cea344` asserts the
same false lane premise in git history, where nothing can be appended. Recorded
on `fortkit-876i` so a reader of that message finds the correction.
