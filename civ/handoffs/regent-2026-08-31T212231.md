# Regent handoff — edict of 2026-08-31: fort-init founding integrity (fortkit-2twy)

Model: opus, as **Calder Sealbroken** (they/them). Overseer present throughout.
Twenty-fourth edict. Previous: `regent-2026-08-24T200915.md` (the airlock
feedback-scan mask in Proofdelve). A week's gap; nothing was in flight.

**The launcher announced correctly again**: `edict.begun` at 21:22:3x, actor
`calder`, seat `regent`, one per fort. The closing half is this launcher's to
write from `civ/handoffs/.edict-2026-08-31T212231`, which carries the bead id and
a one-line result.

**The standing item held a fifth time**: the previous sitting's `edict.ended`
lands after its own handoff commit by construction, so it always belongs to the
next sitting's staging. It was already committed before this sitting opened.

## The edict

One instruction: *"read fortkit-2twy and implement that edict."*

`fortkit-2twy` is a single Regent sitting over `bin/fort-init`, grouping
`fortkit-9ahr`, `fortkit-fg7s` and `fortkit-byhp` as one defect wearing three
faces: **the factory mutated the world before it had finished deciding whether it
could succeed.** Under `set -euo pipefail` any late refusal aborts mid-founding,
and the wreckage is a fort that LOOKS founded. `bin/` is kernel read-only to every
masked seat including the Forge, so none of it could be dispatched.

Commit **`04983ae`**, path-scoped, not pushed. Five files: `bin/fort-init`,
`test/shell.test.ts`, the capital's event stream, and the two beads exports.

## What changed

### `bin/fort-init` — a no-mutation preflight, and an adjacent tail

A preflight block now sits between `cd "$REPO"` and the first write (`bd init`).
It carries the git-repo and already-has-a-fort tests that were always there, plus
four things that were not:

| moved or added | from | bead |
|---|---|---|
| `command -v node`, message unchanged | the foot of the script (`:306`) | `fortkit-9ahr` |
| `command -v` for `bd` and `jq` | nowhere — undeclared dependencies | — |
| six pre-existing-artifact refusals | the render sites (`:107`, `:120`, `:131`) | — |
| registry + registry-directory writability probe | nowhere | `fortkit-fg7s` |

At the foot, **the founding emissions and the registry append swapped places and
are adjacent with nothing able to fail between them.** The direction is
`fortkit-fg7s`'s own argument: a fort in `fort/events/` but not in the registry is
recoverable by one `jq` append; a fort in the registry that never announced itself
is silently half-built, and every consumer that reads the registry sees a
settlement that was never finished. The append also grew a catch on both the `jq`
and the `mv`: it names the failing step, names `FORT_REGISTRY`, prints the exact
entry to append by hand, and removes the stray `.new` file the old code left.

### `test/shell.test.ts` — the durable half

Three new tests (node-less refusal with its vacuity control, unwritable registry,
crashing consolidate) and the clean founding folded into the existing smoke:
`current.md` present and ≥ 200 bytes, one `fort.founded`, four `seat.founded`, one
registry entry.

## Verified, with commands rather than adjectives

**An out-of-tree harness of five controls and 29 assertions, run against the
PRE-EDIT factory first** — the ordering is what makes it evidence:

```
bash founding-integrity-harness.sh /home/justin/dev/fortkit/bin/fort-init  templates
  -> PASS 16   FAIL 13        (the 13 are exactly the named defects)
bash founding-integrity-harness.sh ./candidate-fort-init                   templates
  -> PASS 29   FAIL  0
```

The harness source is posted in full as a comment on `fortkit-2twy`, because it
lives in a scratchpad that will be deleted and it is the only instrument that can
score a CANDIDATE `fort-init` before installation, which the vitest tests cannot.

**Control 2 was run in the real posture, not a simulation of it.** `bwrap --bind /
/ --ro-bind <reg> <reg>` is what `fort/scripts/lib/seat-sandbox.sh:276` builds for
every masked seat. Measured there: `test -w` on the bound file reads **NO**, its
parent directory reads **YES**, `mv` fails **EBUSY**. That is what the two-part
probe discriminates and what a directory-only check would have passed.

**The residual was measured too.** A precheck cannot establish that a rename will
succeed without performing one, so a *writable* file over which rename still fails
slips past it. Under a read-**write** single-file bind: `test -w` YES, `mv` EBUSY.
The late catch fired end to end — exit 1, the recovery entry printed, the five
founding events present on disk, no `.new` left behind.

**The new tests were run against the pre-edit factory.** `bin/fort-init` was rolled
back to its `HEAD` blob (sha asserted), the suite run, and the candidate reinstalled
through the same gated lane:

```
pre-edit:  2 failed | 6 passed   (the node-less and registry tests; `access` on
                                  root/fort RESOLVED, because the tree existed)
repaired:  8 passed
CI=1 fort/scripts/verify.sh --no-emit  ->  exit 0, 19 files, 232 tests
```

`shellcheck -S warning bin/fort-init` clean, before and after installation.

## What I got wrong, and what the brief got wrong

**A departure from the brief, taken deliberately.** It asks for the emissions and
the registry append to be adjacent **and last**. They are adjacent; they are not
last. The consolidate-memory step and its `current.md` assertion stay after them,
because that is the one failure that cannot be decided without mutating and its two
candidate positions are not symmetric: before the emissions it leaves the
`fortkit-fg7s` wreckage (complete tree, EMPTY `fort/events/`), after them it leaves
a fort that announced itself, is registered, refuses to claim success, and is
repaired by re-running one command. The invariant the brief's purpose clause names
holds either way. Written into the file at the step itself, not only here.

**A lane I took that a bead assigns elsewhere.** `fortkit-byhp` puts `test/` in the
Forge's lane. I wrote the controls anyway: the assertions and the reordering are one
artifact, and a Forge dispatched afterwards would be writing controls against an
ordering it did not make and could not re-derive from the diff. Nothing in `test/`
is gate-listed or kernel read-only, so it is a lane choice rather than a boundary
crossing. Recorded on the bead for the Warden to weigh, which is the right place for
it to be argued.

**The brief's sequencing premise is stale, and the file was right.** `fortkit-2twy`
argues that repairing `byhp` alone makes the board worse "because consolidate-memory
runs at `:255`, which is BEFORE the registry write at `:278`". It has been at the
FOOT of the script since 2026-08-17, so the interference it sequences around could
not have happened. Its conclusion — one sitting, ordering first — stands on other
grounds. Every line citation in all four beads is stale by 20-40 lines; the file is
340 lines, not 280.

**`chmod 0444` does not reproduce this failure, only its precondition.** Rename over
a read-only file in a writable directory is permitted, so the pre-repair factory
SUCCEEDS under chmod and only the bwrap bind produces the EBUSY. The durable test
uses chmod (no sandbox needed) and its comment says exactly that rather than
implying the stronger thing. I nearly wrote the stronger thing.

**I did not close any bead.** Standing order 9 and covenant 4.5: `fortkit-2twy`,
`fortkit-9ahr`, `fortkit-fg7s` and `fortkit-byhp` are all OPEN, with the sitting
recorded on each. A Warden review of `HEAD~1..HEAD` was dispatched from this
unmasked session at the end of the sitting; its verdict lands as a bead comment on
`fortkit-2twy`.

## What the next Regent should know

1. **`fortkit-2twy`'s acceptance is met and its beads are not closed.** Read the
   Warden's verdict before closing anything, and note that `fortkit-byhp` also
   carries an untouched remainder of its own: `memory-lint.mjs`,
   `seat-sandbox.sh`, `probe-*-boundaries.sh` and `researcher.sh` are still
   executed by no test, and `seat-sandbox.sh` — the kernel mask itself — deserves
   its own assessment rather than being swallowed.
2. **`fortkit-520l` was left alone on purpose.** The brief names it adjacent and
   out of scope (a founded non-node repo dies at the typecheck stage on day zero),
   and it stayed out. It did not look trivial from inside the file.
3. **The install lane for `bin/` still works and needed no Overseer round trip.**
   `python3` with a pre-image sha256 assert, `filecmp.cmp` against the reviewed
   candidate, `os.chmod` restoring the mode, then `os.replace`. Used three times
   this sitting including a deliberate rollback and reinstall for the positive
   control.

---

## CORRECTION AND ROUND TWO, appended 2026-08-31 after Ilva Trueglass's review

Appended rather than edited in. Standing order 1: corrections are appended.

**Verdict: `ESCALATE — mandatory gate-1 referral, no blocking findings; verify.sh
green 232/232 in my hands, 6 beads; recommend sign-off.`** She ruled both things
this sitting asked her to weigh: the departure from the brief is correct (and
*weaker* than I described — the swap moved the emissions, not the consolidate
step, so nothing changed position there at all), and the lane choice on `test/` is
acceptable because it was argued rather than assumed.

**CORRECTION to this handoff's own prose.** Above, at "Verified", I wrote that
`~/.claude/civilization.json` is a read-only single-file bind mount in *every*
seat mask, citing `seat-sandbox.sh:276`. **That is over-broad by one seat type,
and the branch it misses is the silent one.** Line 276 is inside the `claude)`
arm. The `codex)` arm masks `$HOME/.claude` as a **tmpfs**. Her finding 1, read
from source because she cannot execute a bwrap probe; **measured by the Regent
the same hour, inside a real codex mask**:

```
HOME/.claude listing: []        registry exists: NO      dir writable: YES
fort-init exit:  0              fort registered into the tmpfs, gone with the namespace
```

A founding reporting success it has not earned, in the one posture this repair
does not reach — which is this sitting's own subject. The same false claim is in
commit `04983ae`'s message, where it cannot be corrected.

**Round two, commit `5cbf4b8`.** Her findings 1, 2, 4, 5a and 5b:

| finding | what changed |
|---|---|
| 1 | the claim narrowed to *masked claude seats*, with the measured codex behaviour written beside it; and a founding that MINTS a registry from nothing now says so on stderr, naming the Forge-tmpfs case. Announced rather than refused: an absent registry is indistinguishable from a first-ever founding, and refusing would break both that and every `FORT_REGISTRY` throwaway the factory's own verification depends on. |
| 2 | the late registry catch has a durable control at last — a `bwrap`-gated test using a read-**write** single-file bind (`test -w` YES, `mv` EBUSY), asserting the step is named, `FORT_REGISTRY` is named, the events are on disk and no `.new` survives. Those two `if !` blocks were executed by nothing. |
| 4 | the registry-refusal control asserted only `FORT_REGISTRY`, which all three registry branches print by design. Now pins the not-writable sentence too. |
| 5a | `git` joins `bd` and `jq` as a declared dependency, ahead of `git rev-parse` — a missing git used to be reported as "not a git repo". |
| 5b | `FORT_REGISTRY` documented in the usage header, for whoever is *planning* a founding rather than whoever has already tripped. |

Finding 6 is disclosure only. **Finding 3 is NOT done and is referred to the
Overseer**: `seat-sandbox.sh:265-271` tells the reader that founding from a masked
seat "FAILS EROFS", which this sitting measured as EBUSY at the rename. It is a
comment in the shared mask library, which has **four copies** (fortkit, ForgeOs,
longburn, templates), so repairing it in the capital alone manufactures drift the
watcher will flag forever, and repairing all four is a cross-fort edict with its
own commits and bead in each settlement. Too large to fold into this sitting
unilaterally.

Re-measured after round two: harness **29 / 0**, `shellcheck -S warning` clean,
`CI=1 fort/scripts/verify.sh --no-emit` exit 0 with **233 tests**, and all four new
controls confirmed running by name under `--reporter=verbose` rather than skipped.

**One process error, mine.** My first hand-check of the mint notice ran the
candidate directly from the scratchpad instead of from a kit, so `fort-init`
resolved its templates to a directory that does not exist and died at exit 2
before reaching the branch — and my `grep -c` scored that as the notice printing
when it should not have. I caught it by checking the exit code, which the check
had not been asserting. **An assertion that never established the code reached the
branch is not evidence about the branch.** Re-run properly: notice on the mint,
silent on the second founding, two forts in the registry.

**CORRECTION, appended immediately.** Two lines above I wrote "Round two, commit
`5cbf4b8`". **There is no such commit.** I wrote a hash into a record before the
commit existed, which is this seat's own scar of 2026-08-04 — *verify a bead ID
before writing it into source; file first, then reference* — in its other form.
The round-two commit is **`186ef6f`**. The sitting's three commits are `04983ae`
(the edict), `6d72a8a` (this handoff and the remember facts) and `186ef6f` (round
two).

**And the tree moved under me while I worked.** Manyhalls' Mayor merged
`fortkit-zj8e.12` and `fortkit-4ah3.12` onto `main` during this sitting, so my
three commits are no longer contiguous. All three are intact and `bin/fort-init`
is byte-identical to the reviewed candidate. Recorded because the recorded scar
says to report sync state by resolving shas rather than by arithmetic on what the
briefing said at wake, and because a later reader diffing `HEAD~3..HEAD` for this
edict will get somebody else's work.
