# Regent handoff — edict of 2026-08-07: apply the Mayor's sandbox-blocked fixes

Model: Claude Fable 5, as **Calder Sealbroken** (they/them). Overseer present.
Sixth edict. Previous: `regent-2026-08-05T235246.md` (the launchers).

## The edict

The Manyhalls Mayor, mid-session and masked, had four items he could not
execute himself (fort/scripts/ edits, a main-tree commit, and a host-level
systemd check). The Overseer relayed the exact commands; this session applied
them. This is the least-force shape working as intended: the fort drafted its
own fix through its own machinery, and the Regent was only the hand.

## What changed

1. **fortkit-a0z** — `fort/scripts/warden.sh` rsync excludes: `.beads` anchored
   to `/.beads` (the unanchored form was stripping the test fixture's `.beads/`,
   causing 3 false failures in every review), and `--exclude 'bin' --exclude
   'obj'` removed entirely (.NET-era inheritance; this repo's `bin/` is needed
   for the ShellCheck surface). Lines 34-35 after the edit:
   `--exclude '.git' --exclude '.env*' --exclude '/.beads'` /
   `--exclude 'node_modules'`.
2. **fortkit-q7r** — `fort/scripts/forge.sh` prompt sentence, purely additive:
   the Forge is now told to emit `work.begun`/`work.ended` via
   `fort/scripts/emit.sh` at the edges of its work, and to never close the bead
   or change its status (the Mayor closes after review and merge).
3. **Commit `234002d`** on main, staged path-scoped to exactly those two files.
   One deviation from the Mayor's drafted message: the parenthetical says
   "applied by the Regent, by edict, Overseer present" rather than "by the
   Overseer's hand" — the record names the actual actor.
4. **fortkit-r6x.7 (Herald timer)** — nothing to install. Already present,
   `enabled`, `Linger=yes`, next fire 2026-08-08 05:00 AKDT, never yet fired.

## What was verified, with commands

- `grep -n -- "--exclude" fort/scripts/warden.sh` → two lines, `/.beads`
  anchored, no bin/obj.
- `shellcheck fort/scripts/warden.sh` and `shellcheck fort/scripts/forge.sh` →
  both clean.
- `grep -c "Emit work.begun and work.ended" fort/scripts/forge.sh` → 1
  (inserted exactly once).
- `git status --short fort/scripts/` after commit → clean; nothing else swept in.
- `systemctl --user list-timers herald.timer --all` → 1 timer, LAST empty;
  `systemctl --user is-enabled herald.timer` → enabled;
  `loginctl show-user justin -p Linger` → yes.
- `diff` of both installed units against `civ/systemd/` drafts → byte-identical.
- `edict.begun` confirmed present in fortkit's stream at 12:53:57 (launcher
  emitted it correctly — the nvk fix holds).

## What failed / corrections

- **Wake-message error, corrected here**: I reported the untracked
  `.claude/proven-config.json` / `.claude/.proven-config-version` as *fresh*
  ruflo residue. `stat` shows both files dated 2026-08-04 09:22 — the same
  writing already recorded as fortkit-agf, merely never removed. No evidence of
  post-08-04 writing. The residue remains in place, untracked, as evidence per
  the standing note.
- Nothing else failed.

## For the next Regent

- The empirical proof of a0z arrives with the next Warden review: expect 18/18
  in her scratch copy instead of 15/18. The Mayor said he would file the
  standing-order-5 verification beads; if they are absent, ask.
- The Forge (Kethra) had a live session on fortkit-bzx.2 throughout this edict.
  Her prompt predates the q7r sentence; the new prompt binds from the next
  dispatch. Do not expect work.begun/ended events from sessions dispatched
  before 234002d.
- The Herald's first real morning run is 2026-08-08 05:00 AKDT. If the vault
  has no report after that, the crash ladder (stub + incident + exit 65) is the
  place to look.
