# Regent handoff — edict of 2026-09-01: the Herald's watch, and the EROFS claim in four copies

Model: opus, as **Calder Sealbroken** (they/them). Overseer present throughout.
Twenty-fifth edict. Previous: `regent-2026-08-31T212231.md` (fort-init founding
integrity). One day's gap; nothing was in flight.

**The launcher announced correctly again**: `edict.begun` at 20:08:0x, actor
`calder`, seat `regent`, one per fort. The closing half is this launcher's to
write from `civ/handoffs/.edict-2026-09-01T200804`, which carries the bead id and
a one-line result.

**The standing item held a sixth time**: the previous sitting's `edict.ended`
lands after its own handoff commit by construction, so it always belongs to the
next sitting's staging. Expect it again.

## The edict

Four items from the Mayor's brief, two of which had to land as one commit.
**Every bead left OPEN: the Mayor closes after review.**

| item | bead | where | outcome |
|---|---|---|---|
| 1 — the Herald's law reads the computed verdict | `fortkit-dqu5.2` | `civ/law/herald.md` §2 | applied, commit `ce62098` |
| 2 — herald.sh step 3 stops instructing the join | `fortkit-dqu5.3` | `civ/scripts/herald.sh` | applied, same commit |
| (a) — carry-forward of the false alarms | `fortkit-dqu5.4` | law + prompt | applied, same commit |
| 3 — vault corrections | `fortkit-dqu5.4` | two report files | appended |
| 4 — the EROFS claim | `fortkit-zadt` | four `seat-sandbox.sh` copies | applied, `f7af7b4` / `795ca81` / `de5dbc3` |
| not adopted | `fortkit-p1ju` | — | correctly excluded; wants parenting to `fortkit-52vf` |

## What changed, with paths

**Manyhalls `ce62098`** — `civ/law/herald.md`, `civ/scripts/herald.sh`,
`.beads/issues.jsonl`. Items 1, 2 and channel (a) in ONE commit, as both beads
require: the law and the prompt said the same thing before and say the same
thing after, and landing either alone is the `fortkit-smmw` shape.

**Manyhalls `f7af7b4`** — both capital copies of `seat-sandbox.sh`,
`bin/fort-init`, four `fort/controls/*.md`.

**Proofdelve `795ca81`** (bead `ForgeOs-9tir`) and **Farlantern `de5dbc3`**
(bead `longburn-vfej`) — that fort's `seat-sandbox.sh`. Each fort's event stream
committed after (`4b82c03`, `47ffac4`). `edict.applied` emitted in all three
streams, one `emit.sh` per Bash call, each in its own `( cd … && … )`.

**The vault**, appended never edited in, and proved rather than asserted:
pre-image sha256 checked before writing, then the new file asserted to START
WITH the old bytes exactly, which is the control that distinguishes an append
from an edit.
`reports/2026-08-14.md` 23184 → 25159 bytes, then → 25965 with the correction
of my own append below it; `reports/2026-09-01.md` 27924 → 30423 bytes. This is the one item no seat in Manyhalls could do: `touch` into
that vault returns `Read-only file system` from a masked Mayor.

## Verified, with commands

- `CI=1 fort/scripts/verify.sh --no-emit` **bare-observed exit 0**, 19 files /
  237 tests, control-lint clean, `beads-export` clean.
- `shellcheck -S warning` and `bash -n` clean on the `herald.sh` candidate and
  again in place, on all four `seat-sandbox.sh` copies, and on `bin/fort-init`.
- The `herald.sh` replacement asserted mechanically to contain no `"`, backtick,
  `$` or backslash before install — line 87 is inside a double-quoted shell
  string.
- Preserved clauses counted in the installed `herald.sh`: `fortkit-9sa` ×2,
  `BY NAME` ×2, "zero-draft mornings too" ×1, `beadRef is null` **×0**.
- `FAILS EROFS` count **0** and the EBUSY and codex-tmpfs sentences count **1**
  in each of the four `seat-sandbox.sh` copies.
- Every REMOVED line read individually per copy: six lines each, all six the
  target paragraph, no fort's divergence flattened.
- The law describes **shipped** behaviour, not intent: the three verdict values
  plus `announcedBeadRef` and `beadRefs` read out of `src/types.ts:55-65` and
  `src/digest.ts:107-135`.
- The 2026-09-01 alarm measured myself rather than inherited from the brief:
  `charter.amended` at `2026-08-31T09:33:57-08:00` and `11:31:42-08:00`, both
  targeting `fortkit-zj8e.1`, against `fort/events/events-2026-08-31.jsonl`,
  and both precede their commits (`7603af6` 09:34:40, `bae54a8` 11:33:28).

## What I got wrong

**I falsified five line citations and repaired one.** The `seat-sandbox.sh`
paragraph grows by 20 lines, so every citation beyond it moves. I grepped, saw
five in the output, acted on `:276`, and moved on. **`control-lint` caught the
other three on the next verifier run.** They were in my own grep output and I
did not read it. What caught them was a control independent of the person making
the claim, not care and not knowing the rule.

**I published a wrong attribution into the vault and corrected it by appending.**
My 2026-08-14 append said the investigation of that alarm was "recorded in
`civ/remember.md` the same day". It is in Manyhalls' own stream,
`fort/events/events-2026-08-14.jsonl`. I read it off the aggregated session
briefing, which lists that fort's stream under a heading that made it look like
this layer's memory file — a claim measured against one record and reported under
another record's label, which is the class this seat keeps meeting. A further
correction block sits beneath it in the same file.

**I wrote a bead id into a launcher prompt before the bead existed.** The
CARRIED CORRECTION paragraph first cited `fortkit-dqu5.5`; `bd` assigned `.9`,
and `.5` is an unrelated CLOSED bug, so the reference would have pointed at the
wrong bead entirely. Filed first, read the id back, corrected. The 2026-08-04
scar, met again.

## What the next Regent should know

1. **`fortkit-dqu5.9` is filed and is this seat's lane.** The CARRIED CORRECTION
   paragraph in `civ/scripts/herald.sh` is a ONE-REPORT instruction and says so
   in its own text. Remove it after the first Herald report that actually
   carries the correction (earliest 2026-09-02 05:00) — **verify the report
   states it before removing; if the run was silent or crashed, it stays.** The
   durable half lives in `civ/law/herald.md` §2 and must NOT be removed with it.
2. **Two claims from the brief were narrowed, both because they failed the
   falsification test.** "Four reports running" is the 2026-09-01 report's count
   of reports naming `fortkit-dqu5`, the truncation, not the alarm. And "both
   alarms this watch has ever raised" would have needed twenty report reads I
   did not do — every report mentions `fortkit-9sa` because it is a daily duty,
   so the mention count discriminates nothing. The law names the two dates.
3. **`control-lint` fingerprints are hashes of the cited line's TEXT.** So when
   a change moves lines without changing them, relocate citations by matching
   the recorded fingerprint and leave `scripts/control-fingerprints.json`
   UNMODIFIED — the match is the evidence. Ambiguity is possible and must be
   resolved rather than guessed: `wall-codex-config`'s fingerprint matched two
   lines, because `RO_PATHS+=("$HOME/.codex/config.toml")` appears in BOTH arms.
4. **`wall-codex-config` says "by either seat type" and cites one arm.** True
   but narrower than its claim, and it is the same one-arm class this sitting
   corrected one level up. Recorded on `fortkit-zadt` for the Mayor; not acted
   on.
5. **Farlantern's event stream has drifted out of git again** — 2026-08-19,
   08-24, 08-25 and 08-31 were untracked. I committed only 2026-09-01, because
   it carries this sitting's own announcements and a seat acting above a
   constitution must not leave its record uncommitted. The rest is that fort's
   own record; raised on `longburn-vfej`. Same class as 2026-08-11, second
   sighting.
6. **The capital's tree is shared and another session was writing during this
   sitting.** `bd export` before my last commit carried `fortkit-77bc.3`, a
   Warden-round-two bead the MAYOR filed today, not me. Committed separately and
   named, rather than folded into an edict commit.

## Not adopted

`fortkit-p1ju` ("no test executes seat-sandbox.sh"). The Overseer's "this one"
was singular and the Mayor's reasoning is right: it is a build task, not a
paste-and-verify, and it would turn a bounded edict into an open-ended one. It
wants parenting to `fortkit-52vf`, which needs no sitting.
