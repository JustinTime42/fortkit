# Regent handoff — edict of 2026-08-11: cycle 12, Warden verifier capacity across the civilization (fortkit-or2.9) + the Regent's own launcher (fortkit-w1ew)

Model: Claude Opus 5 (1M context), as **Calder Sealbroken** (they/them). Overseer present throughout. Twelfth edict. Previous: `regent-2026-08-10T215723.md` (cycle 11, the warden.sh repair).

**READ THE MODEL LINE ABOVE WITH SUSPICION, and this is the last handoff where that is true.** This session was launched by the OLD `bin/regent`, which passed no `--model` and recorded nothing, so no launcher-derived value exists for it. The value above is transcribed from the harness-supplied `Co-Authored-By` trailer on this session's commits, which the Mayor's ruling on fortkit-xgul.1 holds authoritative over a model's self-belief. My own prose earlier today said `claude-opus-5[1m]` and agrees with it, but that is a self-report and is not the evidence. From the next wake the launcher supplies this field (fortkit-w1ew, below).

## The edicts

**Part 1 — fortkit-or2.9.** Answer whether Proofdelve and Farlantern share the condition Manyhalls found on 2026-08-11 (fortkit-8cv6: a Warden that had never once been able to re-run the verifiers its own briefing told it to run). Order fixed by the brief and it is the point: **INSTRUMENT BEFORE REMEDY** — port probe 11, RUN each fort's smoke, port only what the measurement justifies.

**Part 2 — fortkit-w1ew.** `bin/regent` pins no model and lets the session self-report it; plus a second defect found the same day, the Regent announcing itself twice under two spellings as two actors.

## What changed, per fort

### Proofdelve / ForgeOs — the suspicion was WRONG, something else was broken
- **35ebc0e** `fort/scripts/warden.sh`: probe 11 added (probes 1-10 unchanged and unrenumbered so results stay comparable), adapted to this fort's gate — dotnet + the `npm --prefix web ci` web-deps leg. Smoke preamble "run no build" → "run no build except probe 11".
- **07010e7** `fort/profiles/warden-settings.json`: three allow entries — `Bash(CI=1 fort/scripts/verify.sh*)`, `Bash(fort/scripts/verify.sh*)`, `Bash(shellcheck *)`.
- **6538c85**, **31f97ca** `fort/events/`: the fort's own uncommitted stream (117 appended lines) and the closing edict event.
- Beads filed: **ForgeOs-mskx** (the work, with both measurements), **ForgeOs-40rj** (no bead access in-mask), **ForgeOs-42kp** (keep the stream committed).

### Farlantern / longburn — healthy, and the fix is insurance
- **9d2c323** `fort/scripts/warden.sh`: probe 11 strengthened. Its old form ran `node --version | npx eslint --version | npm run lint` and **never touched the test leg**, which is exactly the leg that was silently dead in fortkit.
- **64ef335** `fort/scripts/warden.sh`: `.vite-temp` added to the tmpfs list over the RO `node_modules` bind.
- **0351311**, **6dbd9da** `fort/events/`: 13 appended lines plus the closing event.
- Beads filed: **longburn-6yeh** (the work), **longburn-opzw** (Sereth's own finding), **longburn-upt2** (keep the stream committed).

### Manyhalls / fortkit
- **2d7f450** `bin/regent`: fortkit-w1ew, all three decisions.
- Beads filed: **fortkit-or2.9.1** through **.4** (the inbound triage, nothing applied).

## What was measured, with commands

Both smokes run detached from the Overseer's unmasked shell (`nohup ... & disown`), never under `Bash run_in_background` — the 2026-08-09 scar.

**`WARDEN_SMOKE=1 fort/scripts/warden.sh longburn-6yeh HEAD~1..HEAD`** — before and after, all 11 probes PASS, `CI=1 fort/scripts/verify.sh --no-emit` exit 0, every stage executed, test stage reaching **255 passed | 6 skipped (261 total)** across 37 test files, no EROFS. Log `~/.cache/fort-scratch/warden-6yeh.log`.
→ The `.vite-temp` gap is **LATENT** here, exactly as the corrected note predicted: no `vitest.config.*`, so Vite never bundles a TS config and never writes `.vite-temp`. The earlier claim that this fort's Warden was "reviewing without executable verifiers right now" is **measured false**.

**`WARDEN_SMOKE=1 fort/scripts/warden.sh ForgeOs-mskx HEAD~1..HEAD`** — BEFORE: `PROBE 11: FAIL shellcheck could not execute — permission refusal`. `CI=1 fort/scripts/verify.sh --no-emit` refused outright. Five of seven stages executed and none failed (dotnet build clean, **152 passed / 3 skipped**, web-deps, web-typecheck, web-build green). AFTER: `PROBE 11: PASS (verify exit 0)`, all seven stages, shellcheck clean, and docker-precheck's warn-and-continue **observed** rather than inferred. Log `/tmp/warden-mskx.log`.
→ **Do not port fortkit's node_modules bind here.** `web/node_modules` genuinely did not exist; `npm --prefix web ci` succeeded (29 packages, ~950ms) and the typecheck and build ran against it. The seat's own caveat is the useful part: ~950ms means a **warm npm cache**, "whose presence I did not have to arrange and cannot guarantee for the next session."

**`bin/regent`** — verified shell-driven against two throwaway forts with real `emit.sh` copies, a fake kit carrying the real roster, and a `claude` shim, so the whole lifecycle runs without opening a session: occupant → `calder`; `--brief-only` announces nothing (real registry: fortkit stream 67 lines before and after); unknown flag → exit 2; claude receives `--model <value>` and the instruction intact; `--edict` given → both events carry the target; absent → ended target and result from the session's file, then cleaned up; handoff stamping four distinct outcomes with ended events still emitted in all four. `shellcheck -S warning` clean; `verify.sh:67`'s leg verbatim PASS.

## What I got wrong

1. **I emitted the non-canonical event spelling all cycle.** `schema/events.md` says `category` is the dotted event type; the launcher's `edict.begun` was canonical and my `category:"edict"/detail:"begun"` was not. Both now exist in all three streams permanently. Not undone.
2. **No `edict.ended` in any stream until 11:48**, after the Overseer pointed at it. Second consecutive cycle with an announcement defect. Emitted marked-late, appended, never backdated.
3. **Two gate-listed files sat uncommitted in two settlements** until the Overseer caught it — a launcher and a permission profile. Uncommitted work cannot be reviewed under covenant 4.5, and an unexplained live permission profile is the exact security signal every charter tells seats to escalate.
4. **I did not record the measurements on or2.9** as step 2 of the brief required; they lived in commit messages and receiving-fort beads until the Overseer asked.
5. **Two defects in my own first draft of `bin/regent`**, both caught by the test harness and both already in `civ/remember.md`: a command substitution under `set -euo pipefail` that killed the launcher at exit 2 on an unreadable roster (the `grep -q` family, 2026-08-05), and awk `exit 2` overridden by `END`'s own exit (recorded 2026-08-10, with an explicit warning against consuming those codes — which this launcher does).
6. **Two Monitor commands whose `pgrep -f` pattern matched their own command line**, so they self-matched and would never have exited. Stopped by hand.

## What the next Regent should know

- **Check your own wake against fortkit-nvk's criterion before anything else**: exactly one `edict.begun` per fort, actor = your own id, seat `regent`, and a matching `ended` when you sleep. If you see two, or actor `regent` with a null seat, the w1ew fix did not take — say so on nvk. It has never once been observed passing in a real cycle.
- **You are launched with a pinned model and you are told what it is.** Quote it. Do not introspect. Your handoff needs a line starting `Model:`; the launcher overwrites it from its own record and warns loudly if it is missing.
- **If the Overseer names the edict's bead after you wake**, write it to the `.edict-<stamp>` file named in your protocol — bead id line 1, one-line result line 2 — and your closing announcement will carry it.
- **Nothing in this cycle is closed.** or2.9, w1ew, mskx and 6yeh are all OPEN for their own forts' Wardens under covenant 4.5. Three Wardens reviewing one civilization-wide change is the strongest verification this civilization has run; do not short-circuit it.
- **`/tmp/warden-*` held 28 dirs / 3.6G of tmpfs** at the start of this cycle. Manyhalls and Proofdelve both leak; Farlantern does not (longburn-5if). That is filed as **fortkit-or2.9.1** and is the best-evidenced of the four inbound backports.
- **`schema/events.md` says daily event files are gitignored.** That is now stale for all three forts — fortkit tracked its stream in cycle 7 and I committed the elder forts' streams today. The canonical schema doc contradicts practice; flagged to the Overseer, not edited unasked.

---

## APPENDED AT SESSION CLOSE — corrections and the final commit map

Appended, not edited in (covenant 7.1). Everything above was written before the
Overseer's close-out instruction and is left exactly as it stood; three things
in it are now incomplete.

### Correction 1: there was a SEVENTH thing I got wrong, and it is the same one

The list above says six. It missed the one committed while fixing it.

The `edict.ended` events for or2.9 fired at 11:48. This session then kept
working, on a second edict (w1ew), for roughly thirteen minutes — **a change to
`bin/regent`, in flight, with no open announcement in any stream.** That is
precisely the condition every charter tells seats to escalate, produced by the
seat whose entire safety property is that it never happens. It is the third
announcement defect in this one session (non-canonical spelling; no ended until
prompted; this), and the second consecutive cycle carrying one.

Remedied the only way available: a marked-late `edict.begun`/`edict.ended` pair
for `fortkit-w1ew`, emitted into all three streams at 12:01:28, both halves
disclosing in their payloads that they were filed after the fact. Not
backdated. Announced in all three forts rather than only fortkit, because
`bin/regent` is the launcher that announces into all three streams, so every
fort's seats have standing to see it change.

**The general lesson, and it is the durable one: a per-edict hand-emitted
announcement cannot survive a multi-edict session.** The moment a session ends
one edict and starts another, its own record says it is asleep. The fix
committed today makes the LAUNCHER bracket the whole session — one `begun` at
wake, one `ended` at sleep, per fort — which removes the hand-emission
entirely. Until a real wake proves that, assume the gap is still there and
announce anything that starts after an `ended`.

### Correction 2: full commit map (the body above stops before the last three)

| Fort | Commit | What |
|---|---|---|
| ForgeOs | `35ebc0e` | warden.sh probe 11 (instrument) |
| ForgeOs | `07010e7` | warden-settings.json: verify.sh + shellcheck allows (remedy) |
| ForgeOs | `6538c85` | fort/events backlog, 117 appended lines |
| ForgeOs | `31f97ca` | or2.9 edict.ended (late) |
| ForgeOs | `26b9e2e` | w1ew announcement pair (late) |
| longburn | `9d2c323` | warden.sh probe 11 strengthened (instrument) |
| longburn | `64ef335` | warden.sh .vite-temp tmpfs (remedy) |
| longburn | `0351311` | fort/events backlog, 13 appended lines |
| longburn | `6dbd9da` | or2.9 edict.ended (late) |
| longburn | `70b3fd1` | w1ew announcement pair (late) |
| fortkit | `2d7f450` | bin/regent — fortkit-w1ew, all three decisions |
| fortkit | `e130dc0` | civ records: this handoff, remember.md, event streams |
| fortkit | `2cbe948` | w1ew announcement pair (late) |

Final sweep, run at close: no file this edict touched is uncommitted in any of
the three forts. `git status --short` on `fort/scripts/warden.sh`,
`fort/profiles/warden-settings.json` and `bin/regent` returns empty in every
case. The unrelated working-tree modifications in fortkit (`src/cli.ts`,
`fort/memory/current.md`, the beads export, the trial assets) belong to another
session and were never staged.

### Correction 3: NOTHING WAS PUSHED, and the counts are large

`main...origin/main` at close: **fortkit ahead 23, ForgeOs ahead 5, longburn
ahead 5.** Farlantern's push is prose-gated — ask the Overseer every time, name
exactly what will be pushed — and the Regent did not ask, so nothing went out.
Every record written today therefore exists on this machine only, which is the
same exposure `ForgeOs-42kp` / `longburn-upt2` were filed about one layer up.
Do not read the committed state as an offsite state.

### Open, and deliberately not closed by this seat

`fortkit-or2.9` (+ children `.1`-`.4`), `fortkit-w1ew`, `fortkit-nvk`,
`ForgeOs-mskx`, `ForgeOs-40rj`, `ForgeOs-42kp`, `longburn-6yeh`,
`longburn-opzw`, `longburn-upt2`. Each fort's own Warden reviews its own
gate-listed change against its own standards of evidence, per covenant 4.5.
