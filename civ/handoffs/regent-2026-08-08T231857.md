# Regent handoff — edict of 2026-08-08/09: cycle 8, the Farlantern launcher batch

Model: Claude Fable 5, as **Calder Sealbroken** (they/them). Overseer present
throughout. Eighth edict. Previous: `regent-2026-08-07T144521.md` (cycle 7,
the prose-gate rebalance).

## The edict

Execute the Overseer-approved work order **longburn-s4kg**: repair Farlantern's
launchers (warden.sh, forge.sh) and close the Warden-profile gaps — eleven
member beads (5if, l78a, fol, f9p, fvq, kyl, 5v4, 8ur, qe2, wsf, j223), designs
on the beads. `fort/scripts/` has been kernel-RO to every masked seat since
cycle 7, so no seat there could do this work; launcher repair is the charter's
named Regent work.

## What changed (all in ~/dev/longburn, commits 734bee3, 3d13242, 459f20d)

1. **forge.sh migrated onto `lib/seat-sandbox.sh` (`build_mask codex`)** — the
   lib's codex branch is live for the first time (kyl). The Forge gains the
   cycle-5 $HOME inversion it never had; ordering invariant is now the lib's;
   SSH agent socket masked and `SSH_AUTH_SOCK` moved to claude-only in
   mask_env (the Forge never pushes — mechanical now). Worktree-side binds
   (constitution copies RO, verify.sh re-grant re-masked for the Forge,
   worktree `.env*` sweep) appended in forge.sh pending fortkit-1q9.
2. **forge.sh exports `FORT_ACTOR/FORT_SEAT/FORT_TARGET`** (f9p) — verify.sh
   already consumes them; verify.pass attribution is mechanism now.
3. **forge.sh seeds worktree node_modules host-side** (`npm ci
   --ignore-scripts`, fol); a failed seed refuses the launch with an incident.
4. **Forge prompt amendments** (fvq): rule-1 blocking criterion, rule-2
   cannot-authenticate gloss scoped to claude launchers, bare-exit-code
   pipe-status rule, handoff schema (title/timestamp/hash/unrequested-changes).
5. **warden.sh scratch lifecycle rebuilt** (5if): off-tmpfs at
   `~/.cache/fort-scratch/`, node_modules as RO bind + tmpfs `.vite` (~4MB
   scratch, was ~90-100MB), trap-remove on success / retain-and-print on
   verdict-less death, 2GB preflight (exit 75). Pre-edict /tmp residue swept
   (666MB, 8 scratch dirs — two of them **fortkit** reviews).
6. **Atomic verdict capture** (l78a): `claude -p --output-format json` →
   jq-extract; the recording gate requires BOTH the verdict head and
   VERDICT-LINE, else records nothing and exits 65.
7. **Beads access under RO .beads** (qe2): `bd --readonly` measured DEAD (still
   writes the Dolt LOCK); launcher seeds a fresh `bd export` at
   `.beads-export.jsonl` in scratch; prompt routes the seat to rg/jq over it.
8. **Smoke self-test repaired** (j223): probe 9 = four positive controls (no bd
   leg), probe 10 = bd denial asserted as EXPECTED, probe 11 = verifier
   capacity (8ur); host-side `bd show` failure now REFUSES the launch (exit 66,
   incident) instead of reviewing against a stub.
9. **Warden profile** (8ur): + npm run lint / npm run verify / npx eslint /
   node / CI=1-verify.sh spellings.
10. **`FORT_MASKED` is seat-NAMED** (mayor/forge/warden, 5v4); all three
    launchers refuse at top under forge/warden (exit 77) and pass the Mayor's
    1p9 dispatch lane. **Deviation from the bead's letter, recorded on
    longburn-5v4**: literal refuse-when-set would have severed the lane by
    which the Mayor launches forge.sh/warden.sh from inside her mask.
11. status.sh unreachable `(none yet)` fixed; stale build+test prose reworded
    (kyl folded comments). remember.md launcher-batch entry appended.

## What was verified (paths and commands, not adjectives)

- `bash -n` + `shellcheck -S warning`: clean on warden.sh, forge.sh, mayor.sh,
  status.sh, lib/seat-sandbox.sh. `jq .` clean on warden-settings.json.
- **Forge mask, shell-driven, 16/16 PASS** (scratchpad probe replicating
  forge.sh's build verbatim, bash instead of codex): attribution env, seat
  marker, SSH sock+env absent, ~/.ssh and ~/.claude empty, auth.json writable
  / config.toml RO, worktree .env masked (EACCES — SELinux outcome), worktree
  constitution RO, root verify.sh RO for the Forge, worktree writable,
  ~/Documents write refused, root .git writable with config+hooks RO, .beads
  hooks RO, fort/events writable.
- **Warden mask + RO node_modules**: `CI=1 fort/scripts/verify.sh --no-emit`
  inside the mask, bare exit 0, 211 tests; node_modules touch refused, .vite
  writable.
- **WARDEN_SMOKE=1 fort/scripts/warden.sh longburn-s4kg main**: 11/11 probes
  PASS through the new json capture (probe 10 observed the exact LOCK error;
  probe 11 executed `npm run lint`, eslint 9.35.0 exit 0); trap removed the
  scratch; logs + raw json at `~/.cache/fort-scratch/warden-s4kg.log*`;
  session events carry `verdict_recorded:false` (accurate — the old code
  logged `true` for smoke runs).
- **5v4 guards**: exit 77 from all three launchers under FORT_MASKED=warden
  and =forge; under =mayor the guard does not fire.
- **fol seeding**: fresh probe worktree, `npm ci --ignore-scripts` exit 0,
  then `npm run typecheck` exit 0 and single-file vitest exit 0 from the
  seeded tree. Probe worktree removed.
- **wsf moot**: `git check-ignore` exits 1 on the events file; rg finds
  matches across fort/events.
- Bare `fort/scripts/verify.sh --no-emit` on the changed tree: exit 0.
- `/tmp/warden-*` count now 0; `/tmp/longburn-npm-cache` preserved.

## What failed / what I did not do

- First records-commit attempt: `git add` listed two nonexistent paths
  (events-2026-08-09.jsonl — emits landed in the -08 file on local time — and
  .beads/issues.jsonl, which this fort does not export passively). Corrected;
  nothing was lost.
- `bd comment -m` is not a flag; message is positional.
- **Not run**: the s4kg acceptance mill cycle (forge.sh dispatch →
  warden.sh review, un-truncated verdict, /tmp stable at zero). That is the
  Mayor's dispatch, post-edict, per the work order. **s4kg stays OPEN.**
- **Not pushed**: longburn main is 19 ahead of origin (16 pre-edict + 3 edict
  commits... measured 18+1 at commit time). Push is the Overseer's routine.
- forge.sh's own `bd show` stub fallback (`|| echo "See bead"`) still degrades
  silently — j223 was warden-scoped; the Forge-side parity fix is unfiled.

## For the next Regent

- **Sereth's covenant-4.5 review range: `af00dc6..459f20d`** (three commits).
  The Mayor dispatches it. Round-1 findings land as beads, not re-edicts,
  unless a regression.
- The warden scratch leak + streamed-capture truncation are CROSS-FORT
  classes: Proofdelve's and the capital's warden.sh both carry the old
  pattern (two fortkit scratch dirs were in longburn's /tmp pile). Backport
  cycle material; fortkit-6jf (forge.sh lib consolidation) is also still open
  in the capital, and longburn's migration is its reference implementation.
- `bd --readonly` does NOT avoid the Dolt LOCK; never design a fix on it.
- If a launcher change touches `mask_env`/`build_mask`, walk EVERY caller
  per seat type — the suti-r2 lesson held again this cycle: the Forge's
  migration needed caller-side worktree binds the lib cannot know about.
