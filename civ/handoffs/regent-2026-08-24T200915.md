# Regent handoff — edict of 2026-08-24: the airlock feedback-scan mask, in Proofdelve (ForgeOs-15k.2)

Model: opus, as **Calder Sealbroken** (they/them). Overseer present throughout.
Twenty-third edict. Previous: `regent-2026-08-17T140057.md` (the advisory
mechanism). A week's gap; nothing was in flight.

**The launcher announced correctly again**: `edict.begun` at 20:09:16, actor
`calder`, seat `regent`, one per fort. The closing half is this launcher's to
write from `civ/handoffs/.edict-2026-08-24T200915`.

**The standing item held a fourth time**: the previous sitting's `edict.ended`
lands after its own handoff commit by construction, so it always belongs to the
next sitting's staging. Expect it again.

## The edict

Not a bead brief this time. The Overseer asked me to read the last message of his
live conversation with **Marrek Splitstone, Mayor of Proofdelve** (transcript
`~/.claude/projects/-home-justin-dev-ForgeOs/1cd2b6e5-….jsonl`, message of
2026-08-25T04:04:22Z) and execute the steps in it that no seat there may perform.
Four steps. He had already done step 1 by hand.

| step | lane | outcome |
|---|---|---|
| 1 — open the Azure Postgres firewall rule | Overseer, gate 3 | **done before the sitting**, by his hand |
| 2 — mask `scripts/airlock-feedback-scan.sh` (`ForgeOs-15k.2`) | **Regent** | **DONE, measured 6/0** |
| 3 — declare the operation in `fort/airlock/operations.json` | gate 6 | **DONE**, Overseer present and instructing |
| 4 — run the scan | Overseer or Regent | **NOT DONE — blocked on a merge that is the Mayor's** |

Commit **`8b0e250`** in ForgeOs, path-scoped, **not pushed**.

## What changed, and where

Six files. Every one is kernel read-only to every masked seat in that fort,
including its Forge, which is why the work reached this seat at all.

| file | change |
|---|---|
| `fort/scripts/mayor.sh:52` | scan script passed as an **extra-ro-path** — readable, not writable |
| `fort/scripts/forge.sh:130-134` | `--mask-file` for the `$root` and `$wt` copies |
| `fort/scripts/forge.sh:82` | both copies added to `deny_table` |
| `~/.codex/config.toml:43` | the `$root` copy mirrored, per forge.sh's hand-sync rule |
| `fort/profiles/warden-settings.json:92` | `Bash(*airlock-feedback-scan*)` |
| `.claude/settings.json` | `Edit()` denies, both spellings the file uses |
| `fort/airlock/operations.json` | the `feedback-scan` operation, verbatim as drafted plus a provenance sentence |

`~/.codex/config.toml` is outside any repo and therefore appears in no commit.

Install lane: python3 patcher → scratch candidates → `shellcheck -S warning`,
`jq -e`, `tomllib` → `os.replace` gated on a pre-image sha256 AND `filecmp`
against the reviewed candidate. Five files, no Overseer round trip.

## The deviation, stated because it is the interesting part

**`ForgeOs-15k.2` asked for the entry in `fort/scripts/lib/seat-sandbox.sh`'s
`claude)` branch. I did not put it there.** That lib is one consolidated file
across four copies (`fortkit-6jf`); measured divergence from the capital's copy
today is 61 lines and every one is comment reflow or a bead id. A path existing
in one fort only would be permanent architecture drift that the capital's watcher
flags forever — and the lib's own header names the caller parameter as the
mechanism for exactly this case ("deltas the shared list cannot know —
Proofdelve's deploy scripts"). The `extra_ro` positional already existed
(lib `:82`, bound at `:438`) and `warden.sh:72` has always used it.

**The Warden needed nothing at all.** `warden.sh` passes her whole checkout as
`extra_ro`, so she gets this free, exactly as she gets `scripts/verify-impl.sh`.
An entry for her would have been a no-op that read as protection.

## Verified, with commands

`/tmp/…/scratchpad/15k2/probe.sh` — **6 pass / 0 fail**:

```
host fixture readable, 11225 bytes
mayor  scan-script        read-only and readable   w=NO  bytes=11225
mayor  control-sibling    WRITABLE as expected            <- vacuity control
warden scan-script        read-only and readable   w=NO  bytes=11225
forge  scan-script        masked to /dev/null      bytes=0
forge  deploy-script      masked to /dev/null      bytes=0 <- positive control
```

RO binds are existence-guarded and the script is not on `main` yet, so nothing
could be measured until the file existed at the path: the probe rendered it from
`bead/15k.1` to `$root/scripts/` as a fixture, probed, and removed it (absence
re-checked). The control sibling is load-bearing — without an unmasked file in
the same directory staying writable, an all-read-only result is
indistinguishable from a mask that denies everything.

Event `edict.applied` at 21:09:56 in ForgeOs's own stream, actor `calder`, seat
`regent`. Comments on `ForgeOs-15k.2` and `ForgeOs-15k.1`. **`15k.2` left OPEN
for covenant-4.5 Warden review**: standing order 9 makes review the gate, and
arriving from above is not an exemption.

## What I did not do

**The merge of `bead/15k.1`, and therefore step 4.** Tova returned
`APPROVE-WITH-FINDINGS` at 20:10:07 — one minute after I woke, so the Mayor's
message still calls that review "in flight". It merges clean (+285/-3). The
Overseer ruled the merge stays Mayor work rather than being taken into the
edict; Marrek owes the bead close, a verifier run and a handoff with it.

**A pre-merge run was not available, and that is measured rather than assumed.**
The script resolves `root` from `$BASH_SOURCE/..` and dies unless
`$root/.env.staging.local` exists. `test -f` says: present in
`/home/justin/dev/ForgeOs`, **absent** in `/home/justin/dev/ForgeOs-worktrees/15k.1`
— it is untracked, so no worktree has it. Copying it to make a run possible would
be precisely the gate-4 secret duplication the design exists to prevent. So the
Warden's recorded open claim — *"the SQL has never executed"* — is **still open**.
The Overseer has asked me to execute the first run once the script is on `main`.

**`ForgeOs-hi9c` filed and deliberately not applied.** See below.

## The finding

`ForgeOs-15k.2`'s description says the deploy script "is explicitly masked in
both seat postures". Measured from a freshly built Mayor mask:

```
scripts/deploy-azure-staging.sh              writable=YES  bytes=13215
scripts/backfill-staging-slack-feedback.sh   writable=YES  bytes=4204
```

True of the Forge (`--mask-file` ×2) and of the Warden (whole checkout
`extra_ro`). **False of the Mayor**, who until today got no extra-ro-path at all,
has no RO entry in the lib and no `.claude` deny rule. The Warden profile's
`Bash(*deploy-azure-staging*)` stops her *running* it; nothing stopped the Mayor
*editing* it — the human-only script that mutates Azure and runs EF migrations
against staging. **After this edict the new scan script is better protected than
the script whose precedent it cited.**

Not folded into the edict: a false premise found inside a brief is not authority
to widen it. Filed `ForgeOs-hi9c` (P1) with the measurement and the two-line fix,
and reported to the Overseer at close.

## What the next Regent should know

1. **The merge window is a scheduling tool, not just a hazard.** Masks bind at
   launch, so Marrek's live session (pid 2314124, launched 15:36:04) built its
   mask before any of this and can still merge `15k.1`. From the *next* launch
   the file is a mount point and git cannot replace it. Installing a mask while
   the seat that must do the last unrestricted thing is still live is a real
   ordering instrument — use it deliberately.
2. **`ForgeOs-15k.1` merge, then step 4, then `hi9c`** is the queue.
3. `bd` works unmasked from any fort with `BEADS_ACTOR=calder`; `bd comment
   <id> --file` remains the only safe lane for prose.
