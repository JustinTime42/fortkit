# Regent handoff — edict of 2026-08-13: E10, five beads no seat can touch (fortkit-52vf.12)

Model: opus, as **Calder Sealbroken** (they/them). Overseer present throughout.
Nineteenth edict, and the third of the day. Previous: `regent-2026-08-13T113656.md`
(E9, Band 1 of the parity gate).

**The launcher announced correctly again**: one `edict.begun` per fort at
15:26:43, actor `calder`, seat `regent`. The closing half is this launcher's to
write and carries the bead id and result line from
`civ/handoffs/.edict-2026-08-13T152642`.

## The edict

Five beads, every one of them in `civ/scripts/` or `fort/scripts/` — kernel
read-only to every masked seat, so no seat any fort can launch could do any of
it. Ordered: 1 first, then 2 and 3 as a single change (both edit
`drift-watch.mjs`, and a second pass re-opens drift in the opposite direction —
fortkit-29e9), then 4 and 5.

## Rollback, recorded BEFORE the first edit

Pre-image sha256, taken before anything was touched:

```
282c332f… fortkit/fort/scripts/warden.sh
e1877501… fortkit/civ/scripts/drift-watch.mjs
b5998c52… fortkit/fort/scripts/mayor.sh
4603055e… ForgeOs/fort/scripts/mayor.sh
b86a56c4… fortkit/fort/scripts/lib/seat-sandbox.sh
cdaa7d2b… ForgeOs/fort/scripts/lib/seat-sandbox.sh
4f036e8f… longburn/fort/scripts/lib/seat-sandbox.sh
b61bae69… fortkit/templates/fort/scripts/lib/seat-sandbox.sh
64180089… fortkit/civ/scripts/chronicler.sh      (taken at install time)
20ea9b93… longburn/fort/scripts/mayor.sh          (taken at install time)
```

Rollback is `git checkout 5336796 -- <path>` in fortkit, `27c501f` in ForgeOs,
`f6c4bbb` in longburn. Every install asserted its pre-image hash and gated on a
`filecmp` against the reviewed candidate, so a drifted source refuses rather
than being silently patched.

## Baseline, reproduced rather than quoted

The bead's recorded baseline was re-run before any edit and matched exactly:
`drift-watch --dry-run` 39 identity matches, plan `{file 0, comment 12,
suppress 23, defer 0, reobserved 4, propagationGaps 9}`; `verify.sh --no-emit`
176 tests, exit 0; main at 5336796, `origin/main == HEAD`.

## What changed

### 1. fortkit-iist — the review gate could record a verdict the reviewer did not give

`fort/scripts/warden.sh` posted the WHOLE transcript as the bead comment under
Ilva's actor id and took the verdict by `tail -1` over the whole log. Now the
record is bounded at the FIRST terminal `VERDICT-LINE` and the verdict is read
from that same bounded section; anything trailing becomes a launcher-authored
`incident` under actor `harness` — emit.sh's own default and the schema's actor
for launcher-emitted events — never under her name.

**The prompt moved with the code.** `warden.sh:125` told the seat "the launcher
records your ENTIRE final message verbatim". After the fix that overstates what
the launcher does, which is the same class of defect being repaired. It now
states the bound and adds the contract that follows: *your VERDICT-LINE must be
the last line you write.*

**The sibling survey the acceptance required.** `forge.sh` is clean (it tees to
/tmp and never posts under an actor id; the durable record is the Forge's
handoff, which the launcher *stamps* rather than authors). `herald.sh` is clean
(the seat writes its own report; the launcher's only artefact is a crash stub
that says so in its own text). **`civ/scripts/chronicler.sh` was not clean** —
`cp "$log" "$record"` plus the same `tail -1` — and is repaired in the same
change under this bead's own closing clause about any launcher piping raw stdout
into a durable record under an actor id.

### 2 + 3. fortkit-qu46 and fortkit-o9iu — one pass over `civ/scripts/drift-watch.mjs`

**qu46.** `identitySuspect()` flags a hunk where one side carries an identity
placeholder the other does not, by COUNTS not sets — the measured case is
`-a {{ACTOR}}` against `"…summons {{ACTOR}}" -a {{ACTOR}}`: same placeholder,
different number of times, which a set comparison misses entirely. Flagged hunks
are labelled in the body, not hidden. `reason` and the description no longer
claim "outside identity" or "Architecture hunks".

**o9iu.** Finding 2: the clip window now FOLLOWS the divergence, and
`diffCharCap` stays at 12000 — the window costs no extra bytes, so the
`.beads/issues.jsonl` shipping cost is unchanged. Finding 5: `-a` requires
actor-id shape. Finding 6: the roster gap is disclosed PER SEAT FILE. Finding 7:
the census comment states its allowlist exception. Two sub-items of finding 5
are **declined** with the reasons written into the code.

### 4. fortkit-px7e live half — and a correction to the edict that ordered it

The in-sandbox launch refusal, hoisted ABOVE repo resolution, carried to
Manyhalls and Proofdelve as ordered — **and to Farlantern, which E10 excluded.**
See "What the instruments said" below; the Overseer was asked first and directed
it. The push-gate half was verified as a no-op (one occurrence in each of four
copies before the sitting).

### 5. fortkit-52vf.10.1 — four copies of the lib in one change

The shared comment asserting a forge.sh deny table over the codex home was true
in Proofdelve and false in the other three. **Departure from the finding's
suggested fix, recorded:** it says "name the enforcing mechanism per copy", which
would give the four libs a new permanent divergence axis inside the very comment
block whose divergence is the complaint. Instead one wording true in all four
(`codex exec --sandbox workspace-write`, whose writable roots are the explicit
`--add-dir` grants), with Proofdelve's deny profile named as an additional layer.
The recursive codex-mask consequence and the `--mask-file` directory gap are now
written down. Warden findings routed to `fortkit-wtps` and `fortkit-3jv7` are
deferred with reasons.

## What the instruments said

**Every positive control was run BEFORE the fix it controls.** E9's own handoff
records running one second as a process failure; that ordering held throughout
here.

| instrument | before | after |
|---|---|---|
| `scripts/verdict-record-harness.sh` | **12 pass / 8 fail** | 20 pass / 0 fail |
| `test/drift-watch.test.ts` (4 new) | **4 fail / 20 pass** | 24 pass / 0 fail |
| mayor guard probe, per fort | **4 FAIL / 1 PASS** ×3 | 5 PASS ×4 |
| `scripts/mask-harness.sh`, per lib | 61 / 0 | **61 / 0** |

All 8 verdict-record failures were on fixture B and only on fixture B, in *both*
launchers. The mask harness scoring identically before and after is the
*required* result, not a null one: that change is comments, and any movement
would have meant behaviour I did not intend to change.

**The mayor guard probe never launches anything.** Each copy is rendered to
scratch with its repo fallback repointed at a nonexistent path, so `cd` fails on
any path that reaches it — exactly the round-1 condition the Warden measured —
and no `session.start` is emitted and no Mayor is spawned. That matters because
px7e's own Warden finding 4 is that running the real launcher against the real
repository writes a false `session.start` and execs a live Mayor.

**THE EDICT'S OWN SCOPE EXCLUSION RESTED ON A FALSE PREMISE.** E10 said "NOT
Farlantern — longburn is the ORIGIN and needs nothing", on the measurement
recorded at fortkit-px7e that it "checks on ENTRY, at its lines 8-9". Measured:
`cd "$REPO" || exit 1` is at line 4 and the guard at lines 8-11. Lines 8-9 are
not entry. **The origin carried the defect its own backport was hoisting to
fix**, and it probed 4/5 FAIL exactly like the two forts being repaired against
a description of *it*. The earlier reading counted `FORT_MASKED` line numbers and
never established what line 4 was — the same shape as the correction already on
that bead about generalising from a per-hunk diff. I put it to the Overseer
rather than widening on my own; he directed the hoist.

## Differential against the recorded baseline

- `verify.sh --no-emit`: **176 tests exit 0 → 180 tests exit 0**, green in three
  postures — outside a mask, with `FORT_MASKED=1`, and inside the real Mayor mask
  built from `seat-sandbox.sh`.
- `drift-watch --dry-run`: identityMatches **39 → 39**; plan comment 12 → 13,
  suppress 23 → 22, defer 0 → 0, reobserved 4 → 4, file 0 → 0, propagationGaps
  9 → 9. The single move is this fort's own `fort/scripts/warden.sh`, whose
  content *this sitting* changed: same identity, new fingerprint, appended rather
  than re-filed. That is the watcher working.
- `shellcheck -x` clean at verify.sh's own severity on every shell file touched.

## What failed, and what I got wrong

1. **I emitted a fortkit event into Farlantern's stream.** A compound
   `cd /home/justin/dev/longburn && ./fort/scripts/emit.sh …` was followed by a
   second `./fort/scripts/emit.sh …` on the next line of the *same* Bash call —
   cwd persists, and emit.sh resolves its target from `$PWD`. This is
   fortkit-nvk, the founding scar of this seat, in my own hands, with the entry
   sitting in the memory I was briefed with. Corrected append-only:
   `incident.corrected` in longburn naming the misfiled timestamp
   (2026-08-13T15:50:06-08:00), and the incident re-emitted into fortkit.
   **One emit per Bash call, each wrapped in its own `( cd … && … )`.**
2. **The harness refused on its first run** — an awk bracket expression failed to
   match and it extracted an empty block. It exited 3 with "a harness that checks
   nothing must never report success" rather than passing vacuously. Working as
   designed, but it cost a round.
3. **My first `identitySuspect` counted `{{PROJECT}}` and `{{REPO_PATH}}`.** A
   fort-local bead id contains the project name (`ForgeOs-8c9`), so it normalizes
   to `{{PROJECT}}-8c9` and flagged 4 of 7 hunks in Farlantern's lib, none of
   which is a citizen. Narrowed to `{{ACTOR}}`/`{{FORT_NAME}}` — which is what
   qu46 specified in the first place — before shipping.
4. **My first in-mask verify probe was the Warden's posture, not the Mayor's.** I
   passed `$root` as an extra read-only path and got EROFS on
   `node_modules/.vite-temp`. The recorded dispatch-lane recipe is
   `build_mask claude "$REPO"` with no extra_ro.
5. **My correction to the false lib clause QUOTED it verbatim**, so `grep
   'guarded deny table'` still returned 1 in all four copies and the bead's own
   acceptance check would have read as a fail. Paraphrased. A literal that greps
   like the defect is the E1 lesson.
6. **`fortkit-hhjb`, `fortkit-vxpq`, `fortkit-8eq5` did NOT close**, against
   E10's expectation. The reasoning was that the watcher does not strip comments
   — true, but the comment I repaired was *identical in all four copies* and so
   was never part of those findings. What remains is real, comment-only
   divergence: the capital carries one extra explanatory clause; Proofdelve and
   Farlantern each cite their OWN cycle-7 bead beside `fortkit-i4y`. Whether a
   fort's copy of a shared lib may cite its own bead id is a parity-programme
   decision for the Mayor, not the Regent's to take. Recorded on all three.

## What the next Regent should know

- **`bd comment --file <path>` is the safe way to put prose on a bead.** Every
  record in this sitting went that way, and none of the backtick, quote or
  heredoc hazards recorded in `civ/remember.md` could fire.
- **`scripts/verdict-record-harness.sh` is new and re-runnable.** It extracts the
  REAL recording block from the REAL launcher by anchors that exist in both the
  pre-fix and post-fix files, so it can still be pointed at an old copy to
  re-establish its discriminating power. It refuses on an empty extraction.
- **The install lane held for every kernel-RO path this sitting**, including
  `civ/scripts/**`: `python3` `shutil.copyfile` + `os.replace`, with a pre-image
  sha256 assert and a `filecmp` gate. No Overseer round trip was needed for any
  file.
- **Nothing is pushed.** Three forts have one commit each and all three are ahead
  of origin. Pushing is the Overseer's call.
- **All five beads are left OPEN for the forts' Wardens under covenant 4.5.** The
  Regent does not close what the Regent wrote.
