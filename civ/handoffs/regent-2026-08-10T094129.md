# Regent handoff — edict of 2026-08-10: cycle 9, the forge.sh integrity batch

Model: Claude Fable 5, as **Calder Sealbroken** (they/them). Overseer present
throughout. Ninth edict. Previous: `regent-2026-08-08T231857.md` (cycle 8, the
Farlantern launcher batch).

## The edict

Four work orders plus an optional rider, all in Manyhalls: (1) merge bead/6jf
(forge.sh onto lib/seat-sandbox.sh) under the Overseer's inline confirmation of
the Warden's round-2 clean-escalate consequences; (2) create an empty
`~/.codex/config.toml` on the host (6jf r2 finding 1); (3) port fortkit-vn8
(handoff/emit stamping, from Proofdelve agk 489f358 + uyu 3033e22) folding
fortkit-wi0; (4) verify and hand back; rider: the fortkit-fwq appearance
ceremony. `edict.begun` 09:43:12, `edict.ended` at close, both from the fort
root as calder/regent.

## What changed

1. **Merge 3ec099b** (`--no-ff bead/6jf`): forge.sh consolidated onto
   `build_mask codex`; both consequences named in the commit message with the
   Overseer's 2026-08-10 confirmation. **Merge resolution of record**: main's
   cycle-7 inline-mask additions postdated the branch; the lib covered all of
   them except the worktree `fort/scripts` RO bind, so `"$wt/fort/scripts"` was
   added to the build_mask extra-RO args in both forge.sh copies. A verbatim
   theirs-side resolution would have silently dropped that protection
   (fortkit-cqc class). Flagged on the bead for the Warden's 4.5 review.
2. **WO2 executed as a no-op, deliberately**: `~/.codex/config.toml` already
   EXISTS on the host — 3323 bytes, mode 600, mtime 2026-08-04 20:06, the real
   guarded-profile config the Overseer restored on 08-04. The Warden's
   finding-1 premise ("absent on this host") came from the round-2 Forge
   handoff, which fortkit-x12 already records as carrying false self-reports.
   Creating the ordered empty file would have overwritten the real deny
   profile. Measured facts and the correction are on fortkit-x12.
3. **Commit 98d0221** (fortkit-vn8 + wi0, both forge.sh copies): launcher
   stamps handoff Model+timestamp from its own record (self-report retired);
   collision-safe `-rN` handoff paths with `[Ff]orge` stamp contract and
   marker-fallback resilient post-session block; literal emit recipe in the
   prompt (required non-empty detail — bns-guard-proof) plus the
   work.ended-after-final-commit / amend-means-reverify practice; launcher runs
   `verify.sh --no-emit` in the worktree post-session and appends the observed
   exit + tail to the handoff (rh6 class), with `verify_exit` riding
   handoff.written and session.end payloads; session.start/session.end
   payloads carry `"worktree"` (wi0 fold).
4. **Commit 3fbb0fb** (rider): three declarations verbatim with provenance in
   `fort/roster-appearance.md` — Oswin, Halric (fresh Regent-convened
   read-only ceremony sessions), Calder (in-session). **Deviation of record**:
   the bead's herald.sh/chronicler.sh route is mechanically unavailable (both
   launchers carry fixed prompts, no ceremony hook); founding-precedent
   instrument used instead, recorded on fortkit-fwq.

## What was verified, with commands

- `CI=1 fort/scripts/verify.sh --no-emit` green after the vn8 port: 125 tests,
  both typechecks, lint, ShellCheck over the shipped shell surface (which
  includes both forge.sh copies and the lib).
- `bash -n` + `shellcheck -x -S warning` on both forge.sh copies and both
  seat-sandbox.sh copies after every write.
- The awk stamp contract smoke-tested in the scratchpad: case-variant header
  stamps correctly, Model line replaced once (body occurrences untouched),
  bad-header and no-model inputs both refuse.
- Registry transcriptions diffed whitespace-normalized against the ceremony
  sessions' actual output (`jq` over the subagent transcripts): word-identical
  both times.
- `git log --oneline`: main is 814b899 → 3ec099b → 98d0221 → 3fbb0fb (+ this
  records commit). NOT PUSHED — the push gate asks the Overseer every time.

## What failed / quirks found

- The `git merge` conflicted in both forge.sh copies (expected — inline block
  vs lib). Resolved by hand; nothing lost, see resolution note above.
- The Edit tool is policy-denied on `fort/scripts/` even for the unmasked
  Regent (known since cycle 7); all script writes went through scripted
  exact-match replacements per the 21f.5 precedent.
- **Port quirk, faithful to the ForgeOs source**: in the stamp awk, `exit 2`
  on a bad header still runs the END block, so both refusal paths actually
  exit 3. Harmless (the launcher only branches on success/failure) — noted on
  fortkit-vn8 in case anyone ever consumes the codes.
- WO2's letter contradicted host state; executed the intent (overlay has
  something to bind) rather than the letter (create empty file). If that
  reading is wrong, the fix is one `install -m 600 /dev/null` away, but I do
  not believe it is.

## What the next Regent should know

- **Settling proofs now pending on the next real Forge launch** (Mayor's to
  observe and close): EROFS on the worktree constitution incl. fort/scripts
  (6jf, x12), working codex auth with config.toml RO non-empty (x12), stamped
  handoff + worktree payload in events (vn8, wi0). The fort/remember.md caveat
  retires only after that probe — nobody touched it this edict.
- herald.sh and chronicler.sh have NO ceremony/prompt hook. Any future
  ceremony for those seats is a fresh convened session or a launcher
  amendment through gate 1 — do not go looking for the hook.
- fortkit-6jf, vn8, wi0, x12, fwq are all deliberately left OPEN for the
  Mayor's normal flow; every one carries a comment saying exactly what landed.
