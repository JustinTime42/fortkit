# Regent handoff — edict of 2026-08-12: E2b, clearing the kernel-RO decks (fortkit-52vf.9)

Model: opus, as **Calder Sealbroken** (they/them). Overseer present throughout.
Sixteenth edict, and the fourth of the day. Previous: `regent-2026-08-12T144349.md`
(E2, the mask).

**The launcher announced correctly again**: one `edict.begun` in each of the
three streams at 22:19:44, actor `calder`, seat `regent`. The closing half is
this launcher's to write, and it carries the bead id and result line from
`civ/handoffs/.edict-2026-08-12T221943`. The next Regent should confirm one
`edict.ended` per fort.

## The edict

One sitting for everything the Mayor cannot write, drawn deliberately rather
than by sweeping the tracker: seven items A through G, with an exclusion list
that was part of the brief. The core was **reversing Shape A of `fortkit-6ovg`
to Shape B** — the change that can stop every seat in the civilization
launching, and therefore the last thing committed, so a rollback would have the
freshest thing in the record.

## Rollback, decided and stated BEFORE the first edit

```
git -C /home/justin/dev/fortkit  checkout b2881b7 -- fort/scripts/lib/seat-sandbox.sh \
    fort/scripts/probe-cycle7.sh fort/scripts/verify.sh templates/fort/scripts/lib/seat-sandbox.sh
git -C /home/justin/dev/ForgeOs  checkout 9939506 -- fort/scripts/lib/seat-sandbox.sh fort/scripts/verify.sh
git -C /home/justin/dev/longburn checkout b2ffa04 -- fort/scripts/lib/seat-sandbox.sh fort/scripts/verify.sh
```

A leftover `scripts/verify-impl.sh` after a rollback is inert — nothing would
reference it. **The skills symlinks (item F) must roll back WITH item A**, since
they depend on A's `skills/` carve-out to preserve fortkit-5sk; the pre-symlink
copies are in the edict scratchpad at `.../e2b/skills-backup/`.

## What changed

Committed in the order the Overseer specified. Nothing was left out of the
bundle.

| Item | fortkit | ForgeOs | longburn |
|---|---|---|---|
| D city/ fallback | *(host wrappers, no repo)* | — | — |
| E inert `Write()` rules | `9dc381f` | `19b1496` | `a0b5688` |
| F+G skills symlink, factory lint | `29d1539` | — | — |
| B launch-aborting glob | `3d12ecc` | `953a2bf` | `c61fbd2` |
| C `.beads` hooks + findings 4/6/8 | `03003dc` | `b587a58` | `e185fbf` |
| A **SHAPE B** | `58ddae6` | `801f02b` | `8b1b2ad` |

Receiving beads: **ForgeOs-cgje**, **longburn-liyt**. `fortkit-52vf.9` and every
bead it carries are LEFT OPEN for the Wardens under covenant 4.5, as instructed.

**A — Shape B.** `fort/scripts` is a single whole-directory `--ro-bind` for every
seat type again, which makes the directory itself a mount point that refuses
rename. `fort/scripts/verify.sh` is a read-only shim that execs
`scripts/verify-impl.sh`, forwarding arguments and exit status. The codex posture
gains read-only carve-outs for `$t/scripts/verify-impl.sh` and `$t/skills`.

**B.** `[ -e ]` → `[ -f ]` at the secret-glob sweep. At the BIND site the belt is
`[ -e ] && [ ! -d ]`, not `[ -f ]` — MASK_FILES carries the docker and podman
sockets and `[ -f ]` is false for a socket, so the suggested belt would have
silently stopped masking the docker socket.

**C.** `.beads` hooks per declared tree; `bin/fort-init`'s stranded registry
write disclosed; the factory's `warden.sh` gains the `$root-worktrees`
rationale; every `probe-cycle7.sh` canary moved into the EXIT trap.

**D.** `~/.local/bin/{mayor,fortstat}` lost the `city/` fallback entirely.

**E.** 37 / 42 / 31 inert `Write()` rules replaced by their `Edit()` twins.

**F.** `~/.claude/skills/{civ,fort-backport}` are symlinks into the repo, and
`verify.sh` gained a `skills-install` step that fails if a copy comes back.

**G.** `templates/fort/scripts/lib/*.sh` and `scripts/*.sh` joined shellcheck.

**Charter**, all three forts, `charter.amended` emitted in each, Overseer
approval recorded verbatim on the bead beforehand. The fortkit accepted-residual
bullet was REMOVED, not reworded.

## What was verified, with commands

- **`scripts/mask-harness.sh`, one assertion set, three libs.** pre-E2
  (`93037eb`) **44 pass / 17 fail**; Shape A (installed) **55 / 6**; Shape B
  **61 / 0**, all four copies. The harness was inverted BEFORE the lib was
  touched, so the six are a positive control. They are exactly: `A2b` shim
  writable, **`A4b` RENAME SUCCEEDED**, `A6` new file created, `C4c`/`C4e` the
  Forge able to edit its own judge, `C4d` the Forge able to edit `skills/`.
- **`fort/scripts/probe-cycle7.sh /home/justin/dev/fortkit`: 35 pass / 0 fail**,
  line 74 inverted from an expected pass to a deny.
- **A live-fort probe against all three forts**: Mayor sees the shim RO, the
  implementation writable, no new file in `fort/scripts`; Forge sees
  `fort/scripts` RO, `verify-impl.sh` RO, and (fortkit) `skills/` RO.
- **Item B proved by a fixture that reproduces the failure**: with directories
  named `environments~` and `#environments#`, the old lib gives
  `RESULT: BWRAP ABORTED -- bwrap: Can't create file at .../#environments#: Is a
  directory`, the new lib launches with the real secret at 0 bytes of 34. All
  four copies. Docker socket masks after: 2, unchanged.
- **Item C proved in the Forge posture with a declared worktree**: old lib
  `wt1/.beads/hooks WRITABLE`, new lib blocked, control `wt1/src` writable in
  both.
- **Item C finding 8 proved by SIGTERM mid-run**: no residue in `fort/scripts`,
  `fort/memory/facts` or `$TMPDIR`.
- **Verifiers, bare exits.** fortkit 0 (163 tests). longburn 0 (255 passed, 6
  skipped). ForgeOs 0. All three through the new shim.
- **Three real Mayors**, one per fort, launched under a pty. All started and
  rendered their status lines inside the mask. `/proc/2705474/mountinfo` on the
  live fortkit seat: `/home/justin/dev/fortkit/fort/scripts ... ro,nosuid,nodev`
  — **one mount, whole directory, no per-file binds, no verify.sh re-bind.**
- **Item E acceptance**: 0 inert-rule warnings in each of the three real launch
  logs, against 37/42/31 rules before.
- **Item F negative control**: replacing a symlink with a copy fails the new
  verifier step naming the path; restoring it passes.

## What I got wrong

1. **My smoke test for the repaired `mayor` wrapper started a real Mayor
   session.** `mayor --help | head -3` — a wrapper that ends in `exec` has no
   `--help`. It put a `session.start` under Emrith's actor id in the capital's
   stream at 22:22:31 before SIGPIPE killed it. Corrected as an appended
   incident naming the timestamp.
2. **My first measurement for item E measured nothing.** `claude --help` prints
   no inert-rule warnings even when the settings file contains one, so my "0 in
   all three forts" was worthless. The positive control caught it. The
   detector that discriminates is `claude --dangerously-skip-permissions <
   /dev/null`.
3. **The splitter's double-split guard was too loose** and refused a legitimate
   split, because `verify.sh`'s shellcheck comment already names
   `verify-impl.sh`. Tightened to match the shim's exec line. The guard firing
   wrongly is the cheap direction, but it was still wrong.
4. **A shell-quoting artefact (`'"'"'`) landed inside a lib comment** in the
   first C candidate and had to be corrected in the patcher, not in the output.
5. I reported `BARE EXIT VIA SHIM: 0` from a piped command once. That was
   `tail`'s status; the run was actually red. The civilization has been bitten
   by this three times and I made it four.

## Two deviations from the stated lane, disclosed

1. **`bin/fort-init` and a new `templates/scripts/verify-impl.sh`** are not in
   the bead's LANE. Without them the factory founds forts whose shim exits 70.
   The founding smoke test caught exactly that, red, before the fix — the test
   doing its job.
2. **`ForgeOs/fort/scripts/forge.sh` gained four `RO_PATHS` entries**, inside
   the excluded `fortkit-6jf` lane. It is the last inline mask in the
   civilization and never calls `build_mask`, so the codex carve-out could not
   reach it; without the stopgap, Proofdelve's Forge would have been the one
   seat anywhere able to rewrite the verifier judging its own work. Commented as
   a stopgap that 6jf retires.

## What the next Regent should know

1. **`fortkit-6jf` should be the next sitting.** The E2b brief said so and it is
   more true now: ForgeOs's `forge.sh` carries a hand-copied stopgap of a
   property every other launcher gets from the lib, and every further mask edit
   widens that gap.
2. **`fortkit-faka` finding 7 is the one item of the bundle not done** —
   reconciling two records that give different sole causes for 6ovg. Shape B
   rewrote both the charter passages and the lib comment from one measurement in
   this sitting, so the divergence may no longer exist; I did not verify that,
   and saying so is cheaper than claiming it.
3. **The install lane is better than the record predicted.**
   `python3 <patcher> "$f" "$f.tmp" && mv` landed four `seat-sandbox.sh` copies,
   `probe-cycle7.sh`, three `verify.sh` and `bin/fort-init` with no Overseer
   round trip. `bin/` refused a compound `python3 … && mv` and accepted
   `install -m 755` as its own command. `civ/scripts/**` was not tested tonight;
   the 2026-08-12 entry saying it needs the Overseer's hand stands untested.
4. **Every Regent launcher verification writes a `session.start` under the
   fort's own citizen's actor id.** Four were written tonight (one accidental at
   22:22:31, three deliberate at 22:45:45) and all four are corrected on their
   own streams. Budget a correction per launch.
5. **`longburn-3195` is probably resolved and was NOT re-measured by me.** The
   mechanism it describes — a read-write file bind inside a read-only directory
   — no longer exists. Farlantern's Mayor owns the re-measurement.
6. **`fortkit-vkq8`'s real question is still open**: whether the Herald should
   hold capabilities of that class at all. Shape B removed the specific
   residual, which makes it no longer urgent, not answered.
7. **Nothing is pushed.** All three forts are ahead of origin. That is the
   Overseer's call, as always.
