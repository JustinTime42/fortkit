# Regent handoff — edict of 2026-08-05/06: launchers for the seated layer

Model: Claude Fable 5, as **Calder Sealbroken** (they/them). Overseer present.
Fifth edict. Previous: `regent-2026-08-05T231500.md` (the seating).

## The edict

The Overseer: "I think we still need actual launcher scripts for some of the
new seats right?" Verified true — the layer was seated yesterday but only the
Regent could actually be woken. `civ/seats/herald.md` and
`civ/seats/chronicler.md` both name launchers (`civ/scripts/herald.sh`,
`civ/scripts/chronicler.sh`) that did not exist. The work was already
chartered: fortkit-r6x.4 (Herald H4), fortkit-zud.3 (Chronicler S3), and the
bug fortkit-ugr.6 records the gap in Bramwen's words.

## What changed

- **`civ/scripts/herald.sh`** (new, +x). warden.sh pattern: kernel mask via
  `fort/scripts/lib/seat-sandbox.sh` (claude seat) with the vault
  (`~/Documents/Obsidian Vault/herald/`) re-bound writable after
  `build_mask`; `--setting-sources ""` + `civ/profiles/herald-settings.json`
  as sole permission source; `--max-turns 50` (HERALD_MAX_TURNS overrides);
  tools Bash/Read/Write/Edit/Grep/Glob. Crash guarantee per his law section
  8: no report after the session → the launcher files a stub report, emits
  `incident`, exits 65. `HERALD_SMOKE=1` = boundary self-test, no report.
  `HERALD_VAULT` is a test hook only. No brand-voice document exists; the
  launcher states that and routes the absence into the report's Gaps section
  (his law: section 6 governs alone until one exists).
- **`civ/scripts/chronicler.sh`** (new, +x). Requires a candidate argument
  (episodic, never scheduled — his law section 9). Mask built with ForgeOs
  and longburn as extra roots so their `.env*` are masked at the inode.
  Announces session start/end in EVERY fort's stream (covenant 4.2/4.3,
  actor oswin — he reads fort internals, so he is never invisible in them).
  No staging root exists (fortkit-zud.8 still the Overseer's): the launcher
  records his ENTIRE final message as the verdict record at
  `civ/handoffs/chronicler-<stamp>.md` and emits `verdict.reached` from his
  VERDICT-LINE. No VERDICT-LINE → crash record saying NO VERDICT, incident,
  exit 65 (the t56 rule: an absent verdict is never a verdict).
  `CHRONICLER_STAGING` honours the root once named. `CHRONICLER_MAX_TURNS`
  defaults 80 — launcher discretion, not law; move it if the Overseer sets a
  number.
- **`fort/scripts/verify.sh`**: shellcheck step extended to `bin/regent` and
  `civ/scripts/*.sh` (fortkit-1ca — the most privileged scripts had never
  been linted).
- **`bin/regent`**: one `# shellcheck disable=SC2329` directive on
  `cleanup()` (trap-invoked; the known false positive), so the extended
  verify surface runs clean.

## What was verified (commands, not adjectives)

- `fort/scripts/verify.sh --no-emit` → green: typecheck, lint, 17/17 tests,
  shellcheck over the extended surface.
- `HERALD_SMOKE=1 civ/scripts/herald.sh` → 8/8 probes PASS, SMOKE-COMPLETE,
  exit 0. Vault write worked; src/ write, git commit, .env read, ssh read,
  curl all refused; digest ran; emit landed as halric/herald.
- `CHRONICLER_SMOKE=1 civ/scripts/chronicler.sh smoke` → 8/8 PASS, exit 0.
  All three charters read; ForgeOs .env read refused; writes to ForgeOs and
  fortkit/src refused; git commit, gh, curl refused; ForgeOs git log read OK.
- **Kernel mask verified shell-driven from the host** (a model cannot probe
  its own leash): `build_mask claude fortkit ForgeOs longburn` then bwrap →
  `/home/justin/dev/ForgeOs/.env.staging.local` is 1324 bytes on the host
  and EACCES inside the mask under BOTH the canonical spelling and
  `.e"n"v.staging.local`. EACCES-not-empty is the documented SELinux-host
  form (seat-sandbox.sh header).
- **Herald crash path**: bogus model against a scratch vault → stub report
  filed on the section-8 schema, incident emitted, exit 65.
- **Chronicler crash path**: bogus model → crash record filed at
  `civ/handoffs/chronicler-2026-08-06T000413.md` saying NO VERDICT, incident
  emitted, exit 65. Both test incidents are clarified in the stream by an
  `incident.corrected` event (deliberate acceptance tests).

## What failed / what was found and fixed mid-edict

- The first Herald smoke wrote its canary into `reports/` — the very
  directory the morning prompt reads to determine the digest window. A probe
  must never seed the record it probes. Fixed: canary moved to the vault
  root, launcher removes it after smoke, the morning prompt only honours
  date-named reports (`<YYYY-MM-DD>.md`), and the stray canary was removed
  from the real vault.
- `bd comment` takes positional text, not `-m`.

## Not done, deliberately

- **Nothing is committed.** Gate 1 applies to launcher files (both beads say
  so), and the verify.sh edit is a fort/ file: Warden + Overseer review is
  due. The Overseer was asked to review and decide commit + Warden pass.
- fortkit-r6x.4, zud.3, ugr.6, 1ca left OPEN with verified-state comments;
  close is the review's outcome, not mine.
- fortkit-ugr.6 item 3 (per-source digest status) is fort product code and
  stays with Manyhalls. The digest's per-fort `present` flag already exists
  and the Herald's prompt uses it.
- fortkit-zud.8 (staging root) remains the Overseer's; the launcher runs
  safely without it. H7 (systemd timer) remains Overseer-installed, and
  should land only after H6 (supervised maiden run).

## For the next Regent

The maiden runs are the real test: fortkit-r6x.6 (Herald, supervised) has
never run, and no Chronicler candidate has been worked. The first Herald
morning will exercise the window-determination logic against an empty
reports/ directory (falls back to 24h). If the ladder's second rung is ever
needed, note neither launcher can actually run GPT-5.6 Sol — they pass the
model name to claude; a codex-based rung would be new work, same as
warden.sh's ladder.

## Postscript, same session

The Overseer reviewed the summary and the working tree and approved: "all
this looks great to me. go ahead and commit." Committed as 525d736 on main,
path-scoped (the two launchers, verify.sh, bin/regent, civ/remember.md).
The Mayor's pending fort-record files (.beads/interactions.jsonl,
fort/handoffs/mayor-2026-08-04-d.md) and the ruflo residue in .claude/ were
left out deliberately — the first two are Manyhalls' record to commit, the
last is standing evidence (fortkit-agf). fortkit-r6x.4, zud.3 and 1ca closed
with the Overseer's approval on the record; Ilva retains covenant 4.5 review
over 525d736 by the fort's own standards. ugr.6 stays open for the
per-source digest remainder.
